/**
 * سجل العتبات — Financial Precision & KPI Hardening Pack v4.
 *
 * الغرض **إزالة الأرقام السحرية، لا تغيير قواعد العمل**. كل قيمة هنا هي القيمة التي
 * كانت مكتوبة حرفيًا في الكود قبل النقل؛ لم تُوحَّد قيمتان ولم تُبدَّل واحدة.
 *
 * ملف ثوابت لا نظام إعدادات: لا قراءة من قاعدة البيانات ولا واجهة ضبط. تحويلها إلى
 * إعدادات قابلة للتغيير من الشاشة قرار منتج مستقل.
 *
 * ── قاعدة التسمية ────────────────────────────────────────────────────────────
 * اسم الثابت يصف **غرضه** لا قيمته، حتى يتّضح متى يكون رقمان مختلفان مقصودين.
 * مثال حيّ: عتبتا «الذمة العالية» أدناه مختلفتان (5,000 و10,000) وهذا **صحيح** —
 * الأولى تحكم بطاقة قرار عن مدين واحد، والثانية تحكم قائمة تنبيهات لأعلى خمسة.
 * توحيدهما لمجرد اختلافهما كان سيغيّر قاعدة عمل بلا قرار.
 */

// ── عتبات الذمم ───────────────────────────────────────────────────────────────

/**
 * بطاقة القرار «أعلى ذمة مستحقة» في مركز القرار التنفيذي: تصف **مديـنًا واحدًا**
 * بغرض إجرائي (اتصل به، أرسل كشفًا). تجاوز هذا الحد يرفع أولويتها إلى HIGH.
 */
export const TOP_DEBTOR_CARD_HIGH_KWD = 5_000;

/**
 * تنبيهات «مديونية عالية» في الذكاء التنفيذي: قائمة **أعلى خمسة مدينين**، غرضها
 * المسح لا الإجراء الفردي — فعتبتها أعلى عمدًا كي لا تمتلئ القائمة بتنبيهات HIGH.
 */
export const HIGH_OUTSTANDING_ALERT_HIGH_KWD = 10_000;

// ── نوافذ الانتهاء (بالأيام) ──────────────────────────────────────────────────
//
// أرقامٌ متساوية لكنها لأغراض مختلفة، فتبقى ثوابت منفصلة: تغيير نافذة تنبيهات
// المعدات يجب ألا يحرّك نافذة الاتفاقيات معها.

/** تغذية تنبيهات لوحة المعلومات: دفاتر المعدات. تشمل المنتهية أصلًا عمدًا. */
export const EQUIPMENT_REGISTRATION_ALERT_DAYS = 30;

/** تغذية تنبيهات لوحة المعلومات: مستندات الموظفين (إقامة/جواز/رخصة). */
export const EMPLOYEE_DOCUMENT_ALERT_DAYS = 30;

/** مؤشّر «صيانة مستحقة قريبًا» في وحدة الصيانة. */
export const MAINTENANCE_DUE_ALERT_DAYS = 30;

/** رقاقة «اتفاقيات تنتهي قريبًا» في شريط العمليات المعلَّقة. */
export const AGREEMENT_EXPIRY_ALERT_DAYS = 30;

/** رقاقة «عقود تنتهي قريبًا» في ملخّص اليوم بلوحة المعلومات. */
export const CONTRACT_EXPIRY_CHIP_DAYS = 30;

/**
 * نطاقات مركز انتهاء الوثائق — أربعة مستويات إلحاح لا نافذة واحدة.
 * (نطاقات تأمين المركبات 7/15/30 تبقى في `vehicleInsurance.status.ts`
 *  ضمن `INSURANCE_ALERT_DAYS`، فهي جزء من منطق حالة الوثيقة هناك.)
 */
export const EXPIRATION_CENTER_BANDS = { critical: 7, warning: 30, notice: 60, watch: 90 } as const;

// ── تقادم الذمم ───────────────────────────────────────────────────────────────

/** حدّ «حرج +90 يوم» في بطاقتَي ذمم العملاء والموردين. */
export const CRITICAL_AGEING_DAYS = 90;
