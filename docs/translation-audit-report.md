# Translation Audit Report — manarERP
Generated by scripts/translation-audit.mjs (read-only)
## Summary
- **Translation source files:** 1 (frontend/src/lib/i18n.ts — single-module dictionary; no JSON files)
- **Languages:** ar, en
- **Total namespaces (top-level):** 89
- **Total keys (union ar∪en):** 4012
- **Keys in ar:** 4012
- **Keys in en:** 4012
- **Distinct keys referenced in code:** 3267
- **Dynamic/computed key prefixes:** 5
- **Missing keys (referenced, undefined):** 0
- **Missing Arabic entries:** 0
- **Missing English entries:** 0
- **Duplicate keys:** 0
- **Potentially unused keys:** 677
- **Hardcoded Arabic literals:** 3169
- **Namespace issues:** 5
- **Similar keys / typos:** 41
- **Placeholder problems:** 0
- **Empty values:** 0
- **Structural anomalies:** 0

**Findings by severity:** CRITICAL 0 · HIGH 0 · MEDIUM 3174 · LOW 718
## Inconsistent namespace  (5)
- **[MEDIUM]** `page.{ forms ⇄ form }` — Sibling namespaces under 'page.' differ only by pluralisation ("forms" vs "form") — likely the same concept under two names.
  - File: `frontend/src/lib/i18n.ts` : 1328
  - Fix: Standardise on one spelling for this namespace and migrate all keys + their call sites.
- **[MEDIUM]** `inv.{ status ⇄ stats }` — Sibling namespaces under 'inv.' are near-identical ("status" vs "stats", edit distance 1) — possible split namespace.
  - File: `frontend/src/lib/i18n.ts` : 465
  - Fix: Standardise on one spelling for this namespace and migrate all keys + their call sites.
- **[MEDIUM]** `empty.{ cheque ⇄ cheques }` — Sibling namespaces under 'empty.' differ only by pluralisation ("cheque" vs "cheques") — likely the same concept under two names.
  - File: `frontend/src/lib/i18n.ts` : 2271
  - Fix: Standardise on one spelling for this namespace and migrate all keys + their call sites.
- **[MEDIUM]** `action.{ expenses ⇄ expense }` — Sibling namespaces under 'action.' differ only by pluralisation ("expenses" vs "expense") — likely the same concept under two names.
  - File: `frontend/src/lib/i18n.ts` : 2391
  - Fix: Standardise on one spelling for this namespace and migrate all keys + their call sites.
- **[MEDIUM]** `integrations.{ panel ⇄ label }` — Sibling namespaces under 'integrations.' are near-identical ("panel" vs "label", edit distance 2) — possible split namespace.
  - File: `frontend/src/lib/i18n.ts` : 3447
  - Fix: Standardise on one spelling for this namespace and migrate all keys + their call sites.

## Hardcoded UI text (Arabic literals outside the dictionary)  (3169)
- Caveat: some are intentional non-UI constants (e.g. AI intent-routing patterns in frontend/src/ai/*). Focus on page/component/dialog files first.
- `frontend/src/constants/kuwaitLocations.ts` — **196** literal(s); e.g. line 10: "مناطق سكنية"
- `frontend/src/ai/router.ts` — **156** literal(s); e.g. line 24: "مطابقة"
- `frontend/src/ai/skills/dashboard.ts` — **132** literal(s); e.g. line 11: "مهارة مؤشرات لوحة التحكم"
- `frontend/src/config/importEntities.ts` — **129** literal(s); e.g. line 33: "الموظفون"
- `frontend/src/lib/tafqeet.ts` — **125** literal(s); e.g. line 31: "واحد"
- `frontend/src/ai/skills/bankStatement.ts` — **119** literal(s); e.g. line 13: "مهارة كشف الحساب البنكي"
- `frontend/src/ai/skills/payroll.ts` — **119** literal(s); e.g. line 10: "مهارة تحليل الرواتب"
- `frontend/src/forms/shared/contractTranslations.ts` — **84** literal(s); e.g. line 2: "هندي"
- `frontend/src/ai/skills/contracts.ts` — **78** literal(s); e.g. line 10: "مهارة تحليل العقود"
- `frontend/src/ai/skills/expenses.ts` — **66** literal(s); e.g. line 11: "مهارة تحليل المصروفات"
- `frontend/src/utils/headerIntelligence.ts` — **64** literal(s); e.g. line 23: "ا"
- `frontend/src/print-templates/studio/TemplateStudioEditor.tsx` — **63** literal(s); e.g. line 235: "هذا الحقل مرتبط ببيانات المستند — استخدم لوحة الخصائص لتغييره."
- `frontend/src/print-templates/reference/quotations/QuotationBase.tsx` — **51** literal(s); e.g. line 19: "شركة المنار الدولية ذ.م.م"
- `frontend/src/ai/skills/reports.ts` — **46** literal(s); e.g. line 9: "مهارة استكشاف التقارير"
- `frontend/src/print-templates/engine/textStyleTypes.ts` — **44** literal(s); e.g. line 97: "عنوان المستند"
- `frontend/src/print-templates/components/BrandingDesignerPanel.tsx` — **43** literal(s); e.g. line 140: "عودة إلى خصائص التوقيع/الختم"
- `frontend/src/components/FastMonthlyExpenseDialog.tsx` — **41** literal(s); e.g. line 113: "لديك مصروف غير محفوظ في الصف الحالي. الإغلاق سيتجاهله. هل تريد المتابعة؟"
- `frontend/src/config/expenseCategories.ts` — **40** literal(s); e.g. line 30: "تشغيل عام"
- `frontend/src/forms/EmploymentContractTemplate.tsx` — **37** literal(s); e.g. line 38: "الأحد"
- `frontend/src/pages/BankAccountExplorer.tsx` — **37** literal(s); e.g. line 50: "تحويل بنكي"
- `frontend/src/pages/AIAssistant.tsx` — **36** literal(s); e.g. line 67: "لخّص آخر كشف حساب مستورد"
- `frontend/src/pages/BankReconciliation.tsx` — **35** literal(s); e.g. line 46: "الحساب البنكي"
- `frontend/src/print-templates/studio/templateStudioUtils.ts` — **35** literal(s); e.g. line 46: "قالب جديد"
- `frontend/src/print-templates/components/LayoutDesignerPanel.tsx` — **33** literal(s); e.g. line 121: "تراجع (Ctrl+Z)"
- `frontend/src/pages/BankStatementImport.tsx` — **32** literal(s); e.g. line 34: "رفع الملف"
  … and 161 more file(s).

## Similar keys / possible typo  (41)
- **[LOW]** `action.reject  ⇄  action.reset` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `stat.late_lbl  ⇄  stat.leave_lbl` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `field.inv.cat_name  ⇄  field.inv.mat_name` — Leaf segments differ by edit distance 1 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `field.inv.cat_name_en  ⇄  field.inv.mat_name_en` — Leaf segments differ by edit distance 1 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `error.cat_name_required  ⇄  error.mat_name_required` — Leaf segments differ by edit distance 1 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `btn.inv.new_po  ⇄  btn.inv.new_gr` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `btn.inv.new_po  ⇄  btn.inv.new_mi` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `btn.inv.new_gr  ⇄  btn.inv.new_mi` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `confirm.delete_po  ⇄  confirm.delete_gr` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `confirm.delete_po  ⇄  confirm.delete_mi` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `confirm.cancel_po  ⇄  confirm.cancel_mi` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `confirm.post_gr  ⇄  confirm.post_mi` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `confirm.delete_gr  ⇄  confirm.delete_mi` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `modal.inv.new_po  ⇄  modal.inv.new_gr` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `modal.inv.new_po  ⇄  modal.inv.new_mi` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `modal.inv.new_gr  ⇄  modal.inv.new_mi` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `modal.inv.detail_po  ⇄  modal.inv.detail_gr` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `modal.inv.detail_po  ⇄  modal.inv.detail_mi` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `modal.inv.detail_gr  ⇄  modal.inv.detail_mi` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `report.group.operations  ⇄  report.group.operational` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `confirm.backup.restore_file  ⇄  confirm.backup.restore_title` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `stat.maint.spare_parts_count  ⇄  stat.maint.spare_parts_cost` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `agreements.usage.col.count  ⇄  agreements.usage.col.amount` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `bank.explorer.import_data  ⇄  bank.explorer.import_date` — Leaf segments differ by edit distance 1 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `bank.explorer.reconcile_unmatched  ⇄  bank.explorer.reconcile_matched` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `bank.recon.deselect_all  ⇄  bank.recon.select_all` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `bank.recon.transaction_count  ⇄  bank.recon.transactions_count` — Leaf segments differ by edit distance 1 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `bank.salary_analytics.chip_last_3_months  ⇄  bank.salary_analytics.chip_last_6_months` — Leaf segments differ by edit distance 1 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `lbl.geom.offset_x  ⇄  lbl.geom.offset_y` — Leaf segments differ by edit distance 1 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `action.calib.copy_to  ⇄  action.calib.copy_to_n` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `lbl.calib.offset_x_hint  ⇄  lbl.calib.offset_y_hint` — Leaf segments differ by edit distance 1 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `a11y.calib.offset_x_mm  ⇄  a11y.calib.offset_y_mm` — Leaf segments differ by edit distance 1 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `sec.printing  ⇄  sec.pricing` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `intelv2.forecast.expected_31_60  ⇄  intelv2.forecast.expected_61_90` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `intelv2.forecast.due_31_60  ⇄  intelv2.forecast.due_61_90` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `finintel.aging.bucket_31_60  ⇄  finintel.aging.bucket_61_90` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `fc.aging.f_31_60  ⇄  fc.aging.f_61_90` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `dlg.force_delete.invoice.col_journal_entries  ⇄  dlg.force_delete.invoice.no_journal_entries` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `import.preview.filter_invalid  ⇄  import.preview.filter_valid` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
- **[LOW]** `payroll_bank.export.help_intro  ⇄  payroll_bank.export.help_outro` — Leaf segments differ by edit distance 2 — one may be a typo of the other.
  - File: `frontend/src/lib/i18n.ts`
  - Fix: Verify both are intentional; if one is a misspelling, converge call sites onto the correct key.
  … and 1 more (capped for readability; rerun and pipe to a file for the full list).

## Potentially unused key  (677)
- **[LOW]** `layout.tagline` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 11
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `layout.privacy_on` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 17
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `layout.privacy_off` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 19
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `layout.privacy_off_title` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 20
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `layout.privacy_rehide` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 21
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.contracts` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 76
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.employees` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 79
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.expenses` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 81
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.invoices` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 82
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.bank_analytics` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 84
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.accounting` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 87
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.reports` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 88
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.users` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 89
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.settings` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 91
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.inventory` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 92
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.group.core` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 95
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.group.financial` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 96
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.group.warehouse` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 98
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.group.system` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 99
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `nav.group.ai` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 100
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `status.draft` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 106
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `status.posted` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 107
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `status.cancelled` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 108
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `status.approved` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 109
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `status.rejected` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 110
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `status.pending` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 111
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `status.expired` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 112
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `status.partial` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 114
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `mod.contracts.title` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 117
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `mod.contracts.subtitle` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 118
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `mod.contracts.create` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 119
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `mod.customers.title` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 120
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `mod.customers.subtitle` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 121
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `mod.customers.create` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 122
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `mod.suppliers.title` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 123
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `mod.suppliers.subtitle` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 124
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `mod.suppliers.create` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 125
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `mod.equipment.title` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 126
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `mod.equipment.subtitle` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 127
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
- **[LOW]** `mod.equipment.create` — No static t()/translate() reference found. May still be reached via a computed key — verify before removing.
  - File: `frontend/src/lib/i18n.ts` : 128
  - Fix: Confirm no dynamic reference exists, then consider removing to reduce dictionary weight. Do NOT auto-delete.
  … and 637 more (capped for readability; rerun and pipe to a file for the full list).

## Not applicable in this architecture
- Orphan translation files (check 11): N/A — translations live in a single TypeScript module; there are no per-file/per-locale JSON files to be orphaned.
- Invalid JSON (check 12): N/A — no JSON translation files. Reframed as "Dictionary structural integrity" above; TypeScript compilation (tsc) already guards syntax.
- Backend translations: none exist — the audit is frontend-only by design.

---
_Read-only audit. Nothing was modified, renamed, created, or deleted._
