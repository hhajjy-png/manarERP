# Completed Features Archive — الموارد البشرية · الرواتب · المستحقات · الموظفون

Employees, payroll, the payroll bank-import chain, employee financial tabs and the employment contract.

**Archived from:** `PROJECT_STATE.md` → `## Completed Features` (the flat 126-row table that section held)
**Rotated on:** 2026-09-08 — *Completed Features Documentation Compaction & Archive v1*
**Entries in this file:** 10
**Classification spread:** A=1 · C=5 · D=2 · E=1 · F=1

> **Verbatim.** Every feature row below was MOVED, not rewritten — byte-identical to the row as it stood
> in `PROJECT_STATE.md` before compaction, including its stable tag, commit hashes, migration notes,
> validation results, Product Owner approvals and known limitations. Classification metadata lives in the
> index table below and never inside the historical rows themselves.

**Active reference:** `PROJECT_STATE.md` → `## Completed Features — Active Reference` describes what is
true of the system *today*. This file is the historical record and may describe behaviour that has since
been superseded — always prefer the active reference for current behaviour.

See `docs/history/completed-features/README.md` for the full index and search guidance.

---

## Index

| # | Feature | Stable tag | Class | Superseded by | Content SHA-256 |
|---|---------|-----------|-------|---------------|-----------------|
| 1 | Bank Payroll Export Engine — NBK Salary XLS Profile v1 | `stable-payroll-bank-export-nbk-salary-xls-v1` | C | — | `6828d07b4c9477b3` |
| 21 | Employee Financial Payroll Tab v1 | `stable-employee-financial-payroll-tab-v1` | C | — | `b35adb21f8e59bcf` |
| 22 | Payroll Bank Import — Transaction Details Format v1 | `stable-payroll-bank-import-transaction-details-format-v1` | C | — | `651813c64a602e0c` |
| 27 | Payroll Bank Import Assistant v1 | `stable-payroll-bank-import-assistant-v1` | C | — | `9ff0bfef7fd514fc` |
| 28 | Payroll Bank Import — Legacy Retirement Phase 1 | `stable-payroll-bank-import-legacy-retirement-phase1-v1` | C | — | `283da2a273b19060` |
| 62 | Payroll Analytics Explorer — Phase 6 (UI Modernization) | `stable-payroll-analytics-explorer-phase6-v1` | F | — | `805b98b4f7029c88` |
| 72 | Payroll Bank Import Phase 1 — Enterprise Package | `stable-payroll-bank-import-phase1-v1` | E | Payroll Bank Import Assistant v1 | `9744e2d6416d3905` |
| 112 | Employment Contract — Two-Page Layout Fix | `stable-employment-contract-two-page-layout-v1` | D | — | `23c0b3eb44028d43` |
| 113 | Employment Contract Form | `stable-employment-contract-form-v1` | D | — | `a2031e2af5cdff6b` |
| 117 | Payroll System | *(stable)* | A | — | `dbfb621df514dd67` |

---

## Archived entries (verbatim)

| Feature | Stable Tag | Summary |
|---------|-----------|---------|
| **Bank Payroll Export Engine — NBK Salary XLS Profile v1** | `stable-payroll-bank-export-nbk-salary-xls-v1` | **New read-only Payroll Bank Export module** that turns an **APPROVED** payroll month into a bank-ready salary transfer file. Profile-driven engine (`backend/src/modules/payrollBankExport`: types + service + controller + routes + `profiles/nbkSalaryXlsProfile.ts`) isolated per bank so Gulf Bank/KFH/Boubyan profiles can be added later without touching payroll logic; reuses `payroll.read` (no new permission key); registered at `/api/payroll-bank-export` (GET `/profiles`, GET `/preview`). **First supported profile: NBK Salary XLS.** **(1) True legacy `.xls` (BIFF8/OLE2)** — generated client-side with SheetJS (already a dependency; ExcelJS cannot write `.xls`). **(2) Template-preserving generation:** loads the **official NBK template** (`docs/exelform/Salary_File.xls`), bundled as a **cleaned, PII-free** base64 asset (`frontend/src/assets/nbkSalaryTemplateBase64.ts` — sample rows removed) and inserts **only** the approved payroll rows into "Salary Details"; **sheet names, headers, column order/count, widths (`!cols`), cell formats, merges (none), and the entire Bank Codes sheet are preserved verbatim** — verified zero drift vs the template. **(3) Numeric formatting matches the template exactly:** Payment Amount stays **General** (150 → "150", never "150.000" — no forced decimals, no trailing zeros, not text); Civil Id numeric `0` format; NBK accounts numeric; IBAN accounts stay text; KWD currency; Western digits. **(4) Validation blocks export** when data is missing: **Beneficiary Name must be ENGLISH-only** (no Arabic fallback, no transliteration, no generated names — missing English name blocks with «اسم الموظف الإنجليزي مطلوب للتصدير البنكي NBK»); also missing civil ID, missing account/IBAN, amount ≤ 0, currency ≠ KWD, or no approved rows. **No previous-month sample rows are ever exported** (template sample data cleared; rows come solely from approved payroll for the selected month/year). **(5) UI:** a **"التصدير البنكي"** tab under Salaries (`components/salaries/PayrollBankExport.tsx`) — NBK profile selector (only option in v1), month/year, validation panel, **preview table matching the exported rows**, employee-count + total salary, and generate/download `.xls`. Arabic, RTL, ExplorerKit, dark-mode. **Read-only:** single `payroll.findMany`; **no payroll/accounting/employee mutation, no approval changes, no accounting entries; no bank-import / bank-statement-import changes; no DB schema change; no migration.** **Export history deferred to Phase 2** (would need a new table — documented, not implemented). **Files: 15** (+1092): backend `payrollBankExport/*` (+ `app.ts` registration); frontend `api/payrollBankExport.ts`, `utils/payrollBankExportXls.ts`, `components/salaries/PayrollBankExport.tsx`, `assets/nbkSalaryTemplateBase64.ts`, `pages/Salaries.{tsx,css}`, and tests. **Validation:** prisma validate ✅ · backend/frontend/electron tsc ✅ · backend vitest **1417** ✅ · frontend vitest **841** ✅ (2 pre-existing unrelated `printWorkspace` failures — non-blocking) · build:back ✅ · build:front ✅. **Reviews:** Implementation ✅ · strict template-preservation ✅ · numeric-format ✅ · English-only Beneficiary Name ✅ · **Gemini Review: APPROVED ✅**. Note: the raw PII-bearing `docs/exelform/Salary_File.xls` was **intentionally NOT committed**; only the cleaned PII-free asset ships. Branched from `production` @ `194dcfb`; feature commit `68025c8`, merge commit `154a2f0`, stable tag `stable-payroll-bank-export-nbk-salary-xls-v1`. |
| **Employee Financial Payroll Tab v1** | `stable-employee-financial-payroll-tab-v1` | **Frontend-only, read-only** «المالية» tab added to the Employee details drawer (the generic `ResourcePage` explorer drawer). Introduces an **employee-only two-tab** drawer (`البيانات الأساسية` + `المالية`; **other modules' drawers untouched** — the basic-fields panel was hoisted into a shared `basicSection` and the tab bar is gated on `cfg.key === 'employees'`). The financial tab shows **(1) payroll summary** — current monthly salary (from the **Employee** record) + latest-payroll last-month & transfer status; **(2) payroll info** — payment method (latest payroll), masked bank account/IBAN (Employee `bankAccount`, last-4 only), employee status; **(3) payroll history** — latest **12**, newest-first (الشهر/السنة · الأساسي · البدلات · الاستقطاعات · الصافي · حالة الدفع · تاريخ الدفع). **Graceful degradation:** tiles/rows without real data are **hidden** (never rendered as «—»), and the «عرض جميع الرواتب» button is **hidden at zero records** — otherwise it deep-links to `/salaries` preselecting the employee via `location.state` (a race-free `pinnedEmployee` option keeps on-leave/terminated employees visible in the ACTIVE-only filter). **Lazy-loaded:** payroll is fetched **only when the tab is active** (the component mounts only then), keyed by employee id so switching employees never shows stale data, with an `alive` cleanup guard. **Reuses the existing `GET /api/payroll?employeeId=` endpoint (server-guarded by `payroll.read`); no backend/schema/API/migration/permission/security-sensitive changes; no payroll calculation, approval, bank-import, or accounting changes.** **Files: 4** — new `components/employee/EmployeeFinancialTab.tsx` + `.css`; modified `pages/ResourcePage.tsx` (employee tab bar + `drawerTab` reset effect + hoisted `basicSection`) and `pages/Salaries.tsx` (deep-link preselect + `pinnedEmployee`); +346/−10. Feature commit `f712a91`, merge commit `eb190a4`. Checkpoint tag `pre-employee-financial-profile` at `67fca5c`. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, full frontend vitest 702/704 (2 pre-existing `printWorkspace.test.tsx` failures on untouched `PrintWorkspace.css` — out of scope).** Reviews: `/simplify` (3 fixes — hoisted `basicSection`, dropped a needless `useMemo`, aligned the `ON_LEAVE` label with the app-wide renderer), `/code-review` (2 local reviewer agents; CodeRabbit CLI unavailable — **0 critical; fixed 2 warnings**: summary cards degraded like "no payroll" on a failed fetch → gated on `!error`, and a misleading Salaries filter for a non-ACTIVE deep-linked employee → `pinnedEmployee`), `/security-review` **no vulnerabilities** (React auto-escaping; `location.state` not attacker-controllable; the reused endpoint is already `payroll.read`-guarded → **no new IDOR**; masking **reduces** exposure). **Gemini review: SKIPPED** — Gemini CLI `IneligibleTierError` (the free Code Assist tier is no longer supported by the installed CLI build and no API key is configured); this is a **tooling/account-eligibility** issue, **not** a code-quality one. The ready-to-run review packet was saved for a later authorized run. |
| **Payroll Bank Import — Transaction Details Format v1** | `stable-payroll-bank-import-transaction-details-format-v1` | **Frontend-parse-only, additive** support for one new official bank Excel layout in Payroll Bank Import: the monthly **"File Upload - Transactions Details"** report (downloaded after payroll processing). **Root cause it addresses:** the client parser (`parseWorkbook`) had 3 paths — P1 monthly sheet names (`mar-2025`), P2 an `All_Transactions` sheet, and a fallback that reads the **first sheet assuming the header is row 1**. The new report has its header **~row 6** (title + metadata above) and an **arbitrary sheet name** (`Sheet0`), so it hit the fallback where the title row became the "header" and `payrollMonth/Year` defaulted to `0/0` → every row failed validation (`شهر الرواتب غير صحيح`) → format effectively rejected. **Fix:** added **Priority 3** (runs only after P1/P2 decline, so existing formats are byte-identical): scan each sheet's first 15 rows for the header signature (`transaction id`, `beneficiary account number`, `payment amount`, `payment date`, `status`), parse data **relative to the detected header row** (`sheet_to_json({ range: headerRow })`), auto-detect the template (resolves to the existing **NBK** map, which already covers every column), and **derive payroll month/year from each row's Payment Date** via a flexible date parser (day-first `DD/MM/YYYY`/`DD-MM-YYYY` + Excel serials, **timezone-safe** — re-anchors `cellDates` local Dates to UTC midnight so month-boundary dates don't roll on the UTC+3 Kuwait host). **Duplicate protection unchanged:** the layout always carries a **Transaction ID**, which drives the existing backend `existingTxIds` guard, so re-importing the **cumulative** monthly file inserts only genuinely new rows (0 duplicates) — no backend/validator change. **Matching unchanged** (civilId→account→name). Parser **extracted** from `PayrollBankImport.tsx` into `payrollBankImportParser.ts` for unit-testability; the page's supported-formats hint + "no rows" error updated to mention the layout. **UI/workflow, Bank Statement Import, Payroll Bank Analytics, DB schema, migrations, and both existing formats all untouched.** **Real-file smoke test** (`File_Details_06Jul2026_164109.xlsx`, live backend preview, no DB write): NBK · 17 rows · matched 17/0 (`CIVIL_ID_100`) · valid 17/0/0 · 17/17 Transaction IDs · month **07/2026** from Payment Date · **0** "شهر الرواتب غير صحيح". **Files: 3** — new `pages/payrollBankImportParser.ts`, new `__tests__/payrollBankImportParser.test.ts` (9 tests incl. TZ regression), modified `pages/PayrollBankImport.tsx`; +614/−321. Feature commit `ce54fd0`, merge commit `51bff73`. Checkpoint tag `pre-payroll-bank-import-transaction-details-format`. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, backend payroll vitest 99/99 ✅, full frontend vitest 702/704 (2 pre-existing `printWorkspace.test.tsx` failures on untouched `PrintWorkspace.css` — out of scope).** Reviews: `/simplify` (extracted `normalizeRow`, trimmed comments), `/code-review` (local reviewer; CodeRabbit CLI unavailable — **found & fixed 1 critical timezone bug**, verified under `TZ=Asia/Kuwait`). **Gemini review: APPROVED ✅.** |
| **Payroll Bank Import Assistant v1** | `stable-payroll-bank-import-assistant-v1` | **Additive, preview-only, non-blocking** intelligence layer for the `payrollBankImport` module. Runs as a post-pass in `service.preview()`; **`execute()` unchanged and still re-runs full server-side validation; `canExecute` semantics untouched** (a pure function of blocking errors + unmatched rows — the assistant may only flip a row's display status `valid`→`warning` and populate `row.assistantWarnings` + `summary.assistant`). Offline/deterministic, no AI/cloud. **Features:** (1) **IBAN validation** — Kuwait IBAN in new `ibanValidator.ts` (format + country `KW` + length + ISO 7064 **mod-97 checksum** + bank-code extraction); invalid → warning. (2) **Match confidence** — `matchConfidencePct` per row (100/90/50), weak name-only/manual warnings, and **employee-index/in-file collisions** (duplicate civilId / bankAccount / IBAN-in-file; code checked) — matching **priority unchanged**. (3) **Payroll variance report** (`variance.ts`) — imported/matched/unmatched totals, employee counts, previous comparable-period total, variance amount + %, per-period breakdown, and **missing expected employees** (only when a reliable prior period exists — no invented data). (4) **Salary anomaly detection** — ±30% vs `employee.salary` (fallback: last payment), division-guarded; high/low warnings (amount 0 stays the existing hard error). (5) **Duplicate payroll detection** — same employee+month in DB (`danger`), same employee repeated in file, month re-import; **`transactionId` unique hard guard unchanged**. (6) **Missing/extra employees** — missing surfaced conservatively; unmatched rows keep existing blocking behavior. (7) **Import quality score** 0–100 (`quality.ts`), informational. **UI** (`PayrollBankImport.tsx`): assistant panel (quality score, IBAN validity, variance report, warnings grouped by type, collisions, missing employees) + per-row 🤖 markers; wizard structure unchanged, Arabic-first. **Architectural isolation:** logic confined to `payrollBankImport/*`; imports **nothing** from generic `import/warnings`, `bankStatementImport`, or `salaries`. **Bank Statement Import untouched; Generic Data Import / Smart Import Assistant untouched; no Prisma/schema/migration changes; legacy `/api/salaries/bank-import/*` routes remain removed** (regression-guarded by a test). **Files: 14** (payrollBankImport + its FE api/page) — 7 new (`ibanValidator.ts`, `variance.ts`, `quality.ts`, `assistant.ts` + 3 tests) + 7 modified (`types.ts`, `matcher.ts`, `previewBuilder.ts`, `validators.ts`, `service.ts`, `api/payrollBankImport.ts`, `pages/PayrollBankImport.tsx`); +1268/−17. **+32 tests** (Kuwait IBAN checksum, weak match, collisions, salary anomaly, duplicate payroll, variance, quality, execute re-validation, transactionId guard unchanged, legacy routes removed). Feature commit `539dcb4`, merge commit `2062487`. Checkpoint tag `pre-payroll-bank-import-assistant-v1` at `ae85d2d`. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, backend vitest 1294/1294 (72 files), frontend vitest 663/665, build:back ✅, build:front ✅.** Reviews: `/simplify` (reuse & altitude clean; applied 2 efficiency wins — collision `Set` lookups + reuse of normalized IBAN), `/code-review` (local code-reviewer agent; CodeRabbit CLI unavailable — verified non-blocking contract, mod-97 correctness, variance null-safety, anomaly math; removed one unreachable `SALARY_ZERO` branch), `/security-review` **no new HIGH/MEDIUM findings** (same auth scope, React auto-escaping, linear anchored regexes, typed Prisma selects, execute re-validation intact). **Known unrelated issue:** 2 pre-existing `printWorkspace.test.tsx` failures (assert untouched `PrintWorkspace.css`) — frontend untouched, out of scope. |
| **Payroll Bank Import — Legacy Retirement Phase 1** | `stable-payroll-bank-import-legacy-retirement-phase1-v1` | **Backend-only architecture cleanup** retiring the legacy salaries-embedded bank-import HTTP entry path in favor of the dedicated go-forward `payrollBankImport` module. **Legacy routes removed:** `POST /api/salaries/bank-import/preview` and `POST /api/salaries/bank-import/execute` (the second wrote `salary_payments` gated only by the generic `import.create`). Their only caller was the already-orphaned, unrouted `frontend/src/pages/BankImport.tsx` (not in `App.tsx`), so no live UI path was affected. **Shared helpers extracted** into new `backend/src/modules/salaries/salaries.dateHelpers.ts` (`parseSheetMonth`, `parseMonthColumn`, `formatSourceMonth` + `MONTH_NAMES`, moved **verbatim**) so the **live Bank Salary Analytics** service (`salaries.bankAnalytics.service.ts`, which only consumed `formatSourceMonth`) no longer depends on the legacy bank-import service; `salaries.bankImport.service.ts` and the test imports were repointed to the new module. Removed the now-unused `salariesBankImportService` import and `canImportCreate` binding from `salaries.routes.ts`. **Permission-scoping gap closed:** importing bank salary files now requires `payrollBankImport.create` exclusively — the generic `import.create`/`import.read` keys remain **untouched** (still used by generic Data Import + integrations); read-only `/api/salaries/bank-payments/*` analytics routes **unchanged**. **Legacy `salaries.bankImport.service.ts` intentionally kept** (now route-less/inert) for deletion in a later phase. **Live PayrollBankImport module (`/api/payroll-bank-import/*`) and Bank Salary Analytics both unaffected; no Prisma/schema/migration changes; no frontend changes.** **Files: 5** (backend/salaries only) — 1 new (`salaries.dateHelpers.ts`) + 4 modified (`salaries.bankImport.service.ts`, `salaries.bankAnalytics.service.ts`, `salaries.routes.ts`, `__tests__/salaries.bankImport.test.ts`); +65/−70. Feature commit `3899045`, merge commit `a58ebf3`. Checkpoint tag `pre-payroll-bank-import-legacy-retirement-phase1` at `712ba74`. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, backend vitest 1262/1262 (69 files), frontend vitest 663/665, build:back ✅, build:front ✅.** Reviews: `/simplify` clean (cross-module `formatSourceMonth` duplication noted as deferred tech debt — fixing it would violate module isolation), `/code-review` clean (local code-reviewer agent; CodeRabbit CLI unavailable in env), security review **no new findings — attack surface reduced, authorization tightened**. **Known unrelated issue:** 2 pre-existing `printWorkspace.test.tsx` failures (assert untouched `PrintWorkspace.css`) — pre-existing on `712ba74`, frontend untouched, out of scope. |
| **Payroll Analytics Explorer — Phase 6 (UI Modernization)** | `stable-payroll-analytics-explorer-phase6-v1` | Full visual redesign of `BankSalaryAnalytics.tsx` using the exact visual language of Bank Statement Explorer Phase 5C (`BankReconciliation.tsx`). **CSS:** New `BankSalaryAnalytics.css` (860 lines) with `.psa-*` prefix scoped to this page; all design tokens from CSS vars (`var(--primary)`, `var(--accent)`, `var(--surface)`, `var(--border)`, `var(--text)`, `var(--shadow)`, `var(--green)`, `var(--amber)`, `var(--red)`, etc.); `@keyframes psa-shimmer` skeleton animation; `@keyframes psa-panel-in` drawer slide-in. **Package A — Professional Page Header:** `.psa-page-header` with icon, title, subtitle, and export dropdown button (`position:relative` wrapper + absolute `.psa-export-menu`; `document.mousedown` close via `exportMenuRef`; three export formats: Excel, CSV, Print). **Package B — Collapsible Filter Panel:** `.psa-filter-panel` grouped into 4 labeled sections (فترة / موظف / بنك / فرز); `draftFilters` form state + `appliedFilters` trigger pattern; toggle show/hide with active-filter count badge. **Package C — Quick Filter Chips:** `.psa-chip-row` with 10 `QuickChip` options (`'all'|'1m'|'3m'|'6m'|'year'|'lastyear'|'highest'|'lowest'|'newest'|'oldest'`); `handleQuickChip()` modifies `appliedFilters` for time chips and `txSortBy`/`txSortDir` for sort chips; active chip highlighted via `.psa-chip.active`. **Package D — 8 KPI Cards:** `.psa-kpi-grid` 4-col responsive grid (2-col at ≤900px); cards: إجمالي المبالغ المحوّلة, عدد عمليات التحويل, الموظفون المدرجون (clickable → opens filter), الأشهر المرصودة, متوسط الراتب, أعلى شهر مبلغاً, آخر استيراد, أشهر بزيادة; all financial values wrapped in `<PrivateAmount>`. **Package E — Summary Bar:** `.psa-summary-bar` flex row with 4 values separated by `.psa-summary-divider` (1px × 28px); shows إجمالي/عدد/موظفون/متوسط for current filtered view. **Package F — 6 Charts (Collapsible Section):** Toggle button collapses/expands chart section. Chart 1: Monthly totals BarChart (blue). Chart 2: Employee count per month BarChart (green). Chart 3: Monthly variance BarChart with `Cell` (green=positive, red=negative). Chart 4: Top 5 employees horizontal BarChart (purple). Chart 5: Salary range grouped BarChart (highest=green, avg=blue, lowest=red). Chart 6: Cumulative total BarChart (teal). `ChartTooltip` custom component (dark `#1a2535` background). Cumulative data computed client-side via `reduce`. **Package G — Modern Transactions Table:** `.psa-table-wrap` with sticky `<thead>`; alternating `.psa-row-even`/`.psa-row-odd` rows; `.psa-sort-th` / `SortTh` sub-component with sort arrows; clickable rows highlight selected employee. **Package H — Employee Details Drawer:** `.psa-details-side` 380px sticky side panel; `max-height: calc(100vh - 110px)`; `overflow-y: auto`; slide-in `@keyframes psa-panel-in`; shows employee name, bank account, salary history months, total, stats; `<PrivateAmount>` on all financial values. **Package I — Smart Analytics Insight Cards:** `.psa-insight-grid` 4-col responsive grid; 8 insight cards: أكبر تحويل فردي, أصغر تحويل فردي, أعلى موظف راتباً, أكثر موظف تحويلاً, الأشهر الإيجابية, متوسط الموظفين شهرياً, أحدث شهر بيانات, التغيير الإجمالي (% first→last month). **Package J — Data Quality Center:** `.psa-quality-panel` amber background; warnings derived from analytics data (unmatched employees, months without employeeCount, months with variance < −5000 KWD). **Package K — Skeleton Loading:** `Skel` sub-component (`.psa-skel` with shimmer animation); skeleton rendered during `analyticsLoading` state for KPI grid, insight grid, charts. **Package L — Empty States:** `.psa-empty` styled empty states for no filters applied, no data found, and no employee selected states. **Package M — CSV Export:** Client-side CSV from `txData.data`; UTF-8 BOM (`﻿`) for Arabic compatibility in Excel; Arabic column headers (التاريخ/الموظف/المبلغ/البنك/رقم الحساب/الشهر). **Package N — Authenticated Excel Export:** Existing Axios `responseType:'blob'` download preserved; `handleExport(empId?, format)` unified function dispatches to Excel/CSV/Print. **Package O — PrivateAmount Coverage:** All KPI cards, summary bar, insight cards, drawer financial values, chart tooltip amounts wrapped in `<PrivateAmount>`. **Backend untouched:** No changes to API endpoints, Prisma schema, migrations, permissions, or constants. **Tests: 1120/1120 backend (57 files, unchanged). Frontend tsc ✅ 0 errors. Electron tsc ✅ 0 errors. build:back ✅. build:front ✅. Gemini APPROVED ✅. 2 files changed: `frontend/src/pages/BankSalaryAnalytics.tsx` (~1800 lines, rewritten from 1250 lines), `frontend/src/pages/BankSalaryAnalytics.css` (860 lines, new file). Commit `b57689a`.** |
| **Payroll Bank Import Phase 1 — Enterprise Package** | `stable-payroll-bank-import-phase1-v1` | Full-stack bank payroll import workflow integrated into the Integrations Center. **Architecture:** Backend-native matching engine + 5-step frontend wizard; no new Prisma models; reuses existing `SalaryPayment` table (`bankName` field stores `templateName`). **Supported banks (priority-ordered detection):** Gulf Bank, Ahli United, KFH, Warba, Boubyan, NBK — each with unique column signatures in Arabic/English (configurable header arrays in `BANK_CONFIGS`). **Backend — 11 files in `payrollBankImport/`:** `types.ts` (15 interfaces/types: `BankTemplate`, `ParsedBankRow`, `MatchResult`, `PreviewRow`, `PreviewSummary`, `ImportReportRow`, `ImportReport`, etc.); `excelParser.ts` (`BANK_CONFIGS` registry, `detectBankTemplate`, `parseExcelBuffer` via ExcelJS — server-side validation only, not used in Phase 1 flow); `matcher.ts` (`matchEmployee` — 4 confidence levels: `CODE_100` → `CIVIL_ID_100` → `BANK_ACCOUNT_90` (IBAN suffix) → `MANUAL` (name)); `validators.ts` (9 rule-based validators: amount > 0, month 1–12, year 2000–2200, currency KWD/USD/EUR, payroll date format, duplicate detection, TERMINATED employee check, currency/amount type, beneficiary name present); `previewBuilder.ts` (`buildPreview()` — `canExecute = invalid === 0 && unmatched === 0`; TERMINATED employees add extra error); `service.ts` (`preview(input)` + `execute(input, req)` — atomic `prisma.$transaction`, creates `SalaryPayment` records, deduplicates by `employeeId+payrollMonth+payrollYear`); `reportBuilder.ts` (`buildImportReportExcel` — ExcelJS with summary + details sheets; `buildImportReportHtml` — delegates to report engine with branding and A4-landscape profile); `schema.ts` (3 Zod schemas: `PreviewInputSchema`, `ExecuteInputSchema`, strict `ImportReportSchema` with bounded string lengths + enum constraints); `controller.ts` (4 handlers: `previewHandler`, `executeHandler`, `reportExcelHandler`, `reportPdfHandler`); `routes.ts` (4 routes: `POST /api/payroll-bank-import/preview`, `POST /api/payroll-bank-import/execute`, `POST /api/payroll-bank-import/report/excel`, `POST /api/payroll-bank-import/report/pdf`). **Permissions:** `payrollBankImport.read` (SYSTEM_ADMIN/ACCOUNTANT), `payrollBankImport.create` (SYSTEM_ADMIN/ACCOUNTANT), `payrollBankImport.export` (SYSTEM_ADMIN/ACCOUNTANT). **Frontend — 2 files:** `api/payrollBankImport.ts` (typed API client, 4 functions, `exportReport*` use `responseType: 'blob'`); `pages/PayrollBankImport.tsx` (~700 lines, 5-step wizard: Upload → Preview → Confirm → Done; client-side XLSX parsing via `sheetHeaders`/`sheetRows` helpers; multi-sheet detection: monthly sheets → All_Transactions → first sheet fallback; 8 KPI cards; full row table with `MatchBadge` + `StatusPill`; "show all 50+" toggle; explicit checkbox gate before execute; PDF/Excel export on Done step; RBAC-gated). **Integrations Center wiring:** `Integrations.tsx` `handleRun` navigates to `card.targetRoute` when set — clicking تشغيل on payroll-bank-import card navigates to `/payroll/bank-import`. **Security hardening:** Formula injection guard (`isSafeValue()` rejects cells starting with `=`, `+`, `-`, `@`, `\t`, `\r`); strict `ImportReportSchema` replaces `z.any()` on report export endpoints; both report handlers use `ReportExportSchema.safeParse()` before processing; `reportPdfHandler` sends `Content-Disposition: attachment; filename="payroll-import-report.html"` (prevents inline rendering) + `Content-Security-Policy: sandbox; default-src 'none'` (XSS sandbox); HTML table template uses `fmtCell()→esc()` on all cell values. **Tests:** 67 backend unit tests across 5 groups (excelParser/BANK_CONFIGS, validators, matcher, previewBuilder, TERMINATED employee detection). **18 files changed** (13 new backend files, 2 new frontend files, 3 modified: `App.tsx`, `Integrations.tsx`, `seed.ts`; 1 modified backend: `app.ts`). 2769 insertions. No Prisma schema changes. No migrations. No Electron IPC changes. **Tests: 919/919 backend (50 files, +67 tests) + 588/588 frontend (28 files, unchanged). Gemini APPROVED WITH MINOR NOTES ✅. Security fixes applied (CSP sandbox, strict Zod schemas, attachment download).** |
| **Employment Contract — Two-Page Layout Fix** | `stable-employment-contract-two-page-layout-v1` | Replaced flowing wrapper with two explicit sibling page containers. Chromium print fragmentation fix. |
| **Employment Contract Form** | `stable-employment-contract-form-v1` | Kuwait Public Authority for Manpower bilingual form. 16 articles, 2-page A4. Backend read-only GET (`forms.read`). No migration. |
| **Payroll System** | *(stable)* | Full payroll management + approval workflow + payslip print layout. |
