# Completed Features Archive — المالية · الفواتير · المصروفات · المحاسبة · البنوك

Invoices, expenses, accounting and GL, financial and statement centres, banking, bank-statement import and the Bank Account Explorer.

**Archived from:** `PROJECT_STATE.md` → `## Completed Features` (the flat 126-row table that section held)
**Rotated on:** 2026-09-08 — *Completed Features Documentation Compaction & Archive v1*
**Entries in this file:** 31
**Classification spread:** A=1 · B=1 · C=11 · D=4 · E=6 · F=8

> **Verbatim.** Every feature row below was MOVED, not rewritten — byte-identical to the row as it stood
> in `PROJECT_STATE.md` before compaction, including its stable tag, commit hashes, migration notes,
> validation results, Product Owner approvals and known limitations. Classification metadata lives in the
> index table below and never inside the historical rows themselves.

**Active reference:** `PROJECT_STATE.md` → `## Completed Features — Active Reference` describes what is
true of the system *today*. This file is the historical record and may describe behaviour that has since
been superseded — always prefer the active reference for current behaviour.

See `docs/history/completed-features/README.md` for the full index and search guidance.

---

## Index

| # | Feature | Stable tag | Class | Superseded by | Content SHA-256 |
|---|---------|-----------|-------|---------------|-----------------|
| 0 | Historical Financial Data Readiness v1 | `stable-historical-financial-readiness-v1` | C | — | `af5291319b4ee8d1` |
| 3 | Invoice Governance & Collection Date Safety Pack v1 | `stable-invoice-governance-collection-date-safety-v1` | C | — | `13821269fd394210` |
| 4 | Expenses Governance & Classification Standardization Pack v1 | `stable-expenses-governance-classification-v1` | C | — | `f8aa70f8989841e0` |
| 9 | Historical Collection Date Correction v1 + Cheque Default | `stable-collection-date-correction-v1` | C | — | `a4d2ebc69176693c` |
| 10 | Invoice Collection Date v1 | `stable-invoice-collection-date-v1` | C | — | `5aa21ecf9602fd35` |
| 11 | Pricing Unit «معالجات» (mini) | `stable-pricing-unit-mualajat-v1` | D | — | `d6087592494fc370` |
| 13 | Invoice Fast Entry Mode v1 | `stable-invoice-fast-entry-mode-v1` | C | — | `d4d88a688871c9ae` |
| 14 | Expenses Operations Enhancement Pack v1 | `stable-expenses-operations-enhancement-v1` | C | — | `84ce694d12111813` |
| 15 | Expenses Reliability & Usability Pack v1 | `stable-expenses-reliability-usability-v1` | C | — | `20250f23a8e1af58` |
| 19 | Smart Transaction Presentation Engine v1 | `stable-smart-transaction-presentation-engine-v1` | C | — | `5454cbf417ab0d07` |
| 20 | Bank Account Explorer Visual Refresh v2 | `stable-bank-account-explorer-visual-refresh-v2` | C | — | `e7a662e0ca0474e6` |
| 25 | Bank Account Explorer Default Sort Fix v1 | `stable-bank-account-explorer-default-sort-v1` | D | — | `4ae451bfbb447e2c` |
| 32 | Financial Bug Fix Pack C1–C4 | `stable-critical-financial-bugs-c1-c4-v1` | F | — | `76dbc315322847d7` |
| 48 | Bank Account Explorer — Phase E (Premium Executive Experience — Final Polish) | `stable-bank-account-explorer-phase-e-v1` | C | — | `a97a2c24ce3e0010` |
| 49 | Bank Account Explorer — Phase D (Executive UX & Visual Excellence) | `stable-bank-account-explorer-phase-d-v1` | E | Bank Account Explorer — Phase E (Premium Executive Experience — Final Polish) | `8bf8c1ab40f7f590` |
| 50 | Bank Account Explorer — Phase C (Stability + Professional Transaction Explorer) | `stable-bank-account-explorer-phase-c-v1` | E | Bank Account Explorer — Phase E (Premium Executive Experience — Final Polish) | `bb043596e378fcd3` |
| 51 | Bank Account Explorer — Polish Phase 1 | `stable-bank-account-explorer-polish-phase1-v1` | E | Bank Account Explorer — Phase E (Premium Executive Experience — Final Polish) | `1875cdf43e8f4d26` |
| 52 | Bank Account Explorer — Phase B | `stable-bank-account-explorer-phase-b-v1` | E | Bank Account Explorer — Phase E (Premium Executive Experience — Final Polish) | `4b4389694d160f39` |
| 53 | Bank Statement Import — Phase A Engine (Coverage Analysis + Gemini Polish) | `stable-bank-statement-import-phase-a-v1` | E | Bank Statement Incremental Import v2 | `1b3df23451f6ecf0` |
| 54 | Unified Bank Account Timeline Architecture | `stable-bank-unified-timeline-architecture-v1` | B | — | `88248b249135d1d3` |
| 55 | Bank Statement Incremental Import v2 | `stable-bank-statement-incremental-import-v2` | F | — | `baebc1932a44cb90` |
| 63 | Bank Statement Explorer Phase 5C — Export, Delete, Multi-Select & UX | `stable-bank-statement-explorer-phase5c-v1` | F | — | `849a086c5e5f3ea9` |
| 64 | Bank Statement Import Intelligence & Validation Polish | `stable-bank-import-validation-polish-v1` | F | — | `0db4738edab1f679` |
| 71 | Bank Statement Import & Smart Reconciliation Phase 1 | `stable-bank-statement-import-phase1-v1` | E | Bank Statement Incremental Import v2 | `6bcdb7f2e860815f` |
| 78 | Financial Center Phase 2 — GL Report, Trial Balance, Aging, Journal Book, Financial Reports, Financial Dashboard | `stable-financial-center-phase2-v1` | F | — | `122d439745b0ed25` |
| 79 | Statement Center Phase 1 — Customer & Supplier Statements | `stable-statement-center-phase1-v1` | F | — | `390171c4b24cbb98` |
| 81 | Invoice & Expenses Operations Pack | `stable-invoice-expenses-operations-pack-v1` | F | — | `ea30649807840ed4` |
| 82 | Accounting Completeness H1 — Purchase Invoice GL Posting | `stable-accounting-completeness-h1-v1` | F | — | `8ea05679d20e9ff7` |
| 101 | Contract Profitability Dashboard — Phase 1 | `stable-contract-profitability-phase1-v1` | D | — | `9fab006384bde984` |
| 102 | Financial Receivables Center — Phase 1 | `stable-financial-receivables-phase1-v1` | D | — | `8e5ab345d75a15f6` |
| 118 | Accounting Module | *(stable)* | A | — | `5e2bcbbb98e4a882` |

---

## Archived entries (verbatim)

| Feature | Stable Tag | Summary |
|---------|-----------|---------|
| **Historical Financial Data Readiness v1** | `stable-historical-financial-readiness-v1` | **Enables back-dated (2024/2025) financial entries that post to the correct accounting period without contaminating current-year figures.** Backend + frontend; **no DB schema change, no migration; existing data unchanged.** **(1) Posting-date correctness** — accounting date = document date (invoice `issueDate` / expense date / payment date / payroll month-end), never `new Date()`/`createdAt`; entry-number year derives from the document date (`gl.service.generateEntryNumber`). **(2) Payroll date semantics** — `resolvePayrollPostingDate(payroll, explicitPaymentDate)`: explicit `paymentDate` if given, else month-end of the payroll month; used by both ledgers. **(3) GL reversal** — `reverseGL`/`accounting.reverseJournalEntry` post a balanced contra entry on the original date (or an explicit `reversalDate`), block double-reversal, and audit. **(4) Historical aging** — `outstanding = total − Σ payments(date ≤ asOf)`; `Invoice.paidAmount` removed from every as-of path; unified across `financial` (`receivablesAging`/`customerBalances`, `aging.utils`) and `reports` modules and proven consistent by test. **(5) Financial Period Context** — session-only React context (`FinancialPeriodContext`, no localStorage) with presets / per-year / custom range, seeded into all Financial Center tabs (StrictMode-safe fill-empty-on-mount + overwrite-on-change); YTD default, persists on nav, resets on restart. **(6) Flow vs current-status KPI classification** (Phase-7 query safety) so Dashboard/Executive show period flow for the selected year while current-status metrics stay current. **(7) Period lock** — `assertPeriodOpen` guard at the central GL write layer (`createBalancedJournal`, `transactions.postEntry`), **backend-enforced** (403 for non-override users across invoice/expense/payment/payroll/journal/reversal); `finance.lockBeforeDate` setting; `financial.overrideLock` permission is **deliberately manual-grant only** (excluded from broad auto-seed; SYSTEM_ADMIN bypass) and every override writes a `PERIOD_LOCK_OVERRIDE` audit; new `journal.reverse` permission. Actor comes from AsyncLocalStorage (`requestContext`) — no body/query spoofing. **(8) Historical entry UX + audit** — `recordHistoricalEntry` (via existing AuditLog JSON columns; `lateEntryReason` audit-only), current-status badges, historical-date notices. **(9) Payment-before-issue** — accepted as an advance with a warning + `PAYMENT_BEFORE_ISSUE` audit (not rejected). **(10) Invoice historical numbering** — client-supplied `MN-INV-YYYY-NNNNN`, per-year MAX sequence, year derived from `issueDate` (no migration, no renumbering). **(11) Date parsing** — shared `parseImportDate` (DD/MM/YYYY / ISO / Excel serial) across all import validators. **(12) Report/export period awareness** — export filenames carry the period range (`generateExportFileName` period segment); screen data = export data (same `build()`); P&L per-year with no cross-year mixing. **(13) GL range fix (surfaced during UAT)** — `financial.service.getGlReport`/`getGlStatement` had two separate `...(cond && { date: {…} })` spreads whose keys collided, silently dropping the lower `gte` bound so period movement/closing summed all history ≤ `toDate`; merged into a single `date` object with both bounds; two regression tests assert both bounds survive. **(14) Print safeguards** — defensive `@media print` fallbacks (`background:transparent/#fff !important`, `border:0`, `padding:0`) so print output has no dark frame. **Files: 94** (+4437/−444). **Validation:** prisma validate ✅ · backend tsc ✅ · frontend tsc ✅ · electron tsc ✅ · backend vitest **1550/1550** ✅ · frontend vitest **942/942** ✅ (**0 failures**). **Reviews:** UAT **14/14** ✅ · `/code-review` (0 Critical/High/Medium) ✅ · security review **APPROVED** ✅ · **Gemini: APPROVED FOR COMMIT** (1 Low — theoretical invoice-numbering race under high concurrency, non-blocking in single-user Electron). **Doc note:** Gemini mis-attributed the date-collision fix to `reports.service.ts`; the actual fix is in `financial.service.ts` (`getGlReport`/`getGlStatement`) — `reports.service.ts` carries the separate aging `paidAmount`→`payments≤asOf` change. **Deferred (documented):** Bank Explorer opening/closing period summary (`docs/BANK_EXPLORER_HISTORICAL_READINESS.md`); Cheques outside Period Lock (intentional); historical import does not auto-post to GL (`docs/HISTORICAL_IMPORT_POSTING_RISK.md`). Scope: **Historical-Ready for Core Financial Modules — excluding Bank Explorer and Cheques.** Branched from `production` @ `f2c5503`; feature commit `9607553`, merge commit `e2e0857`, stable tag `stable-historical-financial-readiness-v1`. |
| **Invoice Governance & Collection Date Safety Pack v1** | `stable-invoice-governance-collection-date-safety-v1` | **Paid-invoice edit governance + safe collection-date correction action** — backend + frontend; **no DB schema change, no migration, no API contract change, no accounting-model / print-template change.** **(1) Paid invoices are read-only via the normal edit path:** `invoices.service.update()` rejects a fully-paid (`PAID`) invoice **before any write** — a real server-side guard, not UI-only (`CANCELLED` block + party/direction/total guards preserved). No other route can mutate a settled invoice: `addPayment` refuses `remainingDue<=0`, `cancel` refuses `paidAmount>0`, `approve` is PURCHASE-only/idempotent. The shared `frontend/src/utils/invoiceGovernance.ts#canEditInvoice()` hides normal Edit for PAID/CANCELLED in both the Invoices drawer (`canEditRow`) and `InvoicePreview` — matching the backend. **(2) Dedicated collection-date action:** paid invoices expose **"تعديل تاريخ التحصيل"** (SYSTEM_ADMIN) — a top-level quick action for the single-payment case plus per-payment buttons in the payments list — that opens the existing correction dialog (`PATCH /payments/:id/collection-date` → `paymentsService.correctCollectionDate`), **never** the invoice edit form. The drawer now fetches full invoice detail (`GET /invoices/:id`) so payment records are available (previously the drawer opened on a bare list row without payments). `collectionDateAction()` never guesses: exactly one correctable payment → direct action; several → explicit per-payment selection; none/non-admin → hidden. **(3) Collection-date correction unchanged & safe:** `Payment.date` remains the official collection date, `Payment.createdAt` stays audit-only, the linked `JournalEntry.date` updates **atomically** (single `$transaction`, rollback if no entry), the `COLLECTION_DATE_CORRECTION` audit is preserved, and the endpoint is SYSTEM_ADMIN-only (`requireRole`); it mutates no invoice field, total, payment amount, or method, and never reopens the invoice. **Files: 6** (+266/−9): `invoices.service.ts` (PAID guard) + `invoices.governance.test.ts`; `invoiceGovernance.ts` (canEditInvoice + collectionDateAction) + test; `Invoices.tsx` (drawer detail fetch, action, per-payment buttons); `InvoicePreview.tsx` (shared predicate). **Validation:** prisma validate ✅ · backend/frontend/electron tsc ✅ · backend vitest **1392** ✅ · frontend vitest **819** ✅ (2 pre-existing unrelated `printWorkspace.test.tsx` CSS failures — non-blocking) · build:back ✅ · build:front ✅. **Reviews:** Implementation ✅ · Manual Visual Review ✅ · `/code-review` **APPROVED WITH MINOR COMMENTS** (cosmetic only) · `/security-review` **SECURITY APPROVED** (adversarially verified — PAID guard unbypassable, correction SYSTEM_ADMIN-only + two-field atomic write). Gemini intentionally skipped under the updated workflow for small scoped packages. Branched from `production` @ `680da79`; feature commit `ecbc1f4`, merge commit `5ef8592`, stable tag `stable-invoice-governance-collection-date-safety-v1`. |
| **Expenses Governance & Classification Standardization Pack v1** | `stable-expenses-governance-classification-v1` | **Safe approved-expense amendment workflow + expense classification/label standardization** — backend + frontend; **no DB schema change, no migration; API contracts unchanged (enum/code based); Arabic labels presentation-only.** **(1) Amendment:** direct `update()` of an `APPROVED` expense stays blocked at the service guard (and the UI never renders a plain Edit for APPROVED); a new **`PATCH /expenses/:id/amend`** (guarded by **`requirePermission('expenses.approve')`**, consistent with cancelApproval) reverses the accounting impact via an **audit-preserving `EXPENSE_REVERSAL`** (original journal not mutated/deleted), clears the legacy single-entry, and returns the expense to **`PENDING`** for normal edit + re-approval. **Re-approval reposts the amended value** via **`repostExpenseToGL`** (deletes the netted EXPENSE/EXPENSE_REVERSAL pair, posts fresh) — mirrors the invoices `repostInvoiceToGL` lifecycle; all GL mutations transactional; double-post blocked by the DB `@@unique([referenceType,referenceId])` + code guards; double-reversal blocked by `reverseGL`'s existing-reversal guard. Every transition audited (**`amend → AMEND_UNLOCK`** with old/new). **(2) Dictionary standardization** (presentation-only): duplicated expense category/status/payment maps consolidated into shared SoT modules — `backend/src/shared/utils/expenseLabels.ts` (`expenseCategoryAr`/`expenseStatusAr`/`glPaymentMethodAr`) and `frontend/src/config/expensePresentation.ts` (status meta + payment); backend `reports.service`/`expenses.service`/`expenses.accounting` and frontend `Expenses`/`ForceDeleteExpenseModal`/`FastMonthlyExpenseDialog`/`FinancialOperationsDashboard` consume the SoT; enums/codes remain the sole API vocabulary. **(3) `hasJournalEntries`:** additive read-only display flag on expense list rows so the UI hides the normal Delete action when the backend would reject it (amended expense carrying a journal pair) and routes to force-delete; **no backend delete or accounting logic changed.** Force-delete (SYSTEM_ADMIN + code confirmation), approval workflow, permissions, and AuditLog behavior all **preserved**. **Files: 16** (+613/−125). **Validation:** prisma validate ✅ · backend/frontend/electron tsc ✅ · backend vitest **1387** ✅ · frontend vitest **808** ✅ (2 pre-existing unrelated `printWorkspace.test.tsx` CSS failures — non-blocking) · build:back ✅ · build:front ✅. **Reviews:** Audit ✅ · Manual Visual Review ✅ · `/simplify` (5 fixes) · `/code-review` **APPROVED WITH MINOR COMMENTS** (M-1 + M-2 implemented as follow-up) · `/security-review` **SECURITY APPROVED** · **Gemini Review: APPROVED FOR RELEASE ✅**. Branched from `production` @ `4cfbbd0`; feature commit `7a2a018`, merge commit `ac5b502`, stable tag `stable-expenses-governance-classification-v1`. |
| **Historical Collection Date Correction v1 + Cheque Default** | `stable-collection-date-correction-v1` | **Purpose:** an **administrative correction** tool (historical records only — **not** a new payment workflow) to fix the official collection date «تاريخ التحصيل» of an **already-collected** payment, plus a small safe UI default. **Architecture unchanged** — reuses the existing `Payment.date` (official collection date) vs `Payment.createdAt` (immutable audit stamp) split confirmed by prior investigation; **no DB schema change / no migration**. **Security:** new endpoint `PATCH /api/payments/:paymentId/collection-date` guarded by **`requireRole('SYSTEM_ADMIN')`** (strict — SYSTEM_ADMIN-only, so **no new permission key**). New minimal **`payments` module** (routes/controller/service/schema) registered at `/api/payments` in `app.ts`. **Updates ONLY** `Payment.date`; **never** touches `createdAt`, amount, method, invoice totals/status/lines. **Accounting behaviour:** because `JournalEntry.date` is a *copy* of `Payment.date` taken at posting time (it does **not** auto-track), the correction **also updates the linked posting entry's date** (`journalEntry.updateMany` where `referenceType ∈ {PAYMENT, PURCHASE_PAYMENT}` ∧ `referenceId = paymentId`) **atomically in one `$transaction`** — keeping GL/ledger aligned with collection reports. **Reversal entries (`*_REVERSAL`) are deliberately not touched** (distinct event, own date). **Closed-period protection:** none exists in the codebase — left a `TODO` only (per scope; no period system invented). **Audit:** every correction records the self-identifying `action:'COLLECTION_DATE_CORRECTION', module:'payments', entityId` (specific action string, consistent with the existing custom `'PAYMENT'` precedent — no audit schema/enum change; the audit UI renders any action via its text fallback), with **old date, new date, reason, invoiceId, invoiceNumber, username** (reason/username nested in the audit JSON since `AuditLog` has no dedicated columns). **Reports:** verified **zero changes needed** — every collection aggregation (reports/dashboard/executive/statements/accounting) already groups by `Payment.date`, so corrections propagate automatically. **Frontend:** invoice drawer payment rows show a **«تصحيح تاريخ التحصيل» button — SYSTEM_ADMIN only** — opening a new `CorrectCollectionDate` modal (current date → new date → optional reason) with the mandated Arabic confirmation notice; full validation + success toast + backend-error surfacing. **Part B:** the AddPayment collection dialog now **defaults the payment method to «شيك» (`CHEQUE`)** (default-selection change only — all four methods `CASH/BANK/CHEQUE/TRANSFER` stay selectable, unchanged semantics, no migration/report impact). **Refinements (approved final architecture):** (1) **No-Op protection** — if the requested date is the **same collection day** as the current one, the service returns success (`changed:false`, message «لم يتم إجراء أي تعديل…») and writes **nothing** (no payment/JE/audit/DB write); day-granular comparison (`isSameCollectionDay`, mirrors frontend `formatFileDate` local Y/M/D) so an unchanged-day save on a time-bearing payment isn't a spurious change. (2) **Atomic + accounting-consistent** — `Payment.date` and `JournalEntry.date` move together in one `$transaction`; if the linked JE is unexpectedly missing (`updateMany count === 0`) the transaction **aborts (rollback)** rather than update the payment alone. (3) **Reason** trimmed → `NULL` when empty. (4) Impossible dates rejected by Zod `z.coerce.date()`. (5) **Invoice-date protection** — a correction whose new collection **day** precedes the invoice **issueDate** is rejected («لا يمكن أن يكون تاريخ التحصيل أقدم من تاريخ إصدار الفاتورة»); no such rule existed in the project (neither `addPayment` nor the schema enforced it), so it is applied here on **real changes only** (after the no-op guard), reusing the **same single payment query** (issueDate added to its `select` — no extra query) and skipped when `issueDate` is `null`. (6) **Idempotent** — because the guard is day-granular and stateless per call, repeating the exact same correction once applied is always a no-op (no duplicate audit, no Payment/JE write), verified by test. **Files: 8** (5 new + 3 modified) — new `backend/src/modules/payments/{routes,controller,service,schema}.ts` + `__tests__/payments.correctCollectionDate.test.ts`; modified `backend/src/app.ts`, `frontend/src/pages/Invoices.tsx`, `frontend/src/pages/Invoices.css`. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, backend vitest 1356/1356 ✅ (+26 new payments tests: immutable-field guard, journal-date sync, atomic-abort-on-missing-JE, no-op same-instant + same-day-different-time + day-change, invoice-date-guard reject/same-day/null-issueDate, idempotent-repeat, reason trim/null, audit trail, 404, schema validation ×6, authorization ×3), backend build ✅, frontend build ✅.** **Reviews: `/simplify`** (3 quality fixes applied — reuse `formatFileDate` result, single `.trim()||null`, moved `paymentId` validation into schema `params`; reuse & efficiency clean), **`/code-review`** (no Critical; 1 Warning **fixed** — no-op guard changed from ms-precision `getTime()` to day-granular so a time-bearing payment saved unchanged is a true no-op; atomicity/RBAC/audit-placement/Zod-rejection all verified correct), **`/security-review`** (**no findings** — auth ordering, Zod validation, Prisma parameterization, no IDOR, no XSS sink all verified). **Limitations:** (a) payments predating the GL subsystem have no linked journal entry, so the `count===0` guard **rejects** their correction (by design — accounting consistency over permissiveness); (b) correction is **day-granular** (collection date is a business day, not a timestamp); (c) closed-period locking not enforced (no period system exists — `TODO` left). **Gemini production review: APPROVED ✅** (invoiceId-on-JournalEntry suggestion intentionally not implemented — JournalEntry has no `invoiceId` column; official lookup remains `(referenceType, referenceId)`). Checkpoint tag `checkpoint/pre-collection-date-correction-22032ea`; branched from `production` @ `22032ea`; feature commit `37f26da`, merge commit `9045f6c`, stable tag `stable-collection-date-correction-v1`. |
| **Invoice Collection Date v1** | `stable-invoice-collection-date-v1` | **Invoices-module** feature separating **when a payment was collected** from **when it was entered**. **Design decision (confirmed with the user):** the `Payment` model already had a user-overridable `date` field that every collection report/GL entry/statement already groups by — so rather than add a redundant `collectionDate` column and rewire ~20 aggregations, **`Payment.date` is adopted as the official collection date «تاريخ التحصيل»** (user-editable, defaults to today) and a **new audit-only `Payment.createdAt @default(now())`** captures system-entry time. **GL entries follow `Payment.date`** — which required **zero accounting code change** (`postPaymentToGL`/`postPurchasePaymentToGL` already date the journal entry by `payment.date`, so backdating a collection backdates its GL entry, keeping accounting/bank-reconciliation/trial-balance/reports aligned; confirmed by a new test asserting `journalEntry.date === Payment.date` incl. a backdated case). **No report changes needed** — every collection aggregation (dashboard, executive, `collectionsSummary`, statements, accounting) already groups by `payment.date`. **Migration** `20260708100000_add_payment_created_at` is a SQLite table-rebuild (SQLite can't `ADD COLUMN … DEFAULT CURRENT_TIMESTAMP`) that **preserves all Payment columns, the `invoices` FK (`ON DELETE CASCADE`), and the `payments_invoiceId_idx` index**, and **backfills `createdAt = date`** for existing rows — **no amounts/status/payment data changed** (backward-compatible; `date` is non-null on every historical row so the effective collection date always resolves correctly). **Backend:** `addPaymentSchema.date` (already optional) documented as the collection date; the service **resolves the effective date once** (`input.date ?? new Date()`) and uses it for both `payment.create` and the audit log (fixes an audit/DB divergence found in code review); new pure, unit-tested `effectiveCollectionDate(payment) = date ?? createdAt` helper expressing the fallback contract (read-side/defensive; reports key on the `date` column directly). **Frontend:** the AddPayment modal («تسجيل الدفعة / تحصيل») gains a **required «تاريخ التحصيل» date input** (default today via the shared `formatFileDate` local formatter), submitted as `date` for **all** payment methods; the redundant **TRANSFER-only transfer-date input was removed** (the unified collection date now owns the payment date; transfer **number/reference preserved & still validated**). Payment history already renders `date` (collection date). **NO amount/status/accounting-posting/API/route/permission changes.** **Files: 9** (8 modified + 1 new migration) — `schema.prisma`, migration, `invoices.calc.ts` (+test), `invoices.schema.ts`, `invoices.service.ts`, `invoices.accounting.test.ts`, `pages/Invoices.tsx`, `lib/i18n.ts` (`field.collection_date` ar/en); +134/−19. Checkpoint tag `checkpoint/pre-invoice-collection-date-v1`; branched from `production` @ `f00c734`; feature commit `aa2bd26`, merge commit `edc2fc0`. **Validation: prisma validate ✅, migrate status ✅ (38 migrations), backend tsc ✅, frontend tsc ✅, backend vitest 1330/1330 ✅ (+6: 5 `effectiveCollectionDate` + 1 GL-date; invoices/payment 148/148), frontend vitest 802 pass ✅, backend + frontend builds ✅** (2 pre-existing `printWorkspace.test.tsx` CSS failures — out of scope/unrelated, verified). Reviews: `/simplify` (1 reuse fix — inline today-date → shared `formatFileDate`; kept the tested fallback helper per scope), `/code-review` (2 MEDIUM fixed — audit-date/DB consistency + helper doc accuracy; UTC-midnight date coercion skipped as the **established app-wide `z.coerce.date()` convention**, safe for the UTC+3 offline deployment), `/security-review` (**no findings** — parameterized Prisma, route guards intact, non-sensitive timestamp). **Gemini production review: APPROVED ✅** (minor i18n recommendation was already satisfied — the label already uses `t('field.collection_date')`). **Deferred:** none. |
| **Pricing Unit «معالجات» (mini)** | `stable-pricing-unit-mualajat-v1` | **Mini feature** — added a new pricing/contract unit **«معالجات»** to the shared unit lists so it appears in the Pricing Agreement form, Contract Pricing (Zod validation), the price-import validator, and the Reports filter. The value was already present in the invoice line-items editor (`STANDARD_UNITS`) after `درب`; this release brings the four remaining duplicated copies into sync at the **same ordering** (`طن · درب · معالجات · يومية · مقطوعية`). **NO DB schema/migration** — `ProjectPrice.contractUnit` is a plain `String` (no DB enum), so the addition is purely additive and existing saved records remain valid. **NO business-logic, API, route, permission, or accounting changes** — literal additions to unit arrays only. **Files: 4** — `backend/src/config/constants.ts` (`ENUMS.invoiceUnit`), `backend/src/modules/import/validators/prices.ts` (`VALID_UNITS`), `frontend/src/pages/Prices.tsx` (`contractUnits`), `frontend/src/pages/Reports.tsx` (`CONTRACT_UNITS`); +4/−4. Feature commit `4f6487e`, merge commit `0897ab0`. Branched from `production` @ `20eb682`. **Validation: frontend tsc ✅, backend tsc ✅, backend vitest 1324/1324 ✅, frontend vitest 802 pass ✅** (the 2 `printWorkspace.test.tsx` CSS failures are **pre-existing and unrelated** — verified by re-running them with the change stashed; they fail identically). **Gemini production review: APPROVED ✅.** |
| **Invoice Fast Entry Mode v1** | `stable-invoice-fast-entry-mode-v1` | **Invoices-module-only** UX accelerator for issuing many invoices back-to-back — **not** a batch/import/recurring/template system. Each save creates a **completely normal invoice** through the existing `POST /invoices` path (normal number, list, status, and accounting lifecycle), indistinguishable from the normal screen. Delivered via a full spec → plan → implementation workflow (`docs/superpowers/specs/2026-07-07-invoice-fast-entry-mode-v1-spec.md`, `docs/superpowers/plans/2026-07-07-invoice-fast-entry-mode-v1.md`). **(1) Numbering:** new **additive, read-only** `GET /invoices/next-number?year=` (`invoices.create`-gated) backed by `InvoicesService.getNextInvoiceNumber(year)` — **collision-safe max-based** (highest existing numeric `MN-INV-<year>-NNNNN` suffix + 1, ignores non-numeric manual suffixes, tolerates gaps that `count()` would collide on); client `clientNextInvoiceNumber` fallback secondary. **(2) Parity core:** new pure `frontend/src/pages/invoiceFastEntry.ts` — `buildInvoiceCreatePayload` **proven byte-identical (15 unit tests)** to the normal Create-Invoice submit payload (**PD-1**: SALES field set — invoiceNumber/direction/invoiceType/customerId/issueDate/deliveryDate/billing month+year/discount/items via `toInvoiceItemPayload`; **never** taxRate/paymentMethod/dueDate/contractId/notes), plus `validateInvoiceRow` (mirrors the form's rules), summary/number/dirty helpers. **(3) Single-source editor:** extracted `components/invoices/InvoiceLineItemsEditor.tsx` **verbatim** from `CreateInvoice` (shared Item/PriceOption types, STANDARD_UNITS/UNIT_OTHER, unitSelectValue/isCustomUnit, invoiceLineTotal, LocationAutocomplete, workType/location→description composition, unit auto-pricing, price-agreement picker) — **pure refactor, zero behaviour/UI/validation/totals/API/keyboard/permission change**; `CreateInvoice` now renders `<InvoiceLineItemsEditor/>`, `EditInvoice` left intact. **(4) Fast Entry dialog:** new `components/InvoiceFastEntryDialog.tsx` (ExplorerKit) opened via **«إدخال فواتير سريع»** on the Invoices page — pinned shared fields (customer via `SearchableSelect`, contract price-filter, invoice type, issue date→billing period), repeating invoice area reusing the shared editor + discount + delivery date + an **always-pre-filled, editable** invoice number; **Save & Next / Finish / Cancel** with **Ctrl+Enter / Ctrl+Shift+Enter** shortcuts and a dirty-close guard; each save posts to `POST /invoices`, shows a **non-intrusive success toast**, advances the suggested number, updates a **display-only session summary** (count/total/last+next number), and refocuses the first item. **(5) Entry Mode selector:** explicit first step **عميل واحد** (default/recommended) / **عملاء متعددون** with a persistent header chip; Single pins the customer, Multiple makes it a per-invoice field and **gates Save while the customer's price agreements load** (prevents stale prices); mode-switch with a dirty row asks for confirmation. **v1 SALES only** (PURCHASE deferred). **NO accounting/posting/reports/approval/API redesign; the only new backend surface is the additive read-only next-number endpoint; no batch entity/id/status/hidden-grouping; no persisted session (§1.6 lifetime invariant); FastEntrySession abstraction deferred until a third consumer.** **Files: 8** (3 modified + 5 new) — new `invoices/InvoiceLineItemsEditor.tsx`, `InvoiceFastEntryDialog.tsx`, `pages/invoiceFastEntry.ts` (+`__tests__`), `invoices/__tests__/invoices.nextNumber.test.ts`; modified `invoices.{service,controller,routes}.ts`, `pages/Invoices.tsx`. Checkpoint tag `checkpoint/invoice-fast-entry-v1-40e7f82`; branched from `production` @ `40e7f82`; 5 focused commits (`339fc7c` next-number · `cc3a5d6` helpers · `7d6c648` editor extraction · `cd27c3d` dialog Single mode · `561d3bf` mode selector + Multiple), merge commit `919b2fa`. **Validation: backend tsc ✅, frontend tsc ✅, backend vitest 1324/1324 ✅ (+4 next-number: empty-year, max+1-over-gap, ignore-non-numeric, year-scoping), frontend vitest 792 pass ✅ (+15 payload/validation-parity + number/summary/dirty), backend + frontend builds ✅** (2 pre-existing `printWorkspace.test.tsx` CSS failures — out of scope). Reviews: mirrored the shipped Expenses Fast Entry pattern; self + security assessment (only new surface is a read-only, permission-gated endpoint — no injection/exposure); **normal-invoice manual smoke performed by the user before merge**. **Gemini production review: APPROVED ✅.** **Deferred:** PURCHASE Fast Entry (same mechanism + shared paymentMethod); the `FastEntrySession` framework (extract when a third Fast Entry surface exists); optional resume-session affordance (spec §10.1). |
| **Expenses Operations Enhancement Pack v1** | `stable-expenses-operations-enhancement-v1` | **Expenses-module-only** operations pack (two features). **(1) Force Delete (SYSTEM_ADMIN-only):** a safe **«حذف نهائي»** for expenses that cannot be deleted normally (approved/posted, reversed), following the codebase's established invoice force-delete pattern. **Backend** — new `forceRemovePreview(id)` + `forceRemove(id, confirmation, req)`; routes **`GET /:id/force`** (preview) + **`DELETE /:id/force`** gated by **`requireRole(ROLES.SYSTEM_ADMIN)`** (no new permission key; non-admins → 403). `forceRemove` requires the typed `confirmation` to equal the expense `code`, then runs a **single transaction that leaves no orphan records**: unlinks any bank-reconciliation matches (`matchedType='expense'|'journal'` → reset to `UNMATCHED`), clears the legacy `Transaction`, deletes the double-entry `JournalEntry` rows (`EXPENSE` + `EXPENSE_REVERSAL`, lines cascade) and polymorphic `Attachment` rows, then the expense; attachment **files** are unlinked best-effort *after* commit (try/catch, non-fatal). Writes a full **`FORCE_DELETE_EXPENSE`** audit entry (snapshot + affected-record counts). **No posting/approval/report logic changed — deletion only.** **Frontend** — new `components/ForceDeleteExpenseModal.tsx` (mirrors the 4 sibling force-delete modals) shows code/category/amount/date/status/supplier + accounting impact + warnings and requires typing the **exact code**; wired into the Expenses drawer as a danger action visible only when `user.role.name === 'SYSTEM_ADMIN'`. **(2) Fast Monthly Expense Entry:** a **UX accelerator (not a batch/import)** — new `components/FastMonthlyExpenseDialog.tsx` opened via **«تسجيل مصروفات شهرية»** beside «مصروف جديد». Pins the shared fields (date, accounting month/year, payment method, shared supplier incl. per-row + free-text, optional notes prefix) while the row fields (category via the shared `SearchableSelect`, description, amount, notes) repeat. Buttons **حفظ وإضافة التالي / حفظ وإنهاء / إلغاء**, with **Ctrl+Enter** / **Ctrl+Shift+Enter** shortcuts and a dirty-close guard; save-and-next creates one **normal individual expense via the existing `POST /expenses` flow**, clears only the row fields, and refocuses the category. A live **session summary** (count/total/last saved) is **display-only — no parent/batch record, batch id, status, or hidden grouping**. Logic extracted into pure, tested `pages/fastExpenseEntry.ts` whose `buildFastExpensePayload` produces a payload **byte-identical to the normal ExpenseForm** (test-verified). Also exposed a shared **`EXPENSE_CATEGORY_SELECT_OPTIONS`** and reused it in the Expenses page (dropped the duplicated options builder). **NO DB redesign, no new expense model, no batch/import concept, no recurring/scheduled/templates; the only new backend surface is the admin force-delete endpoints.** **Files: 12** (6 modified + 6 new) — new `components/ForceDeleteExpenseModal.tsx`, `components/FastMonthlyExpenseDialog.tsx`, `pages/fastExpenseEntry.ts` (+`__tests__/fastExpenseEntry.test.ts`), `__tests__/expenses.forceDelete.test.ts`; modified `expenses.{service,controller,routes,schema}.ts`, `config/expenseCategories.ts`, `pages/Expenses.tsx`. Checkpoint tag `checkpoint/expenses-ops-pack-v1-2026-07-07`; branched from `production` @ `8300bb4`; 3 focused commits (`677bada` feat force-delete · `26a468c` feat fast-entry · `2bbee0e` tests), merge commit `70a06da`. **Validation: backend tsc ✅, frontend tsc ✅, backend vitest 1320/1320 ✅ (+8 force-delete: confirmation-mismatch, success+audit, missing-file safety, preview counts, admin-allow, non-admin-403), frontend fast-entry + search 20/20 ✅ (+12 fast-entry), backend + frontend builds ✅** (2 pre-existing `printWorkspace.test.tsx` CSS failures — out of scope). Reviews: established-pattern reuse (invoice force-delete), self + local security-review (no HIGH/MEDIUM findings — admin-only destructive endpoint gated server-side, unit-tested 403). **Gemini production review: APPROVED ✅.** **Deferred (non-blocking):** portal-based dropdown popover for the fast-entry category selector inside short dialogs (inherited from the pack-v1 selector). |
| **Expenses Reliability & Usability Pack v1** | `stable-expenses-reliability-usability-v1` | **Expenses-module-only** reliability + usability pack (four focused changes). **(1) Reliability — deletion-safe expense code:** root-caused the recurring **P2002 "تعارض في البيانات — الحقل: code"** conflict to `ExpensesService.generateCode()` deriving the next sequence from `expense.count()` — surviving rows, not the highest sequence issued — so deleting **any non-last** PENDING/REJECTED/CANCELLED expense dropped the count below the max and the next code collided. **Fix:** adopt the project's already-proven collision-safe pattern (from `transactions`/`accounting`) — take the **last row by `id desc`, parse its sequence, +1** (`findFirst` instead of `count`); 3 new unit tests cover the deletion-collision, empty-year, and client-supplied-code-bypass paths. **(2) Catalog — +21 categories via a single source of truth:** new **`frontend/src/config/expenseCategories.ts`** (`value`/`labelAr`/`labelEn`/`icon`/`group`, logically grouped تشغيل عام · مركبات · رسوم حكومية · عن طريق أشخاص · أخرى) added Tires/Oils/Toll/Traffic-Violations/Court-Fees/Charity/Residency/Bills-via-Nazeer/Driver-Expenses/Driver-Meals/Government-Labor-Insurance-Vehicle-fees/Battery/Tow-Truck/vehicle paint-bodywork-electrical/Gifts/Misc (reusing existing `PURCHASES`); `category` is a free **String** column → **no migration, existing records untouched, IDs preserved**. Backend `ENUMS.expenseCategory` (Zod allow-list) + backend `CATEGORY_AR` (GL/stats labels) + `cat.*` i18n (ar+en) + `modules.tsx` + AI skill all wired to the shared source, collapsing the prior 5-way label duplication. GL posting is category-agnostic (`GENERAL_EXPENSE`) → accounting-safe. **(3) Usability — searchable category selector:** new reusable **`components/SearchableSelect.tsx`** (+`.css`, +test) — Arabic-name search (matches the label, **never the enum key**) with diacritic/alef/ya/ta-marbuta normalization so typing «كر/بط/زي/رس» surfaces the right items — keyboard nav (arrows/Home/End/Enter/Esc/type-ahead), group headers, ExplorerKit `xpl-select` look, theme-aware; used on the **create form, edit form, and list filter**. **(4) Compliance — export date:** the Excel billing-period fallback now routes through the shared **`dateText` (DD/MM/YYYY, English digits)** formatter instead of raw `slice(0,10)`. **NO accounting/posting/journal/GL logic, NO API/route/permission/IPC changes; one required additive backend change (enum + labels) to make new categories valid.** **Files: 12** (8 modified + 4 new) — new `config/expenseCategories.ts`, `components/SearchableSelect.tsx` + `.css` + `__tests__/SearchableSelect.search.test.ts`; modified `expenses.service.ts` (+test), `config/constants.ts`, `schema.prisma` (comment only), `pages/Expenses.tsx`, `config/modules.tsx`, `ai/skills/expenses.ts`, `lib/i18n.ts`; +771/−64. Checkpoint tag `checkpoint/expenses-pack-v1-2026-07-07`; branched from `production` @ `06a47dc`; 4 focused commits (`a65b2e7` fix code-gen · `9833acb` feat catalog · `b27487c` feat selector · `e705d33` fix export date), merge commit `a21d8da`. **Validation: backend tsc ✅, frontend tsc ✅, backend vitest 1312/1312 ✅, frontend Arabic-search 8/8 ✅, backend + frontend builds ✅** (2 pre-existing `printWorkspace.test.tsx` CSS failures — out of scope). Reviews: systematic-debugging root-cause, self + local security-review (no HIGH/MEDIUM findings). **Gemini production review: APPROVED ✅.** **Deferred (non-blocking):** portal-based dropdown popover so the selector can float above a very short create dialog (currently scrolls within `.xpl-dialog-body`). |
| **Smart Transaction Presentation Engine v1** | `stable-smart-transaction-presentation-engine-v1` | **Frontend display-only** upgrade of the Bank Account Explorer transaction description from the v2 text-first parser (`describeTransaction`) to a safer, structured **Smart Transaction Presentation Engine**. New `frontend/src/pages/bankTransactionPresentation.ts` exports `presentTransaction(t) → SmartTransactionPresentation { category, detail?, raw, provenance }`. **Category is resolved structured-first:** (1) backend-classified `bankFeeType` → Arabic label via a **compile-time-complete** `Record<BankFeeType,string>` (رسوم تحويل / رسوم شهرية / فائدة / رسوم بنكية / رسوم صراف آلي / رسوم دفتر شيكات / رسوم أخرى / سحب نقدي / دفع شيك / تحويل بنكي), with a **same-family** anchored phrase allowed only to *refine* the label (e.g. CHEQUE_PAYMENT + "inward clearing" → «شيك مقاصة وارد»); (2) when `bankFeeType` is null, an anchored high-confidence bank phrase (Cheque Paid / Inward-Outgoing Clearing Cheque / Outgoing RTGS / ATM-Cash Withdrawal); (3) debit/credit **direction** (إيداع/سحب); (4) clamped raw **fallback**. **Detail is a single, safe, ordered extractor** — inward-clearing «رقم N · مقدَّم في X» → anchored «Cheque Number: N» → structured `chequeNumber` field (≤7 digits) → standalone «Presented in X» → explicit `reference` field → verbatim Arabic mirror → explicitly-labelled project hint — each passed through an `acceptable()` gate that **blocks category/detail duplication and any `\d{8,}` run**. **Privacy (core of the release):** removed the v2 ALL-CAPS **name** extractor entirely; **names, CIVIL ID, mobile numbers, and long account/instrument numbers are never promoted** to the table row or Basic line — they remain only in `raw` (table tooltip + Audit tab). **UI:** table line 1 = `category.label`, line 2 = `detail.text` (raw in `title`); drawer **Basic tab** shows category + detail + collapsible raw «النص الأصلي»; a new drawer **Audit-tab** «التصنيف الذكي» section surfaces provenance (rule, confidence, matched source, raw) for full transparency; **Financial tab left numbers-only**. **No backend, API, Prisma, database, import, accounting, normalization, or reconciliation changes** (only a **type-only** import of `TimelineTransaction`/`BankFeeType`); **no saved-data changes; no fabricated values** (test-enforced: every shown digit is a source substring). **Files: 4** — new `pages/bankTransactionPresentation.ts` + new `__tests__/bankTransactionPresentation.test.ts` (14 cases), modified `pages/BankAccountExplorer.tsx`, deleted obsolete `__tests__/bankTxDescription.test.ts`; +515/−201. Feature commit `a2d598f`, merge commit `41d4f22`. Branched from `production` @ `0525316` (which carries the approved Phase-1 roadmap `docs/roadmap/SmartTransactionPresentationEngine-Phase1.md`). **Validation: frontend tsc ✅, Bank Explorer tests 22/22 ✅ (14 presentation + 5 timeline + 3 analytics), vite build ✅, full frontend vitest 716/718 (2 pre-existing `printWorkspace.test.tsx` failures on untouched `PrintWorkspace.css` — out of scope).** **Intentional roadmap deviations** (documented in the Gemini packet): anchored high-confidence phrase ordered before the generic direction label (keeps specific categories, avoids type-badge duplication; all category sources except fallback stay HIGH confidence); Arabic-first category labels (English bank phrasing preserved verbatim in raw/Audit); detail lines wrap a **verbatim** extracted token in a fixed Arabic label (value always literal, only the wrapper is a fixed lookup). **Gemini production review: APPROVED ✅.** |
| **Bank Account Explorer Visual Refresh v2** | `stable-bank-account-explorer-visual-refresh-v2` | **Frontend presentation-only** refresh of the Bank Account Explorer (`pages/BankAccountExplorer.tsx` + `.css`) realigned to the approved visual master, plus a display-only transaction-description enhancement. **Visual Refresh v2:** executive header with a horizontal identity + meta strip (logo, bank name, derived account identifier + copy, currency, live status, last import, statement count, primary «إضافة كشف»); compact indigo **balance hero** (balance + net-flow pill) beside **5 KPI stat tiles** (deposits/withdrawals/net/operations/statements, colored values, `KWD` units); primary **navigation tabs**; grouped **filter workspace** (نوع → الفترة → نطاق المبلغ, live/debounced — no misleading Apply button, Clear provided); **transaction table** columns realigned (العملية type-icon + reconcile dot · التاريخ · النوع · الوصف · المبلغ · الرصيد بعد العملية); right **drawer converted to a tabbed Information Hub** (5 tabs: البيانات الأساسية / المالية / الاستيراد / التدقيق / المرفقات) with tx-id+badge header, horizontal fields, and a persistent «ملخص سريع» before→amount→after strip. **Polish pass:** page title + breadcrumb (was missing), underline nav tabs, lighter filter groups, softer shadows, cleaner primary/secondary description typography, subtler selected-row accent. **Transaction Description Enhancement (display-only):** `describeTransaction()` parses the raw bank text into a clean two-line label — line 1 category (Cheque Paid / Inward Clearing Cheque / Bank Charges / bilingual Latin↔Arabic / generic), line 2 extracted detail (beneficiary name / cheque no / «Presented in» / reference) — **nothing stored, bank text verbatim, no line-1/line-2 duplication, low-confidence → original text, no fabricated values**; wired into the table cell and the drawer Basic tab (parsed lines + collapsible raw «النص الأصلي» only when it adds detail). **Graceful degradation (no fabrication):** fields absent from the dashboard/timeline APIs (separate account-number+IBAN, Arabic bank tag, single-tx print, «…» menu, audit imported-by/updated-at, attachments) are **hidden**, not faked. **No backend, API, Prisma, database, import/accounting/payroll/validation, routing, auth, or saved-data changes; no new dependencies.** **Files: 4** — modified `pages/BankAccountExplorer.tsx` + `pages/BankAccountExplorer.css`, modified `__tests__/bankTimelineTab.test.tsx` (fingerprint assertion navigates to the tabbed drawer's Audit tab), new `__tests__/bankTxDescription.test.ts` (9 parser unit tests); +934/−298. Feature commit `a1ff9bc`, merge commit `f028768`. Checkpoint: branched from `production` @ `34005e1`. **Validation: frontend tsc ✅, Bank Explorer tests 17/17 ✅, vite build ✅, full frontend vitest 711/713 (2 pre-existing `printWorkspace.test.tsx` failures on untouched `PrintWorkspace.css` — out of scope).** Reviews: prior `/simplify` + `/code-review` + `/security-review` context from v2 development. **Gemini production review: APPROVED ✅.** |
| **Bank Account Explorer Default Sort Fix v1** | `stable-bank-account-explorer-default-sort-v1` | **Backend ordering-only fix** for the Bank Account / Transaction Explorer timeline list. **Root cause:** `getTimeline()` in `modules/bankStatementImport/service.ts` ordered `statementDate: 'asc'`, so the table displayed **oldest transactions first**; single-key date ordering was also non-deterministic for same-date rows (unstable pagination). **Fix:** the default transaction timeline now shows **newest transactions first** via the new exported `TIMELINE_ORDER_BY` constant — **`statementDate` DESC → `importId` DESC → `id` DESC** — so the first rows come from the latest imported statement's newest transaction and the order is fully deterministic (**stable pagination for same-date transactions**). `importId` (monotonic with import time) stands in for "imported batch date descending"; `id` DESC is the stable final tiebreak. **Scope strictly limited:** the reconciliation report query (`buildReport`, single-import `asc`) is untouched; **no import logic, statement parsing, balances/calculations, schema/database/Prisma, filters, search, CSV export, or frontend UI changes** (the page already renders `result.transactions` in server order with no client-side sort). **Files: 2** — `modules/bankStatementImport/service.ts` (extract + apply `TIMELINE_ORDER_BY`) + `__tests__/timelineFilters.test.ts` (2 new focused ordering tests); +30/−3. Feature commit `90104a4`, merge commit `68aff1c`. Branched from `production` @ `d30c9c7`. **Validation: backend tsc ✅ (except unrelated pre-existing untracked `currency.test.ts`), backend bankStatementImport vitest 208/208 ✅ (timelineFilters 9/9 incl. 2 new ordering tests).** **Gemini review: PASS ✅.** |
| **Financial Bug Fix Pack C1–C4** | `stable-critical-financial-bugs-c1-c4-v1` | **Backend-only** fix pack for four critical runtime financial bugs — **finally accepted; Claude post-merge verification and Gemini review both APPROVED; no hotfix required.** **C1 (invoices):** a PURCHASE invoice paid by CASH/BANK is settled at creation (`paidAmount=total`, `status=PAID`) via the new `isImmediatelySettledPurchase()` helper, and `addPayment` rejects any payment once `remainingDue ≤ 0` — preventing a second settling payment that double-credited Cash and drove Accounts Payable negative. **C2 (payroll):** `computeRegularHours(present+late)` counts LATE days as normal working days, so their logged hours no longer get misclassified as 1.25× overtime. **C3 (payroll):** salary advances now subtract amounts already committed in other open (DRAFT/APPROVED) payrolls from the effective remaining, preventing the same advance being deducted twice across months. **C4 (expenses + inventory):** expense reversal now `clearByReference('EXPENSE', id)` clears the legacy single-entry transaction (not just GL) so a reversed expense leaves the dashboard total, and goods receipts post as `TRANSFER` (asset acquisition) instead of `EXPENSE` so buying inventory is no longer counted as an operating expense at receipt **and** again at issue. **No schema / migration / frontend / dependency changes.** **Files: 11** — 6 source (`invoices.calc.ts`, `invoices.service.ts`, `payroll.calc.ts`, `payroll.service.ts`, `expenses.service.ts`, `inventory.service.ts`) + 5 regression tests (`invoices.calc`, `payroll.calc`, `payroll.advance`, `expenses.cancelApproval`, `inventory.posting`); +457/−9. Feature commit `5b61f4e`, merge commit `7ae1239`. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, electron tsc ✅, backend vitest 1225/1225 (67 files — incl. 99 tests across the 5 C1–C4 files), build:back ✅, build:front ✅.** Logical accounting review confirmed consistency (cash/bank purchase GL credits Cash directly; dashboard/reports/transactions aggregate only `type:'EXPENSE'`/`'REVENUE'`, so `TRANSFER` is correctly excluded). Claude verification APPROVED ✅. Gemini review APPROVED ✅. **Future note:** the legacy `Transaction` table and the double-entry `JournalEntry` (GL) remain two parallel systems — see Deferred Accounting Notes #1 (Accounting Architecture Audit, deferred). |
| **Bank Account Explorer — Phase E (Premium Executive Experience — Final Polish)** | `stable-bank-account-explorer-phase-e-v1` | **Frontend-only** final visual-excellence pass — **2 files** (`BankAccountExplorer.tsx` +233/−207, `BankAccountExplorer.css` +146). **Zero backend / API / Prisma / Electron / business-logic / dependency changes**; offline-first + Arabic-first preserved. **Part 3 — KPI hierarchy:** Current Balance promoted to a **dominant gradient hero card** (`bae-balance-hero`, indigo gradient, 30px value via `PrivateAmount`, net-cash-flow line) with 5 secondary cards beside it (Deposits/Withdrawals/Net/Transactions/Statements). **Part 4 — Executive header:** rebalanced to identity-focused — logo card, name, account identifier + copy, currency + نشط chips, and a right meta block (الكشوف المستوردة / فترة التغطية / آخر تحديث); the giant balance moved out of the header into the KPI hero; soft gradient background. **Part 1 — Drawer redesign:** rebuilt into grouped section panels (`bae-drawer-section`) instead of many loose rows — Hero (icon + type badge + signed amount + balance-after) → المعلومات الأساسية (date/reference/type/currency/status) → الأطراف والمراجع (cheque, when present) → الوصف (collapsible) → بيانات الاستيراد (batch/statement date/file/imported-at/bank/notes) → بيانات تقنية (account key + fingerprint·SHA-256); amounts folded into the hero to cut fragmentation; footer copy-reference / copy-fingerprint / close. **Part 2 — Executive timeline rows:** restyled to a banking feel — leading direction chip + two-line identity cell (description + import badge + reference), دائن/مدين merged into one **signed amount** column (green +/red −), balance-after, and a **status dot** (`bae-status-dot-cell`, good/warn/neutral); keyboard rows + selected/hover retained. **Part 5 — Filter toolbar:** now **sticky** (`position: sticky; top: 0`) with animated chip entrance. **Part 6 — Empty states:** larger illustration + explanatory copy + primary action (clear-filters / refresh). **Part 7 — Motion:** 200–250ms transitions (drawer slide easing, overlay fade, card/hero hover, chip-in) with `prefers-reduced-motion` guard. **Part 10 — Accessibility:** added a real **Tab focus trap** to the drawer (cycles focus within the dialog, both directions) on top of existing Escape / focus-into-panel / focus-return / scroll-lock / `aria-modal`/`aria-labelledby`. **Honest data notes (no API change):** IBAN→parsed accountKey, currency/status static (KWD / نشط), fees KPI→total transactions, Transaction ID/Hash omitted (only fingerprint exposed). **Validation: prisma ✅, backend tsc ✅, frontend tsc ✅, electron tsc ✅, backend vitest 1190/1190 (63 files), frontend vitest 610/610 (32 files), build:back ✅, build:front ✅.** Security review: no findings (presentational; only `navigator.clipboard.writeText` of already-visible values; no unsafe sinks). Commits: `feat(bank): premium executive experience for bank account explorer (phase e)`, `docs: update PROJECT_STATE for bank account explorer phase e`. **Status: merged & tagged (see Stable Tag column); on `production`.** |
| **Bank Account Explorer — Phase D (Executive UX & Visual Excellence)** | `stable-bank-account-explorer-phase-d-v1` | **Frontend-only** visual/UX excellence pass implementing the approved mockup. **Zero backend / API / Prisma / Electron / business-logic / architecture changes — 3 files touched, all under `frontend/src`.** **Layout (Option A — unified executive page + slim tabs):** the page now lands on one modern banking dashboard — Executive Header → KPI row → Filter toolbar → Timeline → Transaction drawer. The former 5-tab structure is preserved as a slim secondary tab strip (السجل الزمني default · التحليلات · دفعات الاستيراد · تصدير); the old Overview tab's content (AccountHealthCard + top tables + monthly) folds into Analytics so nothing is lost. **Executive header (`ExecutiveHeader`):** bank logo card, account name, account identifier (parsed from `accountKey` — the dashboard API exposes no separate IBAN, so the stable key is surfaced with a copy button), currency chip (KWD·د.ك), key chip, نشط status badge, dominant hero current-balance, net-cash-flow line, coverage range, imported-statements count, إضافة كشف. **KPI row (`KpiRow`, 5 cards):** عدد الكشوف · إجمالي السحوبات · إجمالي الإيداعات · صافي الحركة · إجمالي المعاملات — colored icon chips, soft shadow, rounded corners, hover lift. (Mockup's إجمالي الرسوم card → إجمالي المعاملات: the dashboard API has no fee aggregate and no API change was permitted, so a real metric is shown instead of fabricated data.) **Transaction drawer rebuilt to mockup:** hero block (green/red icon circle + type badge + signed amount + balance-after), sections المعلومات الأساسية / المبالغ / بيانات الاستيراد / بيانات تقنية (Fingerprint·SHA-256 — `transactionId`/Hash not exposed by the timeline API, shown only if present) / الحالة (matching + import status badges); footer نسخ المرجع · نسخ البصمة · إغلاق (clipboard with transient confirmation); long descriptions collapse via `CollapsibleDescription` (عرض المزيد). Retains all Phase C a11y: `role="dialog"`, `aria-modal`, `aria-labelledby`, Escape closes, focus into panel on open + back to row on close, body scroll-lock. **Timeline table restyled to mockup columns:** المصدر (direction icon + Import #N badge) · الرصيد بعد العملية · دائن · مدين · المرجع · النوع (semantic `bae-tx-badge`: إيداع/سحب/رسوم/شيك/تحويل) · الوصف · التاريخ · chevron; keyboard-accessible rows, hover + selected highlight, sticky header. **Skeleton loaders** replace the spinner while the timeline loads. **New reusable pieces:** `txTypeBadge`, `CopyButton`, `CollapsibleDescription` (all in-file, memo-friendly, no new deps). **CSS (+270 lines, scoped `.bae-*`):** executive header, KPI grid (5-col → responsive 3/2), slim tabs, type/status badges, source cell, chevron, drawer hero/footer, skeleton shimmer, collapsible desc, copy buttons; IBM Plex Sans Arabic + Material Symbols preserved, RTL throughout. **Files (3):** `frontend/src/pages/BankAccountExplorer.tsx` (+769/−367), `frontend/src/pages/BankAccountExplorer.css` (+270), `frontend/src/__tests__/bankTimelineTab.test.tsx` (drawer-title label update). **Validation: prisma ✅, backend tsc ✅, frontend tsc ✅, electron tsc ✅, backend vitest 1190/1190 (63 files), frontend vitest 610/610 (32 files), build:back ✅, build:front ✅.** Commits: `feat(bank): executive UX & visual excellence for account explorer (phase d)`, `docs: update PROJECT_STATE for bank account explorer phase d`. |
| **Bank Account Explorer — Phase C (Stability + Professional Transaction Explorer)** | `stable-bank-account-explorer-phase-c-v1` | Critical stability + UX upgrade. **Bank module stays independent (display/filter/analyze/inspect only) — no accounting integration, no reconciliation workflow, no posting, offline-first unchanged.** **PART 1 — Critical Analytics white-screen fix (root cause):** `AnalyticsTab` previously assumed `dashboard.monthly`/`topDeposits`/`topWithdrawals` were always arrays and fed monthly data straight into Recharts. Two compounding failures: (a) a dead `[...topDeposits, ...topWithdrawals].forEach` that threw if either was undefined; (b) backend `fillMonthlyGaps` could generate a runaway month array (tens of thousands of buckets) from a malformed/out-of-range `statementDate`, freezing Recharts → white screen → forced app exit. **Backend fix (`bankAccounts.service.ts`):** `fillMonthlyGaps` now caps at `MAX_MONTHLY_BUCKETS = 600` (50 yrs) and bails (returns raw entries) when a month bound is unparseable (`Number.isFinite` guard) — no infinite/runaway loop. **Frontend fix:** every array defaults to `[]`, every chart number passes through `safeNum`/`safeAmount` (NaN/undefined/Infinity → 0), chart data capped at `MAX_CHART_POINTS = 120`; removed the dead `forEach`. **PART 6 — Error Boundary:** new reusable `frontend/src/components/ErrorBoundary.tsx` (class component; `getDerivedStateFromError` + `componentDidCatch`; Arabic fallback "حدث خطأ أثناء عرض هذا القسم" + retry + "العودة للنظرة العامة"; auto-resets on `resetKey` change). Explorer tab content wrapped with it (keyed by active tab) — a single tab crash can no longer white-screen the Electron app. First error boundary in the codebase. **PART 5 — Analytics redesign (safe):** Running Balance line chart (cumulative from `openingBalance` + Σ `netFlow`), Deposits vs Withdrawals bar chart, monthly transaction-frequency bar chart, cash-flow distribution pie (zero slices dropped), top deposits/withdrawals tables, monthly details table; professional empty state; all inputs sanitized; responsive `.bae-chart-duo` (collapses < 880px). **PART 2 — Professional filter bar:** quick ranges (اليوم/هذا الأسبوع/هذا الشهر/آخر 30 يوم/آخر 90 يوم/الكل via pure `quickRangeToDates`), transaction type (الكل/إيداعات/سحوبات/رسوم/شيكات/تحويلات — **server-side**), amount range من/إلى (**server-side**), text search (debounced), date range, refresh, CSV export; active filter chips with individual remove + "مسح الكل"; live result count; distinct filtered-empty vs unfiltered-empty states. **Backend filter support:** `buildTimelineWhere()` extracted+exported (pure, composes date+type+amount+search via Prisma `AND`/`OR`); `TimelineQuerySchema` + controller + `getTimeline` extended with `type`/`minAmount`/`maxAmount`; frontend `getTimeline` switched to an object `TimelineFilters` arg (call site in `BankReconciliation.tsx` updated). **PART 3/4 — Timeline + Drawer:** keyboard-accessible rows (`tabIndex`/`role="button"`/Enter+Space), selected-row + focus-visible states, direction icon (green in / red out), sticky header, scroll container; `TransactionDrawer` upgraded — Escape closes, focus moves into panel on open and returns to trigger on close, body scroll-lock, `aria-labelledby`, added العملة/مفتاح الحساب/حالة المطابقة (raw, display-only)/البصمة (`transactionFingerprint` now exposed by the timeline API). **PART 7 — UI quality:** semantic colors (green deposits / red withdrawals / blue balance), section-title icons, scoped `.bae-*` CSS (+110 lines). **PART 8 — Tests (+34):** backend `fillMonthlyGaps` cap/NaN guards (5) + `buildTimelineWhere` composition (7); frontend `bankTimelineFilters` quick-range/safeNum (10), `ErrorBoundary` catch/retry/reset (4), `AnalyticsTab` empty+malformed+NaN safety (3), `TimelineTab` drawer open/Escape/keyboard/chips/clear/empty/type-request (5). **Files:** 2 new frontend (`ErrorBoundary.tsx`, `bankTimelineFilters.ts`) + 4 new test files; modified `BankAccountExplorer.tsx/.css`, `bankAccounts.service.ts`, bankStatementImport `service.ts/schema.ts/controller.ts/types.ts`, `api/bankStatementImport.ts`, `BankReconciliation.tsx`. **Validation: prisma ✅, backend tsc ✅, frontend tsc ✅, electron tsc ✅, backend vitest 1190/1190 (63 files), frontend vitest 610/610 (32 files), build:back ✅, build:front ✅.** Commits: `fix(bank): stabilize account explorer analytics tab`, `feat(bank): enhance transaction explorer filters and details drawer`, `docs: update PROJECT_STATE for bank account explorer phase c`. |
| **Bank Account Explorer — Polish Phase 1** | `stable-bank-account-explorer-polish-phase1-v1` | Pure UX/UI polish pass on top of Phase B. Zero backend/DB/architecture changes. **Icon fix (highest priority):** all `material-icons-round` class references replaced with `material-symbols-outlined` across `BankAccounts.tsx`, `BankAccountExplorer.tsx`, and `BankAccountExplorer.css` — icons now render correctly instead of displaying as raw text strings. **Executive Header redesign:** shows bank name, current balance in large primary-color text, and a metadata row with date range / transaction count / import count beneath. **Primary KPI card:** Current Balance card promoted with `bae-kpi-card--primary` CSS class — spans 2 columns, gradient background, 22px value text, 48px icon. **Account Health Card:** new `AccountHealthCard` component in OverviewTab showing: coverage period in months, imports count, avg transactions/month, deposit count, withdrawal count, data freshness with good/warn/bad color coding (green=<30 days, orange=30-90, red=>90). **Transaction Drawer:** clicking any row in TimelineTab opens a read-only slide-in side drawer (`TransactionDrawer` component, 400px right-anchored, `@keyframes` slide-in, overlay backdrop) showing debit/credit amounts, balance, statement date, posting date, description, reference, cheque number, fee category badge, import batch label, file name, import date, import ID, duplicate/fee flags. **Improved Empty States (BankAccounts landing):** illustration icon in rounded container + heading + description + primary action button; search-no-results state now has a clear-search CTA button. **CSS additions (230+ lines):** executive header (`.bae-exec-balance`, `.bae-meta-row`), primary KPI card (`.bae-kpi-card--primary`), health card (`.bae-health-card`, `.bae-health-grid`, `.bae-health-value.good/warn/bad`), drawer (`.bae-drawer`, `.bae-drawer-overlay`, `.bae-drawer-header`, `.bae-drawer-body`, `.bae-drawer-amounts`, `.bae-drawer-field`, `@keyframes bae-drawer-slide-in`), blue KPI variant (`.bae-kpi-card--blue`), drawer status classes (`.bae-drawer-warn`, `.bae-drawer-primary`), improved row hover, empty illustration (`.bac-empty-illus`). **3 files changed** (`BankAccounts.tsx`, `BankAccountExplorer.tsx`, `BankAccountExplorer.css`). **Validation: frontend tsc ✅ 0 errors, backend tsc ✅ 0 errors, electron tsc ✅ 0 errors, 1178/1178 tests ✅ (61 files, unchanged), build:back ✅, build:front ✅. Commit `b2227b2`.** |
| **Bank Account Explorer — Phase B** | `stable-bank-account-explorer-phase-b-v1` | Full-stack Bank Account Explorer built on top of the existing Unified Bank Account Timeline architecture. **No new permissions, no schema changes, no migrations** — reuses `bankStatementImport.read` for all access control. **Backend — new module `bankAccounts/` (6 files):** `bankAccounts.types.ts` (`BankAccountSummary`, `BankAccountDashboard`, `MonthlyEntry`, `TopTransaction` interfaces); `bankAccounts.service.ts` — `listBankAccounts()` groups `bankStatementTransaction` by `accountKey`+`bankName` (single Prisma `groupBy`), joins import counts via `bankStatementImport.groupBy`, then fetches latest balance via `findFirst` per account — N+1 acceptable (<10 accounts); `getBankAccountDashboard(accountKey)` runs parallel `Promise.all` aggregates (count, meta, overall stats, deposit agg, withdrawal agg, import count) + `prisma.$queryRaw` with `strftime('%Y-%m', statementDate)` on `bank_statement_transactions` (SQLite table `@@map` name) for monthly grouping + `findMany` for top-5 deposits/withdrawals; all `Decimal` fields converted via `Number()`; dates formatted as `YYYY-MM-DD` strings; returns `null` when no transactions exist. `bankAccounts.controller.ts` — `listBankAccountsHandler` + `getDashboardHandler`; uses `ok(res, data)` from `@core/utils/response.js` + `throw AppError.badRequest/notFound`; `decodeURIComponent(req.params.accountKey)` for special characters in accountKey. `bankAccounts.schema.ts` — `AccountKeyParamSchema` (Zod, min 1 char). `bankAccounts.routes.ts` — `GET /api/bank-accounts` + `GET /api/bank-accounts/:accountKey/dashboard`; both require `authenticate` + `requirePermission('bankStatementImport.read')`. Registered in `backend/src/app.ts`. `__tests__/bankAccounts.test.ts` — 9 tests: `listBankAccounts` (4: empty array, single account mapping, sort by lastTransactionDate desc, missing importMap graceful); `getBankAccountDashboard` (5: null when 0 txs, netCashFlow = deposits − withdrawals, coverage dates YYYY-MM-DD, monthly raw SQL mapping with netFlow, top deposits/withdrawals mapping). **Frontend — 4 new files:** `frontend/src/api/bankAccounts.ts` — `listBankAccounts()` + `getBankAccountDashboard(accountKey)` API client with full TypeScript types mirroring backend. `frontend/src/pages/BankAccounts.tsx` — landing page at `/bank-accounts`; one `AccountCard` per `accountKey` showing bank name, IBAN, balance, debit/credit totals, net flow badge, import count, last import date, transaction count, date range; client-side search filter across bankName/IBAN/accountNumber/accountKey; empty/loading/error states with retry; click navigates to `/bank-accounts/:accountKey`; CSS inline `<style>` block with `.bac-*` prefix; all buttons have `type="button"`. `frontend/src/pages/BankAccountExplorer.tsx` — 5-tab explorer at `/bank-accounts/:accountKey`; tabs: Overview (KPI cards + coverage strip), Timeline (reuses `getTimeline(accountKey)` from `bankStatementImport.ts` — not duplicated), Analytics (Recharts BarChart monthly + PieChart deposit/withdrawal split), Imports (filters `listImports()` client-side by accountKey), Export (paginates timeline in 500-row chunks and downloads CSV); `KpiCard` sub-component uses `colorVariant?: 'green' | 'red'` with CSS modifier classes (no inline styles); `ChartTooltip` dynamic fill inline style left as-is (Recharts standard pattern); Pie label callback types as `{ name?: string; percent?: number }` for Recharts v3.8.1 compatibility; `encodeURIComponent` on navigation, `decodeURIComponent` on params. `frontend/src/pages/BankAccountExplorer.css` — `.bae-*` prefix, all styles externalized; `.bae-kpi-card--green` + `.bae-kpi-card--red` modifier classes for color variants; `.bae-coverage-icon` for icon styling; tab bar, timeline table, pagination, chart sections, export card. **Modified files (4):** `frontend/src/App.tsx` (+2 routes: `/bank-accounts`, `/bank-accounts/:accountKey`); `frontend/src/config/modules.tsx` (nav entry in `nav.group.import_center`: `bank-accounts` / `account_balance_wallet` / `bankStatementImport.read`); `frontend/src/lib/i18n.ts` (`nav.bank_accounts`: مستعرض الحسابات / Account Explorer); `backend/src/app.ts` (bankAccounts router registered). **10 files changed** (6 new backend files, 4 new frontend files, 4 modified). **Tests: 1177/1177 (61 files, +9 from bankAccounts.test.ts)**. Validation: prisma validate ✅, backend tsc ✅ 0 errors, frontend tsc ✅ 0 errors, electron tsc ✅ 0 errors, build:back ✅, build:front ✅. |
| **Bank Statement Import — Phase A Engine (Coverage Analysis + Gemini Polish)** | `stable-bank-statement-import-phase-a-v1` | Gap-fill pass on the existing incremental import engine applying all Gemini review notes. **Types:** `CoverageWarning` union type (`'FULLY_DUPLICATE' \| 'OVERLAPPING' \| 'GAP_BEFORE' \| 'GAP_AFTER'`) added to `types.ts`; `CoverageSummary` extended with `coverageWarning`, `isFullyContained`, `importFrom`, `importTo` fields. **Backend:** `buildCoverageSummary` in `dedupDetector.ts` now computes date-range overlap/gap analysis — 4 distinct warning scenarios derived from lexicographic date string comparisons; returns `null` when no existing data or import dates absent. `previewBuilder.ts` updated to pass `importFrom/importTo` to `buildCoverageSummary`. `types.ts` exports updated. **Frontend (`bankStatementImport.ts`):** `CoverageWarning` + extended `CoverageSummary` mirrored in API types. **Frontend (`BankStatementImport.tsx`):** Coverage banner replaced with `COVERAGE_WARNING_CONFIG` — scenario-aware Arabic copy: FULLY_DUPLICATE shows amber "يبدو أن هذا الكشف مستورد بالفعل" (safe, duplicates skipped automatically); OVERLAPPING shows blue "تداخل في الفترة الزمنية — آمن تماماً" (zero alarm language); GAP_BEFORE/GAP_AFTER show green informational messages. `fileLoading` state added — drop zone disables and shows "جارٍ قراءة الملف…" spinner during file parse, preventing double-parse. Analyze/back/execute buttons all gated on their respective loading states. **Tests (`phaseA.test.ts`, 13 tests):** `buildCoverageSummary` — 7 cases (empty DB, FULLY_DUPLICATE, GAP_BEFORE, OVERLAPPING, GAP_AFTER, no importFrom/To, field population check); `buildDedupSummary` zero-new — 2 cases (all-match by transactionId, all-match by fingerprint; wouldInsert=0, duplicateRate=1); session summary count accuracy — 3 cases (all-new batch, 50/50 overlapping file, potential-duplicate separation); incremental overlapping statement — 1 case (Jan+Feb existing, Mar new → wouldInsert=1). **No schema changes, no new migrations, no new API routes, no permission changes.** 6 files changed. Validation: prisma validate ✅, backend tsc ✅ 0 errors, frontend tsc ✅ 0 errors, electron tsc ✅ 0 errors, 1168/1168 tests ✅ (60 files, +13 from phaseA.test.ts), build:back ✅, build:front ✅. |
| **Unified Bank Account Timeline Architecture** | `stable-bank-unified-timeline-architecture-v1` | Architectural alignment establishing the Unified Bank Account Timeline as the primary operational entity. Import Batches are provenance/audit only. **Explorer (BankReconciliation.tsx):** auto-switches to Timeline mode when `accountKey` is present; toggle bar between `التسلسل الزمني الموحد` and `دفعة الاستيراد الحالية`; timeline KPI cards (total ops, from/to dates, import batch count, page-level debit/credit); date-range + debounced search filter bar; timeline table with provenance columns (import batch label, file name); pagination (50/page); CSV export with account-aware filename `bank-account-timeline-{bank-slug}-{fromDate}-to-{toDate}.csv`. **Backend:** `getWorkspace` returns `accountKey`; `getTimeline` extended with `fromDate/toDate/search` query params; `listImports` exposes `accountKey`. **UI terminology:** الحساب البنكي / السجل الزمني / دفعة الاستيراد / إضافة كشف بنكي; nav labels updated (الحسابات البنكية / إضافة كشف بنكي); empty state "لا توجد بيانات عمليات بنكية مستوردة حتى الآن"; delete dialogs warn about timeline impact explicitly; batch CSV filename `bank-import-batch-{importId}-{date}.csv`; BankStatementImport.tsx "الانتقال إلى السجل الزمني". **ADR:** `docs/ADR-001-unified-bank-account-timeline.md` — canonical reference for architecture decisions, subsystem relationships, and Phase v3.1 roadmap. No schema changes. No deduplication changes. No timeline logic changes. Gemini: APPROVED WITH NOTES, all notes applied. Commit: `273df7e`. |
| **Bank Statement Incremental Import v2** | `stable-bank-statement-incremental-import-v2` | Cross-import deduplication and unified account timeline. `fingerprint.ts`: `buildAccountKey` (IBAN > accountNumber > bankName), `computeFingerprint` (SHA-256/32, txId-path or date+amount+desc-path), `isSimilarDescription` (Arabic-normalised fuzzy match). `dedupDetector.ts`: 5-stage classification — (1) transactionId exact match, (2) reference match within accountKey, (3) accountKey+transactionFingerprint match, (4) potential duplicate (date+amount+similar description → REVIEW status), (5) new. Batch-fetches existing DB snapshot before classifying entire file. Prisma migration `20260629100000`: `accountKey` + `transactionFingerprint` columns on `bank_statement_transactions`; composite partial unique index (raw SQL `WHERE both NOT NULL`). `previewBuilder`: injects `dedupSummary` + `coverageSummary`. `service.execute`: incremental — confirmed dupes skipped, potential dupes inserted with `REVIEW` reconcileStatus, session report persisted (`insertedNewCount`, `skippedDuplicateCount`, `potentialDuplicateCount`, `duplicateRate`, `newDataRate`, `executionTimeMs`). Timeline endpoint: `GET /timeline/:accountKey` — unified view across all imports with provenance (`importBatchLabel`, `fileName`, `importedAt`, `importId`). Frontend: coverage banner + dedup analysis card in preview step; extended done-screen stats (inserted/skipped/potential/rate/time); "View Unified Timeline" button + timeline table with provenance columns. Tests: +35 (23 fingerprint, 12 dedupDetector) → total 1155. Gemini review: APPROVED. Commit: `1f269b6`. |
| **Bank Statement Explorer Phase 5C — Export, Delete, Multi-Select & UX** | `stable-bank-statement-explorer-phase5c-v1` | 8-package enhancement layer on top of `BankReconciliation.tsx` (مستكشف كشف الحساب البنكي). **Package X — Authenticated Export Dropdown:** Replaced `<a href>` export links (JWT bypass bug) with Axios `responseType:'blob'` download; export dropdown menu (PDF / Excel / CSV / Print); `downloadExport()` + `exportToCsv()` functions in `bankStatementImport.ts` API client; CSV has UTF-8 BOM (`﻿`) for Arabic compatibility in Excel. **Package Y — Professional Import Cards:** `ImportSelector` rewritten — bank icon, smart-truncated filename (`smartTruncate()`), date-range chip, statement-duration chip, color-coded debit/credit stats. **Package Z — 5 New KPI Cards:** أعلى سحب (`maxDebit`), أعلى إيداع (`maxCredit`), متوسط المبلغ (`avg`), أول عملية (`fromDate`), آخر عملية (`toDate`), مدة الكشف (days count from importMeta). `analyticalStats` updated: replaced `largest` with `maxDebit`/`maxCredit`. **Package AA — Row Color Tinting:** Row class logic: `BANK_TRANSFER→tx-row-transfer` (green), `CHEQUE_PAYMENT→tx-row-cheque` (blue), `CASH_WITHDRAWAL→tx-row-withdrawal` (amber), `isBankFee→tx-row-fee` (purple); CSS uses `color-mix(in srgb, color 5%, transparent)` overlays. **Package AB — Clickable Warning Tags:** `DataQualityWarnings` props extended (`activeWarningCode`, `onFilterWarning`); tags are `<button>` with `.recon-warning-tag-active` when matching; `warningCode: string | null` added to `ClientFilter`; `displayedTransactions` filters by `warningCode` when set. **Package AC — Multi-Select ImportSelector:** Per-card checkboxes, select-all with indeterminate state (`ref={(el) => { if (el) el.indeterminate = ... }}`), `selectedIds: Set<number>` state; aggregate stats bar (count, rows, debits, credits); group actions: عرض (single-select only) and حذف المحددة. **Package AD — Delete Single & Bulk:** Backend `DELETE /:importId` + `POST /bulk-delete` routes; `deleteImport()` + `bulkDeleteImports()` in service.ts; `deleteHandler` + `bulkDeleteHandler` in controller.ts; `BulkDeleteImportSchema` in schema.ts; `bankStatementImport.delete` added to `MODULE_ACTIONS` + GENERAL_MANAGER + ACCOUNTANT in `seed.ts`; cascade-safe (`BankStatementTransaction.onDelete:Cascade`); `AuditLog` on single and bulk. Frontend: `deleteImport()` + `bulkDeleteImports()` in API client; `canDelete` = `hasPermission('bankStatementImport.delete')`; `ConfirmModal` reusable danger-styled component with loading spinner; 🗑 حذف button in workspace header (permission-gated). **Package AE — Skeleton Loading & Polish:** 3 animated skeleton placeholder cards during `ImportSelector` loading; hover-reveal per-card delete button; full-click card selection. **No Prisma schema changes. No migrations. No API contract changes. No reconciliation algorithm changes.** 8 files changed: `backend/prisma/seed.ts`, `backend/src/modules/bankStatementImport/controller.ts`, `routes.ts`, `schema.ts`, `service.ts`, `frontend/src/api/bankStatementImport.ts`, `frontend/src/pages/BankReconciliation.css` (new), `frontend/src/pages/BankReconciliation.tsx`. +3116 insertions, -403 deletions. **Validation: backend tsc ✅, frontend tsc ✅, electron tsc ✅, 1120/1120 backend tests (57 files), build:back ✅, build:front ✅. Gemini APPROVED ✅. Commit `f669b74`.** |
| **Bank Statement Import Intelligence & Validation Polish** | `stable-bank-import-validation-polish-v1` | Validation & classification improvement for bank statement import — no new modules, no schema changes, no permissions, no migrations. **Root cause fix:** `BALANCE_BREAK` was a fatal error, causing 114/115 rows of a Gulf Bank statement (newest-first descending order) to fail. Moved `BALANCE_BREAK` from `errors[]` to `warnings[]` in `validators.ts` — balance continuity is a data-quality signal, not a structural validity check. **Gulf Bank parser update:** Column template corrected to actual export layout (Date / Description / Debit / Credit / Balance / Currency); auto-detection upgraded to duplicate-Currency column signature (positions 2 and 5). **Transaction classification (Phase C):** Added `CASH_WITHDRAWAL`, `CHEQUE_PAYMENT`, `BANK_TRANSFER` to `BankFeeType`; `FeePattern` extended with `isBankFee: boolean` so non-fee categories (e.g. cash withdrawal) are classified without flagging as bank fees. Gulf Bank-specific patterns added (cash withdrawal, cheque payment, outgoing RTGS/clearing, outgoing clearing cheque). True fee patterns extended: `local bank charge` → `CHARGE`; `cheque book charges` → `CHEQUEBOOK_FEE`. **KD → KWD alias:** `normalizer.ts` normalises Gulf Bank's non-standard `KD` currency code to `KWD` via `CURRENCY_ALIASES` map. **CSV preamble support (Phase F):** `findCsvTransactionHeaderRow` (backend `parser.ts`) and `findCsvTransactionHeaderRowClient` (frontend `bankStatementParser.ts`) scan rows for the first line containing ≥3 known column keywords, skipping bank-info preamble rows; `parseCsvRows` / `parseCsvRowsClient` use first-occurrence-wins column index map. **Re-parse on bank change (Phase G):** Frontend stores raw sheet data / CSV text on file select; `handleBankChange()` re-runs the parser with the newly selected bank template when user changes the dropdown — no re-upload required. **UI improvements (Phases D/E/I):** `VALIDATION_LABELS` Arabic error-reason map + `rowReasonLabel` helper; "ملاحظة" (note) column added to preview table; blue category badge for non-fee classified transactions; empty-state warning when 0 rows parsed. **Tests (Phase H):** 25+ new tests across 6 new describe blocks: Gulf Bank actual export format (7 tests), BALANCE_BREAK is a warning (2 tests), Gulf Bank fee classification (9 tests), currency alias normalisation (2 tests), CSV preamble detection (3 tests), CSV preamble parse (2 tests). All 1120 backend tests pass (57 files). **Files changed:** `bankStatementImport/validators.ts`, `bankStatementImport/normalizer.ts`, `bankStatementImport/types.ts`, `bankStatementImport/bankFeeDetector.ts`, `bankStatementImport/parser.ts`, `bankStatementImport/__tests__/bankStatementImport.test.ts`, `frontend/api/bankStatementImport.ts`, `frontend/pages/BankStatementImport.tsx`, `frontend/utils/bankStatementParser.ts` (9 files, 456 insertions, 58 deletions). **Validation:** backend tsc ✅, frontend tsc ✅, backend build ✅, frontend build ✅, 1120/1120 tests ✅. **Gemini APPROVED ✅. Commit `02a2054`.** |
| **Bank Statement Import & Smart Reconciliation Phase 1** | `stable-bank-statement-import-phase1-v1` | Full-stack bank statement import and reconciliation system. **Architecture:** New Prisma models `BankStatementImport` + `BankStatementTransaction` (cascade delete). Frontend parses Excel/CSV client-side and sends `StatementTransaction[]` to backend for re-validation + atomic write. **Supported banks (7):** NBK, KFH, Gulf Bank, Boubyan, Warba, Ahli United, UNKNOWN fallback — each with unique column header signatures in Arabic/English via `STATEMENT_CONFIGS`. **Backend — 16 files in `bankStatementImport/`:** `types.ts` (all interfaces/types: `ReconcileStatus` enum `UNMATCHED\|MATCHED\|IGNORED\|DUPLICATE\|REVIEW`, `MatchedType`, `MatchConfidence` 100/90/75/0, `BankFeeType`, `BankColumnMap`, `BankTemplate`, `StatementTransaction`, `ValidationRule`, `RowValidation`, `PreviewRow`, `ImportPreviewSummary`, `MatchCandidate`, `MatchResult`, `ReconciliationTransaction`, `ReconciliationWorkspace`, `PostingSuggestion`, `ReconciliationReport`, etc.); `parser.ts` (`STATEMENT_CONFIGS` registry, `detectBankTemplate`, `parseDateString` handles ISO/DD-MM-YYYY/Excel serial, `parseAmount`, `parseExcelRows`, `detectCsvDelimiter`, `parseCsvRows`); `normalizer.ts` (`buildNormalizedText` Arabic normalization, `roundKwd` 3dp, `normalizeRow`); `validators.ts` (9 validation rules errors+warnings, `checkBalanceContinuity`, `detectFileDuplicates`, `validateRows`); `matcher.ts` (8 matching strategies — exact cheque number confidence 100, exact transactionId→payment reference 100, invoice number in description 90, payment reference in description 90, cheque in description 90, amount+invoice 75, amount+expense 75, amount+payroll netSalary 75; deduplication per type:id pair; `loadMatcherContext` loads 6 data sources from Prisma; `matchAllTransactions` batch); `bankFeeDetector.ts` (6 fee types: WIRE_FEE, MAINTENANCE_FEE, CARD_FEE, ATM_FEE, FOREX_FEE, OTHER; Arabic+English regex patterns); `previewBuilder.ts` (`buildPreview()` — normalize→validate→duplicate-detect→match-all→fee-detect→assemble in one pass); `reconciliationEngine.ts` (`ALLOWED_TRANSITIONS` guard, `getWorkspace` with filter, `updateStatus` with transition validation, `bulkUpdateStatus` batch); `postingSuggestions.ts` (4 suggestion types: INVOICE_PAYMENT, EXPENSE_LINK, JOURNAL_ENTRY, IGNORE); `reportBuilder.ts` (`buildReconciliationReportExcel` ExcelJS 3-sheet: ملخص/المطابقة/غير المطابقة; `buildReconciliationReportHtml` via Unified Report Engine `buildReportHtml()`); `schema.ts` (strict Zod: `StatementTransactionSchema` max 10,000 rows, bounded fields; `PreviewRequestSchema`, `ExecuteImportSchema`, `WorkspaceQuerySchema`, `UpdateStatusSchema`, `BulkUpdateStatusSchema`, `ReportExportSchema`); `service.ts` (`preview`, `execute` via `prisma.$transaction` with chunk-500 `createMany`, `getWorkspaceByImport`, `updateTransactionStatus`, `bulkUpdateTransactionStatus`, `getPostingSuggestions`, `exportReport`, `listImports`); `controller.ts` (`ok`/`created` responses, `recordAudit` on all mutating ops, `req.user.username` from `TokenPayload`); `routes.ts` (8 RBAC-gated routes: `bankStatementImport.read/create/export/reconcile`); `__tests__/bankStatementImport.test.ts` (100+ tests across 8 groups). **Permissions:** `bankStatementImport.read`, `bankStatementImport.create`, `bankStatementImport.export`, `bankStatementImport.reconcile` added to SYSTEM_ADMIN + ACCOUNTANT; `bankStatementImport.read` added to PROJECT_MANAGER. **Frontend — 4 new files:** `api/bankStatementImport.ts` (typed API client, all TypeScript types mirroring backend); `utils/bankStatementParser.ts` (client-side parser mirroring backend: `CLIENT_STATEMENT_CONFIGS`, `detectBankTemplateClient`, `parseExcelRowsClient`, `parseCsvRowsClient`, `detectCsvDelimiterClient`); `pages/BankStatementImport.tsx` (5-step wizard: Upload→Detect→Preview→Confirm→Done; 10MB cap; drag-and-drop; up to 500 preview rows with remainder note); `pages/BankReconciliation.tsx` (full reconciliation workspace: `ImportSelector` when no importId, status summary cards with clickable filters, filter bar with search/isBankFee/isDuplicate, bulk action toolbar, transaction table with per-row actions, `PostingSuggestionsPanel` modal, pagination). **Never auto-post:** all posting suggestions require explicit user confirmation — no automatic GL writes. **Integrations registry:** `bank-statement-import` promoted from `planned` to `available/stable` with `targetRoute: '/bank-statement-import'`. **Routing:** `/bank-statement-import` + `/bank-reconciliation` + `/bank-reconciliation/:importId` in `App.tsx`. **Nav:** `استيراد كشف الحساب` + `مطابقة كشف الحساب` (bank/financial nav group, `bankStatementImport.read` permission). **Security:** 10MB file cap, 5MB per-row string bounds, dangerous formula rejection in Zod schemas, HTML injection blocked via `esc()` in report builder, no SQL injection via Prisma parameterized queries, `Content-Security-Policy: sandbox; default-src 'none'` on PDF export, `Content-Disposition: attachment` on all downloads, AuditLog on every mutating operation, never auto-post. **29 files changed** (16 new backend, 4 new frontend, 9 modified), 4760 insertions. Prisma schema + migration (2 new models). **Tests: 1018/1018 backend (51 files, +100+ new tests) + 588/588 frontend (28 files, unchanged). Gemini APPROVED WITH MINOR NOTES ✅.** |
| **Financial Center Phase 2 — GL Report, Trial Balance, Aging, Journal Book, Financial Reports, Financial Dashboard** | `stable-financial-center-phase2-v1` | Full financial intelligence layer added to the existing Financial Center (`FinancialCenter.tsx`). **Architecture: Orchestrator pattern** — `financial.service.ts` delegates all queries to `accountingService` and shared utilities; never reimplements query logic. **No Prisma schema changes. No migrations. No Electron IPC.** **Part 1 — GL Report:** `GET /api/financial/gl-report` + `/gl-report/export`; `GlReportSchema` (accountId/fromDate/toDate/page/pageSize/format); `getGLReport()` service method fetching `JournalEntryLine` rows joined to `Account` + `JournalEntry`; M9 balance display via `BalanceDisplay.tsx` + `formatBalance()` (absolute value + مدين/دائن indicator, EPSILON guard for near-zero); `GlReportTab.tsx` component (account picker, date range, paginated table with debit/credit/running balance in KWD 3dp, Excel/PDF export). `scrollY` restoration on pagination. N+1 eliminated (single Prisma query with select instead of chained findMany). **Part 2 — Trial Balance:** `GET /api/financial/trial-balance` + `/trial-balance/export`; aggregates debit/credit per account for date range; `TrialBalanceTab.tsx` (date range filter, account-level totals, grand total row). **Part 3 — Aging Report:** `GET /api/financial/aging-report` + `/aging-report/export`; customer/supplier aging buckets (0–30/31–60/61–90/90+ days) computed from unpaid/partial invoices; `AgingReportTab.tsx` (AR/AP tabs, entity search, color-coded buckets). **Part 4 — Journal Book:** `GET /api/financial/journal-book` + `/journal-book/export`; paginated journal entries with line-level debit/credit; `JournalBookTab.tsx` (date range, entry-level table with expand-to-lines). **Part 5 — Financial Reports + Financial Dashboard + Dashboard مالي tab + Migration banner.** `backend/src/shared/services/financial/dashboard-summary.service.ts`: 45s TTL module-level cache (`let cache`); 9 parallel Prisma queries (AR aggregate, AP aggregate, top-5 customers/suppliers via `$queryRaw` tagged template literals, 30-day collections/payments, account count, 90-day critical AR/AP). `backend/src/shared/services/financial/export/summary.export.adapter.ts`: `toSummaryReportInput()` converts `FinancialResponse<FinancialRow>` to `ReportInput` for Excel/PDF. Three new service methods on `FinancialService`: `getFinancialSummary(filters)` (delegates to `accountingService.financialSummary(from,to)` — positional params), `exportFinancialSummary(filters,format)`, `getDashboardSummary()`. Three new routes: `GET /api/financial/summary` (`finreports.read`), `GET /api/financial/summary/export` (`finreports.export`), `GET /api/financial/dashboard-summary` (`financialdashboard.read`). `constants.ts`: `'financialdashboard'` added to MODULES array. **Frontend — FinancialReportsTab.tsx:** 4 summary KPI cards (إيرادات/مصاريف/صافي/تحصيلات), M10 disclaimer (operational tables not GL), ExportBar, 4 "قريباً" placeholder cards. **Frontend — FinancialDashboardTab.tsx:** AR/AP outstanding cards with حرج +90 يوم sub-line + navigation links; 30-day collections/payments cards; top-5 customers/suppliers tables (clickable rows → `/financial?tab=statement`); `آخر تحديث` from `data.generatedAt`; `X-Cache-Age` response header. **Frontend — Dashboard.tsx:** `GeneralDashboardContent` (renamed existing function, no export), new thin `export default function Dashboard()` adds `.db-tab-bar` + `db-tab-btn` tabs (عام/مالي) with `localStorage.getItem('dashboard.tab')` persistence; مالي tab gated by `financialdashboard.read`; wrapper uses plain `<div>` (not `.db-page`) to avoid dark-theme double-nesting. **Frontend — Statements.tsx:** dismissible migration banner with `localStorage.setItem('statements.bannerDismissed','1')` guard; "فتح الإصدار الجديد" button navigates to `/financial?tab=statement`. **Frontend — FinancialCenter.tsx:** finreport tab wired to `<FinancialReportsTab>` with `frFrom`/`frTo` URL params (scoped to avoid collision with glFrom/glTo and fromDate/toDate). **CSS:** financial.css appended Part 5 styles; dashboard.css added `.db-tab-bar` / `.db-tab-btn` / `.db-tab-btn.active`. **New permission key:** `financialdashboard.read` (added to MODULES; `financialdashboard` module). **Tests: 718/718 backend (45 files) + 516/516 frontend (25 files). Gemini APPROVED ✅. 64 files changed, 6292 insertions, 11 deletions.** |
| **Statement Center Phase 1 — Customer & Supplier Statements** | `stable-statement-center-phase1-v1` | Shared Statement Engine for real-time customer and supplier account statements. **Backend — Types (`statement.types.ts`):** `StatementEntityType` (`'CUSTOMER' \| 'SUPPLIER'`), `StatementReferenceType` (`'INVOICE' \| 'PAYMENT' \| 'EXPENSE'`), `StatementFilters`, `StatementEntry` (id/date/reference/referenceType/referenceId/description/debit/credit/runningBalance/status/entityName/entityCode), `StatementSummary` (openingBalance/totalDebit/totalCredit/closingBalance/transactionCount), `StatementResult`, `StatementInput`. **Backend — Engine (`statement.service.ts`):** `buildStatement()` dispatcher → `buildCustomerStatement` (AR: invoices=debit, payments=credit, formula `balance + debit - credit`) or `buildSupplierStatement` (AP: invoices=credit, expenses=credit, payments=debit, formula `balance - debit + credit`); opening balance computed from all movements before `fromDate`; `assembleResult()` applies entity-type-specific running balance and closing balance formulas (`isSupplier` branch); chronological sort; filters by referenceType / status / search; customer opening balance: `Σinvoices − Σpayments`; supplier opening balance: `Σinvoices + Σexpenses − Σpayments`. **Backend — Module (`statements`):** 4 routes — `GET /api/statements/customers/:id` (requires `statements.read`), `GET /api/statements/suppliers/:id` (requires `statements.read`), `GET /api/statements/customers/:id/export` (requires `statements.export`), `GET /api/statements/suppliers/:id/export` (requires `statements.export`); registered in `app.ts`. **Backend — Controller:** NaN/invalid entityId guard (`parseEntityId` → HTTP 400 with Arabic message) on all 4 handlers; Excel export via `buildExcel()` (8-column sheet: التاريخ/المرجع/النوع/البيان/مدين/دائن/الرصيد/الحالة); `refTypeAr()` helper; KWD 3-decimal formatting; `ok()` response utility. **Backend — Constants:** `'statements'` added to `MODULES` array; `statements.read` and `statements.export` permission keys. **Backend — Seed:** `statements: ['read', 'export']` in MODULE_ACTIONS; ACCOUNTANT role → both permissions; PROJECT_MANAGER → `statements.read`. **Backend — Tests (11 tests):** Customer Statement — empty statement, sales invoices as debit + payments as credit, running balance across entries, opening balance from pre-fromDate transactions, date range exclusion (Prisma filter delegate), referenceType filter, summary totals (totalDebit/totalCredit/closingBalance/transactionCount). Supplier Statement — invoices+expenses as credit + payments as debit, opening balance (Σinvoices+Σexpenses−Σpayments=900), running balance decreases with payments. Error handling — unsupported entity type throws. **Frontend — API (`statements.ts`):** `import { api }` named export; `cleanFilters()` strips undefined/empty strings; `statementsApi` with `getCustomerStatement`, `getSupplierStatement`, `exportCustomer` (triggers file download via `downloadBlob`), `exportSupplier`; all types re-exported. **Frontend — Page (`Statements.tsx`, 591 lines):** RTL page with two tabs (customers/suppliers, fully independent state via `TabPanel` sub-component); entity picker (fetches 500 entities from `/customers` or `/suppliers`); filter bar: entity dropdown, fromDate, toDate, referenceType select (الكل/فاتورة/دفعة/مصروف), search input, عرض button, Excel export button; 5 summary cards: رصيد افتتاحي, مجموع مدين (green), مجموع دائن (red), رصيد ختامي (bold, red if negative), عدد الحركات; plain `<table>` with sticky header, color-coded debit (green if >0) / credit (red if >0) / running balance (red if negative); invoice references clickable → navigate to `/invoices/:id/preview`; empty states for no entity selected / no results; `cancelRef` cancellation guard on entity switch; export error message surfaced on failure (replaces silent catch). **Frontend — Routing:** `<Route path="/statements" element={<Statements />} />` added to `App.tsx`. **Frontend — Navigation:** `{ key: 'statements', label: 'nav.statements', icon: 'account_balance_wallet', permission: 'statements.read' }` in financial group in `modules.tsx`. **Frontend — i18n:** `'nav.statements': 'كشف الحساب'` in both AR and EN. **Accounting sign convention (AP/AR standard):** Customer (AR) — debit increases receivable, credit reduces it, positive balance = customer owes us. Supplier (AP) — credit increases liability, debit reduces it, positive balance = we owe supplier. Opening balance formula identical for both: `Σinvoices + Σexpenses − Σpayments`. **14 files changed** (8 new: `statement.types.ts`, `statement.service.ts`, `statement.service.test.ts`, `statements.controller.ts`, `statements.routes.ts`, `statements.schema.ts`, `statements.ts` API client, `Statements.tsx`; 6 modified: `app.ts`, `constants.ts`, `seed.ts`, `App.tsx`, `modules.tsx`, `i18n.ts`). 1611 insertions. No Prisma schema changes. No migrations. No Electron IPC changes. No new npm packages. **Tests: 628/628 backend (36 files — +11 tests, 1 new test file) + 507/507 frontend (24 files, unchanged). Gemini APPROVED ✅.** |
| **Invoice & Expenses Operations Pack** | `stable-invoice-expenses-operations-pack-v1` | Full-stack invoice and expenses operations hardening. **Backend — Smart FK Conflict Guards:** `customers.remove` — throws rich 409 with the contract code list when linked contracts exist (e.g. "مرتبط بـ 3 عقود: EQ-2024-001، …"); `suppliers.remove` — throws rich 409 with last invoice number + linked expense code (e.g. "مرتبط بـ 2 فاتورة، آخرها MN-INV-2024-001، و1 مصروف EXP-2024-00001"); `equipment.remove` — throws rich 409 with per-category record counts (maintenance / breakdown / fuel / spare parts). **Backend — Expenses Stats Extension:** `stats()` now runs 3 parallel Prisma aggregates for `currentMonth`, `previousMonth`, and `currentYear` based on `billingMonth + billingYear` fields; returned as `periods` sub-object. **Backend — Reports:** billing period column (`شهر الحساب`) added to expenses Excel export. **Frontend — Smart Price Picker:** `ContractOption` type + `contracts`/`contractId`/`pickerSearch` state added to both CreateInvoice and EditInvoice; `partyId` effect fetches contracts + resets `contractId` on customer switch; `filterAsphaltPlant`/`displayPrices` derived values filter prices by active contract's plant; contract selector dropdown renders when SALES + customer + contracts exist; price banner shows active filter badge; picker button and dropdown use `displayPrices` filtered by unit; search input inside picker (sticky top) filters by مصنع/موقع/شركة with Arabic empty state "لا توجد اتفاقيات أسعار مطابقة لهذا العميل / العقد / المصنع". **Frontend — Invoice Item UX:** column header row gets `borderBottom` separator. **Frontend — Expenses Stats Display:** period cards (currentMonth/previousMonth/currentYear) rendered as colored cards below KPI strip; top-6 suppliers by total shown compactly. **Frontend — Billing Period Polish:** `شهر الفوترة` → `شهر الحساب` in DataTable column label, select title, and both form labels (CreateInvoice + EditInvoice); i18n value for `lbl.inv.billing_period` corrected (`حساب شهر` → `شهر الحساب`). **Frontend — i18n Cleanup:** DataTable column labels wired to `lbl.inv.billing_period` and `lbl.inv.remaining_amount`; form labels use `t()`. **Frontend — Conflict Message UI:** `archiveCandidate` state extended with `conflictMessage`; ResourcePage archive modal now shows rich backend conflict message instead of generic fallback. **No Prisma schema changes. No migrations. No new npm packages. No Electron IPC changes.** 12 files changed: 5 modified + 3 new backend test files + 4 modified frontend files. 421 backend insertions + 205 frontend insertions. **Tests:** 591/591 backend (33 files — +20 tests across 3 new test files: `customers.service.test.ts`, `suppliers.service.test.ts`, `equipment.service.test.ts`) + 507/507 frontend (24 files, unchanged). |
| **Accounting Completeness H1 — Purchase Invoice GL Posting** | `stable-accounting-completeness-h1-v1` | Closes the GL gap for purchase-direction invoices and supplier payments. **Purchase Invoice GL (`postPurchaseInvoiceToGL`):** already existed (Phase D); now exposed via an explicit `PATCH /invoices/:id/approve` endpoint secured by `invoices.approve` permission. **Purchase Payment GL (`postPurchasePaymentToGL`):** NEW — Dr ACCOUNTS_PAYABLE (2000), Cr CASH (1000) or BANK (1010) depending on `payment.method` (BANK/TRANSFER → BANK account; CASH/CHEQUE/other → CASH account). Called automatically from `addPayment()` when `invoice.direction === 'PURCHASE'`. Idempotent double-posting guard (`journalEntry.findFirst` on `PURCHASE_PAYMENT` + `referenceId`). Skips SALES invoices and zero-amount payments. KWD 3 decimal precision via `round3()`. **AP Settlement flow:** invoice creation creates liability (Dr PURCHASES 5000, Cr AP 2000); payment creation settles it (Dr AP 2000, Cr Cash/Bank) — net AP = 0 when paid in full. **Reversal (`reversePurchasePaymentGL`):** NEW — creates `PURCHASE_PAYMENT_REVERSAL` journal via `reverseGL()` (debit↔credit swap); never deletes original; no-op if no original GL entry; no-op if reversal already exists. **Approve endpoint:** `PATCH /invoices/:id/approve` → 3-attempt retry on `entryNumber` P2002 collision (`isEntryNumberCollision`); guards: PURCHASE direction required, CANCELLED invoices rejected; records `APPROVE` audit entry. **`clearGLForInvoice()` extended:** now includes `PURCHASE_PAYMENT` and `PURCHASE_PAYMENT_REVERSAL` in referenceType deleteMany for force-delete cleanup. **GL_REFERENCE_TYPES extended:** added `PURCHASE_PAYMENT` and `PURCHASE_PAYMENT_REVERSAL` to `gl.service.ts`. **No Prisma schema changes. No migrations. No frontend changes. No Electron IPC changes. No new npm packages.** 7 files changed (5 modified + 2 new test files), 851 insertions, 4 deletions. **Tests:** 571/571 backend (31 files — +45 new tests across 2 new test files: `invoices.purchase-payment.test.ts` 19 tests + `invoices.approve.test.ts` 26 tests) + 507/507 frontend (24 files). |
| **Contract Profitability Dashboard — Phase 1** | `stable-contract-profitability-phase1-v1` | Contract Financial Summary — per-contract revenue, expense, and profitability analysis. |
| **Financial Receivables Center — Phase 1** | `stable-financial-receivables-phase1-v1` | Customer Statement, Receivables Aging, Customer Balances, Collections Summary reports. Gemini-hardened. |
| **Accounting Module** | *(stable)* | Chart of Accounts, journal entries, transaction ledger, P&L. |
