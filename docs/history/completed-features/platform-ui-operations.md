# Completed Features Archive — المنصّة · الواجهة · العمليات · الأنظمة الأساسية

ExplorerKit and the unified design system, dashboards and executive centres, the AI assistant, integrations, data import, backup, RBAC, auth and the core platform modules.

**Archived from:** `PROJECT_STATE.md` → `## Completed Features` (the flat 126-row table that section held)
**Rotated on:** 2026-09-08 — *Completed Features Documentation Compaction & Archive v1*
**Entries in this file:** 53
**Classification spread:** A=7 · B=4 · C=12 · D=6 · E=14 · F=10

> **Verbatim.** Every feature row below was MOVED, not rewritten — byte-identical to the row as it stood
> in `PROJECT_STATE.md` before compaction, including its stable tag, commit hashes, migration notes,
> validation results, Product Owner approvals and known limitations. Classification metadata lives in the
> index table below and never inside the historical rows themselves.

**Active reference:** `PROJECT_STATE.md` → `## Completed Features — Active Reference` describes what is
true of the system *today*. This file is the historical record and may describe behaviour that has since
been superseded — always prefer the active reference for current behaviour.

See `docs/history/completed-features/README.md` for the full index and search guidance.

---

## Index

| # | Feature | Stable tag | Class | Superseded by | Content SHA-256 |
|---|---------|-----------|-------|---------------|-----------------|
| 2 | ExplorerKit Metrics Consistency & Filter UX Pack v1 | `stable-explorerkit-metrics-consistency-filter-ux-v1` | C | — | `c3fe3b7b65b78804` |
| 5 | ExplorerKit Professional Modernization Pack v1 | `stable-explorerkit-professional-modernization-v1` | C | — | `a811a6fce391e1ce` |
| 6 | Dashboard Executive Polish Pack v1 | `stable-dashboard-executive-polish-v1` | C | — | `d317f480f14510d9` |
| 7 | Unified Excel Export Professional Standard v1 | `stable-unified-excel-export-professional-standard-v1` | C | — | `a4ee0a972156022a` |
| 8 | Executive Command Center Evolution v1 | `stable-executive-command-center-evolution-v1` | C | — | `0ec64f9c7be27232` |
| 12 | Presentation Settings Compliance Pack v1 | `stable-presentation-settings-compliance-v1` | C | — | `a76bd5f9bd68e874` |
| 16 | ExplorerKit Information Hub — Phase 1 | `stable-explorerkit-information-hub-phase1-v1` | C | — | `51173d5dd9c9a3ec` |
| 17 | Universal Export File Naming Standard v1 | `stable-universal-export-file-naming-v1` | B | — | `97b94aaa573d18d3` |
| 18 | Global Date Presentation Standardization v1 | `stable-global-date-presentation-standardization-v1` | B | — | `2fa300aec77bb0a5` |
| 24 | Global Monetary Formatting Production Promotion v1 | `stable-global-monetary-formatting-production-v1` | B | — | `9102036b5e7090b2` |
| 29 | Smart Import Assistant — Phase 2 (2A + 2B) | `stable-smart-import-assistant-phase2-v1` | F | — | `3f9331acd1413a3a` |
| 30 | Smart Import Validation — Phase 1 | `stable-smart-import-validation-phase1-v1` | E | Smart Import Assistant — Phase 2 (2A + 2B) | `919fe83e1e71d702` |
| 31 | Executive Command Center Dashboard Redesign | `stable-executive-command-center-dashboard-v1` | E | Executive Command Center Evolution v1 | `a94b1374a2c2f2b8` |
| 34 | Stability & UX Safety Pack — Phase 1 + Frontend Performance Pack — Phase 1 | `stable-stability-performance-pack-v1` | C | — | `3c2aad9f8cf7c68c` |
| 40 | Executive Dashboard Polish Pack — Phase 1 (Executive Daily Summary) | `stable-executive-dashboard-polish-phase1-v1` | F | — | `51bb27c7d231d066` |
| 41 | Unified Explorer Design System Rollout — Phase 3E (Premium Invoices Explorer) | `stable-unified-explorer-ui-phase3e-v1` | E | ExplorerKit Professional Modernization Pack v1 | `f9d5f63beec8d9b8` |
| 42 | Unified Explorer Design System Rollout — Phase 3D (Premium Cheques Explorer) | `stable-unified-explorer-ui-phase3d-v1` | E | ExplorerKit Professional Modernization Pack v1 | `c07b03838154d7d9` |
| 43 | Unified Explorer Design System Rollout — Phase 3C (Heavy Operational Modules: Inventory + Maintenance) | `stable-unified-explorer-ui-phase3c-v1` | E | ExplorerKit Professional Modernization Pack v1 | `9beed46fecfd17d7` |
| 44 | Unified Explorer Design System Rollout — Phase 3B (Medium-Complexity Operational Modules: Salaries + Project Prices + Attendance) | `stable-unified-explorer-ui-phase3b-v1` | E | ExplorerKit Professional Modernization Pack v1 | `0bf040389ff38a12` |
| 45 | Unified Explorer Design System Rollout — Phase 3A (Quick Wins: Contracts + Users + Forms Hub) | `stable-unified-explorer-ui-phase3a-v1` | E | ExplorerKit Professional Modernization Pack v1 | `5952da8fd9f7d44f` |
| 46 | Unified Explorer Design System Rollout — Phase 2 (Core Operational Modules) | `stable-unified-explorer-ui-phase2-v1` | E | ExplorerKit Professional Modernization Pack v1 | `c4027f5f63491664` |
| 47 | Unified Explorer Design System Rollout — Phase 1 (Reports + Document Expiry + Data Import Centers) | `stable-unified-explorer-ui-bundle-phase1-v1` | E | ExplorerKit Professional Modernization Pack v1 | `5f6529c64b7e6835` |
| 56 | AI Assistant — Phase AI-2.5 (Professional Business Skills Expansion) | `stable-ai-assistant-business-skills-v1` | C | — | `bbf751f8fb9c6849` |
| 57 | AI Assistant — Phase AI-2 (Safe Intelligence Engine) | `stable-ai-assistant-phase-ai-2-v1` | E | AI Assistant — Phase AI-2.5 (Professional Business Skills Expansion) | `149ae3d29a3b6930` |
| 58 | AI Assistant — Phase AI-1.5 (Smart Workspace Polish) | `stable-ai-assistant-phase-ai-1.5-v1` | E | AI Assistant — Phase AI-2.5 (Professional Business Skills Expansion) | `8ed2c7a9e21ee234` |
| 59 | AI Assistant — Phase AI-1 (UI Shell & Workspace Foundation) | *(no separate tag — rolled into `stable-ai-assistant-phase-ai-1.5-v1`)* | E | AI Assistant — Phase AI-2.5 (Professional Business Skills Expansion) | `e27ba69b749deb7b` |
| 60 | Reports Center Explorer — Phase 7 (UI Modernization) | `stable-reports-center-phase7-v1` | F | — | `c50fa48ec6d6c939` |
| 61 | Integrations Center Phase 2 — Professional UI/UX Modernization | `stable-integrations-center-phase2-v1` | F | — | `876ad36a48e03fb8` |
| 65 | Operational Polish Suite Phase 4 | *(feature branch — pending production merge/tag)* | C | — | `24c5d0ced1f361a0` |
| 66 | Operational Polish Suite Phase 3 | `stable-operational-polish-suite-phase3-v1` | C | — | `fc033f342f458c48` |
| 67 | Operations & Financial Workflow Suite Phase 2 | `stable-financial-workflow-suite-phase2-v1` (pending)` | C | — | `a911dd1bb1cbeeb8` |
| 68 | Dashboard Financial Fix + Manus Audit | `stable-dashboard-financial-fix-v1` | F | — | `9b3b36278741a738` |
| 69 | Final Professional Polish Suite Phase 2 | `stable-final-polish-suite-phase2-v1` | F | — | `c1983d7952cedf10` |
| 70 | Operations & Management Suite Phase 1 | `stable-ops-management-suite-phase1-v1` | F | — | `8546b4a59e8ad128` |
| 73 | Integrations Center Phase 1 — Foundation | `stable-integrations-center-phase1-v1` | F | — | `84309da2cac7b6f5` |
| 80 | Approval Workflow Pack v1 — Phase A (Universal Approval Engine Foundation) | `stable-approval-workflow-pack-v1-phase-a` | B | — | `ec1fcd221b75bb4c` |
| 83 | UX Polish Pack v4 | `stable-ux-polish-pack-v4` | F | — | `8afdaf1eb255127b` |
| 95 | Executive Decision Center — Phase 1 | `stable-executive-decision-center-phase1-v1` | F | — | `6a00c6a8a953c588` |
| 99 | Executive Intelligence Bundle — Phase 2 | `stable-executive-intelligence-bundle-phase2-v1` | E | Executive Command Center Evolution v1 | `49f7a28091f1456c` |
| 100 | Executive Intelligence Bundle — Phase 1 | `stable-executive-intelligence-bundle-phase1-v1` | E | Executive Intelligence Bundle — Phase 2 | `dc90a171f0d724c0` |
| 103 | Stabilization Audit Sprint 1 | `stable-stabilization-audit-sprint1-v1` | D | — | `97240223613bd8f9` |
| 104 | Critical Hardening Sprint 1 | `stable-critical-hardening-sprint1-v1` | D | — | `9277cec4187032e7` |
| 108 | UI Typography Refresh v1 | `stable-ui-typography-refresh-v1` | D | — | `bc6f703e23a971c5` |
| 114 | Ultimate Professional UX Pack | `stable-ultimate-professional-ux-pack-v1` | D | — | `1576fc7047d0bc74` |
| 115 | Auto Backup — Phase 1 | `stable-auto-backup-phase1-v1` | D | — | `e8993078abc3616a` |
| 116 | Page-Level Improvements — Phase 1 | *(merged)* | D | — | `5794e35688389423` |
| 119 | Inventory Module | *(stable)* | A | — | `700f7a0bc823767f` |
| 120 | Data Import | *(stable)* | A | — | `46775cd02495a352` |
| 121 | Audit Log | *(stable)* | A | — | `4bea208da4757473` |
| 122 | Reports & Export Center | *(stable)* | A | — | `a4362c44c45d8e6a` |
| 123 | Roles & Permissions (RBAC) | *(stable)* | A | — | `c8937dd55ec5dae7` |
| 124 | Backup & Restore | *(stable)* | A | — | `7fb64140d346f59a` |
| 125 | Authentication | *(stable)* | A | — | `95608f020a0ef38a` |

---

## Archived entries (verbatim)

| Feature | Stable Tag | Summary |
|---------|-----------|---------|
| **ExplorerKit Metrics Consistency & Filter UX Pack v1** | `stable-explorerkit-metrics-consistency-filter-ux-v1` | **Bank Account Explorer filter/total fixes + KPI card standardization for two modules** — presentation/UX + one filter bug fix; **no accounting-logic change, no DB schema change, no migration, no API breaking change, no print/template change.** **(1) Cheque filter corrected:** `buildTimelineWhere`'s `cheques` case now matches `chequeNumber != null` **OR** `bankFeeType === 'CHEQUE_PAYMENT'` (mirrors the `txTypeBadge` «شيك» rule), composed safely with date/amount/search — cheque-payment rows without a parsed cheque number are no longer missing. **(2) Dynamic filtered total:** `getTimeline` adds a second `prisma.bankStatementTransaction.aggregate({ where, _sum: { debit, credit } })` using the **same** `where` as count/findMany, returned as `TimelineResult.filteredTotal`; the Transaction Explorer shows it beside the result count as «{label}: {amount} KWD», computed from the **complete filtered dataset** (not just the paginated page) and respecting every active filter. Per-type labels (`TIMELINE_TOTAL_LABELS`/`timelineTotalLabel`): الكل / إيداعات / سحوبات / رسوم / شيكات / تحويلات. **(3) KPI standardization:** the shared ExplorerKit `MetricCard` (used by 16+ pages) was **not** touched; instead a new `frontend/src/components/KpiStat.tsx` + `KpiStat.css` replicate the `.bae-kpi-card` / `.bae-kpi-grid--secondary` baseline one-to-one (card `14px 16px`/radius `14px`, icon `30px`/`17px`, label `11.5px`, value `17px/800`, sub `10.5px`, `auto-fit minmax(160px,1fr)`), adopted **only** by Invoices (الفواتير والمطالبات) and Expenses (المصروفات والتشغيل). **Dashboard, Bank Account Explorer, and every other module are intentionally excluded.** **(4) Inline currency label:** additive `formatMoneyParts()` / `moneyParts()` split a monetary value into `{ number, currency }`, rendered as the amount + an inline `.kpistat-unit` (`white-space: nowrap`) → `17,097.620 KWD` on one line instead of a wrapped/stacked «KWD»; preserves the 3-decimal standard and the English/Arabic (`KWD`/`د.ك`) display setting. Dark mode (theme tokens) + RTL preserved. **Files: 17** (+348/−25): backend `bankStatementImport/{service,types}.ts` + `timelineFilters.test.ts`; frontend `api/bankStatementImport.ts`, `pages/{bankTimelineFilters.ts,BankAccountExplorer.tsx,BankAccountExplorer.css,Invoices.tsx,Expenses.tsx}`, `lib/format/{currency.ts,index.ts}`, `config/modules.tsx`, `components/{KpiStat.tsx,KpiStat.css}`, and 3 test files. **Validation:** prisma validate ✅ · backend/frontend/electron tsc ✅ · backend vitest **1394** ✅ · frontend vitest **829** ✅ (2 pre-existing unrelated `printWorkspace.test.tsx` CSS failures — non-blocking) · build:back ✅ · build:front ✅. **Reviews:** Manual Visual Review ✅ · Code Review ✅ · Security Review ✅ · **Gemini Review ✅**. Branched from `production` @ `1b9c31f`; feature commit `155ea56`, merge commit `fac8a84`, stable tag `stable-explorerkit-metrics-consistency-filter-ux-v1`. |
| **ExplorerKit Professional Modernization Pack v1** | `stable-explorerkit-professional-modernization-v1` | **Presentation/UX-only** refinement of four Explorer pages within the existing **ExplorerKit** design system — **no redesign, no ExplorerKit replacement, no component rewrites, frontend only**. **Scope: Expenses, Cheque Management, Accounting, Document Expiration Center** (Dashboard / Financial Center / Executive Financial Operations explicitly untouched). Preceded by an approved in-chat UI/UX audit; only LOW/MEDIUM-risk presentation findings implemented. **(1) ExplorerKit consistency — shared chevron styling:** the row-affordance chevron `.decx-col-chevron` (defined in one page's CSS yet consumed by ~13 pages — cross-page CSS coupling) promoted into the always-loaded kit `explorer-kit.css` as **`.xpl-col-chevron`** with a `.decx-col-chevron` **alias** so the ~7 out-of-scope pages render unchanged; the 4 in-scope pages migrated off the coupled class and their duplicated inline chevron styles removed. **(2) Tabular numeric alignment:** `font-variant-numeric: tabular-nums` applied to KPI/hero values (`.xpl-metric-value`/`.xpl-hero-value`/`.xpl-mono`) and the pack's amount/code/day columns so digits align. **(3) Expenses:** removed the duplicate total header chip (total already dominant in the hero); the **supplier filter migrated native `<select>` → `SearchableSelect`** (memoized options) to match the category filter. **(4) Cheque Management (non-print only):** KPI **scope labels** clarified to «هذه الصفحة» (page-scoped value aggregates vs server-total status counts); preview **action bar grouped** (primary issuance vs print tools) with clearer print-guidance copy. **(5) Accounting:** foreign `rcx-metrics` class → proper **`.accx-metrics`** with responsive breakpoint; tabular amounts. **(6) Document Expiration:** **ISO date leak fixed** (`expiryDate` raw `YYYY-MM-DD` → shared `formatDate` → `DD/MM/YYYY`) in table + drawer; urgency color hardcoded hex → **`--xpl-*` tokens** (theme-aware), deduped via the existing `urgencyTone` ladder. **Cheque printing logic untouched** — `ChequePrintOutput`, coordinates (`CHEQUE_PAGE_OFFSET`), `handlePrint`, `printCurrentView`, `fieldStyle`, print `@page`/`@media`, and `ChequeCalibrator` math all byte-unchanged (diff-verified). **NO backend/API/DB/business/accounting/permissions/printing/workflow changes; presentation-only.** **Files: 9** (frontend only) — `components/explorer/explorer-kit.css`, `pages/Expenses.tsx`+`.css`, `pages/Cheques.tsx`+`.css`, `pages/Accounting.tsx`+`.css`, `pages/DocumentExpirationCenter.tsx`+`.css`; +107/−56. **Reviews:** manual visual review both themes **APPROVED**, `/simplify` (3 fixes: deleted dead `.xpl-num` utility, deduped `urgencyVar` via `urgencyTone`, memoized `supplierOptions`; `<ChevronCell/>` component + shared metrics-grid deferred as cross-page scope-creep), `/code-review` (**clean — no correctness bugs**; supplier-filter behavior parity + `urgencyVar` color parity verified; CodeRabbit CLI not installed, ran local high-effort review), `/security-review` (**no findings** — presentation-only, no HTML sink / auth / network change), **Gemini Production Review: APPROVED ✅**. **Validation: frontend tsc ✅, build:front ✅, frontend vitest 802/804 ✅** (known baseline: **2 pre-existing unrelated `printWorkspace.test.tsx` CSS failures**). **Playwright-verified light + dark themes** across all 4 pages (localized dates, shared chevron served from the kit, scope labels, grouped action bar, `.accx-metrics` grid — no white-on-white / theme regressions). Checkpoint tag `checkpoint/pre-explorerkit-professional-modernization-v1`; branched from `production` @ `73e3714`; feature commit `dc8c377`, merge commit `d84972d`, stable tag `stable-explorerkit-professional-modernization-v1`. |
| **Dashboard Executive Polish Pack v1** | `stable-dashboard-executive-polish-v1` | **Presentation-only** refinement of the executive Dashboard within the existing bespoke dark **db-\*** design system — **no redesign, no ExplorerKit migration, no section moves, frontend only**. Preceded by a **Claude Fable 5 read-only audit** (7-page executive UI audit; this pack implements the Dashboard slice). **(1) Year-To-Date wording:** every remaining «آخر 6 أشهر / Last 6 months» label replaced with «منذ بداية العام / Year to Date» (i18n AR+EN `section.revenue_6m`, `CommandCenter`, `FinancialIntelPanel`, `ExecutiveIntelligenceV2Panel`) so the labels finally match the **YTD backend window** shipped in Executive Command Center Evolution v1 — **backend aggregation untouched**. **(2) Arabic month chart labels:** raw `YYYY-MM` axis codes (the backend `ytdMonths()` aggregation join-keys) now render as Arabic month «يناير» on axes and «يناير 2026» in tooltips across **all 4 dashboard charts** (RevenueChart, PerformanceChartSection, FinancialIntelPanel CollectionChart, ExecutiveIntelligenceV2Panel TrendChart). **Display-only** via two shared `lib/date.ts` helpers (`formatMonthShort`/`formatMonthLabel`, both defensive pass-through on non-`YYYY-MM` input, delegating to the existing `formatMonthYear`); the raw label stays the recharts `dataKey`/join key — **the data binding is unchanged; zero ISO-date leaks on screen** (Playwright-verified). **(3) Executive header polish:** CommandCenter titlebar gains a bottom divider + a leading accent bar; the top Executive-Summary strip (`.db-today`) becomes a proper theme-aware surface card. **(4) KPI presentation** harmonized via shared tokens — **no information removed**. **(5) Expiration Widget modernization:** rewritten off the generic `.card` shell + inline styles onto `db-*` classes with a data-driven `--chip` custom property consumed by `color-mix()` (**behavior identical** — same fetch, hide-when-empty, buckets). **(6) Chart visual polish:** theme-aware tick fills + gridlines/cursors visible in **both** themes — **fixed the `.db-chart-wrap` CSS override that kept the marquee RevenueChart gridlines invisible in light theme** (white `rgba(255,255,255,0.055)` → slate `rgba(148,163,184,0.18)`); ContractStatusChart tooltip retokenized (`#1a2535`/`#fff` → `var(--db-card)`/`var(--db-text)`). **(7) Accessibility:** the 5 operational-alert cards converted from keyboard-inaccessible `<div onClick>` to `<button type="button">` with `aria-label` + `aria-hidden` icons (**same navigate handlers**); tab-bar `:focus-visible` ring added. **(8) Theme consistency:** `.db-tab-bar` retokenized to global tokens — **fixed the dark-mode white strip** and removed the third-brand navy `#1d4e6f`; `.db-today` white-alpha surfaces → tokens. **(9) Dead CSS cleanup:** removed all confirmed-dead `.db-exec-*`, `.db-actions`, `.db-action-btn`, `.db-refresh-*` blocks (grep-verified **zero `.tsx` usage**) from `dashboard.css` + their light-theme overrides in `stitch-full.css`, plus a dead computed `today` variable. **(10) i18n cleanup:** extracted the tab labels («عام»/«مالي»), executive-summary trend chips, and the entire operations-pending block to i18n keys (AR+EN; all new keys verified present in **both** dictionaries). **Responsive:** uneven two-column rows collapse one step earlier (≤1200px) for tablet breathing room (no mobile redesign). **No DB schema change / no migration; no backend/API/business-logic/accounting/reports/permissions/data-flow changes.** **Files: 12** — `pages/Dashboard.tsx`, `lib/date.ts`, `lib/i18n.ts`, `components/dashboard/dashboard.css`, `styles/stitch-full.css`, `RevenueChart.tsx`, `ContractStatusChart.tsx`, `ExpirationWidget.tsx`, `FinancialIntelPanel.tsx`, `ExecutiveIntelligenceV2Panel.tsx`, `command/CommandCenter.tsx`, `command/PerformanceChartSection.tsx`; **+287 / −510 (net −223, mostly dead CSS)**. **Reviews:** Claude Fable 5 audit ✅, **manual visual review (both themes) APPROVED** ✅, `/simplify` (2 fixes: the RevenueChart gridline-override bug + `formatMonthLabel`/`formatMonthShort` helper consolidation) ✅, `/code-review` (1 cosmetic fix: `ops.pending.invoice_unit` for English unit-tag consistency; correctness/theme/i18n all verified) ✅, `/security-review` (**no findings** — UI-only diff, no HTML-sink/auth/network changes) ✅, **Gemini production review: APPROVED ✅** (no Critical, no Warnings). **Validation: frontend tsc ✅, frontend vitest 802 pass ✅ (2 pre-existing unrelated `printWorkspace.test.tsx` CSS failures), build:front ✅; Playwright-verified light + dark themes** (Arabic-month axes, no ISO leaks, tab-bar dark surface, RevenueChart gridline visible). **Backend untouched** (`date.ts`/`i18n.ts` are frontend-only, not imported by backend). Checkpoint tag `checkpoint/pre-dashboard-executive-polish-v1`; branched from `production` @ `ac81be4`; feature commit `9adab3e`, merge commit `ff4bada`, stable tag `stable-dashboard-executive-polish-v1`. **Deferred (out of scope, documented):** the other 6 pages from the Fable 5 executive UI audit (Financial Center ISO dates + raw enums, Executive Financial Operations `--db-*` token scope bug, currency-suffix unification, debounced searches) remain for future packs. |
| **Unified Excel Export Professional Standard v1** | `stable-unified-excel-export-professional-standard-v1` | **Standardizes every `.xlsx` export** on one professional backend ExcelJS engine so files are ready-to-use without manual formatting. Preceded by a **Claude Fable 5 audit** (backend + frontend export inventory). Implemented by **Claude Sonnet** across 5 phases. **Shared Professional Excel Engine (additive, backward-compatible — all ~25 existing exports upgraded with zero caller changes):** new **Professional Excel Design System** `reportEngine/excelStyle.ts` (Tahoma font, navy `1D4E6F` header, zebra, borders, `KWD_FORMAT '#,##0.000'` unchanged, date/int formats, **reusable frozen style constants** — no per-cell allocation); `excel.service.ts` `buildExcel` (single sheet) + `buildExcelWorkbook` (**multi-sheet**) share `renderSheet`; **shared styling + shared workbook metadata** (`creator/company='manarERP'`, title/subject/category/created) applied by default along with **RTL**, **Freeze Pane**, **AutoFilter**, auto column width (honors explicit widths), **Zebra rows**, **workbook print settings** (fit-to-width, repeat header row, margins, centered), and **professional number/date formatting** (real numeric money cells + real Date cells). New optional `ReportInput` fields: `zebra`/`autoFilter`/`sheetName`/`metaFooter`/`rowStyle` (conditional row fill) + `ReportColumn.type`/`align`. New **shared MIME/response helper** `core/utils/excelResponse.ts` (`EXCEL_MIME` + `sendExcel` with **RFC5987 Arabic filenames**) and shared `core/utils/arabicMonths.ts` (`ARABIC_MONTHS`, deduped 3 divergent copies). **4 bespoke exports migrated** onto the engine (signatures/sheets/data preserved): bank reconciliation (3 sheets), payroll bank import (2 sheets, **light green/red status row fill** + AutoFilter), expirations (Arabic RTL headers, fixing the English-headers/no-RTL defect), salary bank analytics (2 sheets). **Backend export routing/parity:** reports **invoices** report gained additive columns (`invoiceType`, `issueDate` date-cell, `status` via shared `translateInvoiceStatusAr`, `notes`) + filters (**search mirroring the list**, `billingMonth`, `billingYear`); **expenses** report gained `notes` + filters; **no columns removed**. **New monthly invoice export endpoint** `GET /invoices/monthly-report/export` (guarded `invoices.read`; columns identical to the modal; `ReportInput` built in the service layer). `sendExcel` adopted across the migrated modules' send sites. **Frontend SheetJS retirement (where applicable):** Expenses / Invoices list / monthly-modal exports now download the backend professional file (filters + canonical filenames preserved; gated on `reports.export` / `invoices.read`); raw client-side `.xlsx` generation removed (`downloadXlsx` deleted; `xlsx` kept for import parsing); **6 duplicated blob-savers consolidated** into the shared `downloadBlob`. **Security improvement:** `DocumentExpirationCenter` raw-`fetch` + `localStorage`-token bypass replaced with the centralized axios `api` client. **No DB schema change / no migration; no business-logic/calculation changes** — only additive report columns/filters + two intentional consistency fixes (invoice **status wording** `غير مسدد`→`غير مسددة` and Arabic **month spelling** `إبريل`→`أبريل`, now matching the rest of the backend); `exportFilename.ts` and CSV exports untouched. **Files: 26** (4 new: `excelStyle.ts`, `excelResponse.ts`, `arabicMonths.ts`, engine test; 22 modified). **Reviews:** Claude Fable 5 audit ✅, `/simplify` (font-constant/dead-code/shared-helper fixes) ✅, `/code-review` (2 warnings fixed: invoices export search parity + summary-sheet number formatting) ✅, `/security-review` (**no findings**) ✅, **manual Excel visual review completed** (20 representative files) ✅, **Gemini production review: APPROVED ✅**. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, backend vitest 1372/1372 ✅, frontend vitest 802 pass ✅ (2 pre-existing unrelated `printWorkspace.test.tsx` CSS failures), build:back ✅, build:front ✅.** Checkpoint tag `checkpoint/pre-excel-standard-10af0c0`; branched from `production` @ `10af0c0`; feature commit `5ea1fd9`, merge commit `80d22cb`, stable tag `stable-unified-excel-export-professional-standard-v1`. **Deferred (documented):** `sendExcel` not adopted in the untouched `financial`/`statements`/`bankStatementImport-service` send sites; two CSV savers left per the "don't change CSV" rule; expirations category/urgency values still raw enum codes (future value-mapping polish). |
| **Executive Command Center Evolution v1** | `stable-executive-command-center-evolution-v1` | **Refinement pack** for the Executive Command Center (the bespoke dark `db-*` dashboard, NOT ExplorerKit — confirmed with the user to **fix within the existing `db-*` system**, no ExplorerKit migration / no redesign). **Full audit first** (3 parallel agents: frontend structure, backend data layer, contrast/empty-states). **Part 2 — data-integrity bugs fixed (root-caused, not guessed):** (A) **«January missing»** — all fixed monthly chart generators used a 6-month **trailing** window anchored to the current month (July ⇒ Feb…Jul); replaced globally with a shared **Year-To-Date** helper `core/utils/dateWindows.ts::ytdMonths()` (Jan→current month) applied to `monthlyTrend`, `executive.trend`, `executiveFinancialV2.collectionTrend`, `executiveIntelligenceV2.monthlyTrends`; also fixed the `sixMonthsAgo` fetch/bucket mismatch that fetched January then discarded it (now `yearStart`). **Never special-cases January.** (B) **Forecast = 0** — was a logic bug: only *future-due* receivables counted, so an all-overdue AR book yielded zero. Now **overdue outstanding is counted in the ≤30-day expected bucket** (approved standard cash-forecast assumption; not fabricated data). (C) **Revenue Distribution empty** — was built only from ACTIVE contract-linked SALES invoices; now aggregates **all non-cancelled SALES invoices grouped by customer** (`executive.service.decisionCenter.topCustomersByRevenue`), so real customer revenue shows regardless of contract linkage. **Part 1 — layout** reordered by business priority: the enhanced **Executive Summary leads** (moved above the KPI/charts hub); the composed **Command Center** (KPIs/health/actions/charts) relocated lower with the analytics panels — **all widgets preserved, no component redesign**. **Part 4 — Executive Summary** enriched with financial-glance chips (net-profit-this-month status + revenue/collections/expenses MoM trend) from data already in the decision-center payload — **existing data only, shown only when a real value exists (no invented figures)**. **Part 3 — contrast (root-caused via running-app inspection, both themes):** the dashboard is **theme-aware** — `styles/stitch-full.css` remaps `--db-*` to the app's `--surface`/`--text` tokens, so cards turn **white in light theme**. The real bug: **hardcoded colors that bypass the tokens** — `#F9FAFB` (white) inline styles stayed white on the now-white cards → **white-on-white/invisible text** across the panels (Executive Alerts, Smart Recommendations, Contract Profitability, Debt Aging, Forecast, Revenue Distribution). Fix: converted all hardcoded card text to theme-aware tokens (`#F9FAFB`→`var(--db-text)`, muted→`var(--db-muted)`, tone hex→`var(--db-red/amber/green/blue)`); made chart **tooltips** theme-aware (`rgba(15,23,40,.94)`→`var(--db-card)`); added a **theme-aware Recharts tick** CSS rule (SVG `fill` via CSS property resolves `var()`); added **light-theme-only** darker semantic tones (`html:not([data-theme="dark"])` scope) so amber/green/red text is AA-readable on white while **dark theme keeps its bright tones**; the light-surface «data-source note» island → `db-*` tokens. **Verified in the running app (Playwright, computed-style scan): light-theme AA failures 45→4 with ZERO invisible text (was many at 1.0 contrast); dark theme unchanged (no regressions, screenshot-confirmed).** **Part 9 — empty states:** the Financial Forecast, KPI-comparison, and debt-aging card bodies no longer `return null` (blank) — each now shows a distinct state (calculation-unavailable vs no-data). **Files: 13** (1 new) — backend: `core/utils/dateWindows.ts` (new), `dashboard.service.ts`, `executive.service.ts`, + 2 updated tests (`dashboard.executiveIntelligenceV2/FinancialV2` — assert YTD length + overdue-in-30 rule); frontend: `pages/Dashboard.tsx`, `dashboard.css`, `ExecutiveIntelligenceV2Panel.tsx`, `FinancialIntelPanel.tsx`, `ContractStatusChart.tsx`, `RevenueChart.tsx`, `ExpirationWidget.tsx`, `command/PerformanceChartSection.tsx`. **No DB schema change / no migration.** **Deferred (documented, not done — would be redesign/regression risk):** Part 7 deep endpoint/query consolidation (7 overlapping dashboard endpoints, triplicated MoM aggregates) and Part 8 full ExplorerKit migration. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, backend vitest 1356/1356 ✅, frontend vitest 802 pass ✅ (2 pre-existing unrelated `printWorkspace.test.tsx` CSS failures), build:back ✅, build:front ✅.** **Gemini production review: APPROVED ✅** (no blockers). Checkpoint tag `checkpoint/pre-dashboard-evolution-b019129`; branched from `production` @ `b019129`; feature commit `bec3330`, merge commit `11837d7`, stable tag `stable-executive-command-center-evolution-v1`. **Playwright + manual visual verification completed** (both themes). **Files: 21** (1 new `core/utils/dateWindows.ts` + 20 modified). **Deferred (documented):** Part 7 deep endpoint/query consolidation, Part 8 full ExplorerKit migration. |
| **Presentation Settings Compliance Pack v1** | `stable-presentation-settings-compliance-v1` | **Presentation-only** pack (frontend-only; **zero backend code changes**): a date compliance guard + a company Currency Display Language setting. **(1) Date compliance guard:** audited the recent/high-impact UI surfaces (Expenses, Invoices, Invoice/Expense Fast Entry, ExplorerKit drawers/hubs, Reports, exports, tables, dialogs, session summaries) against Global Date Standardization v1 (DD/MM/YYYY, English digits, shared formatter). Found & fixed **2 remaining user-facing display violations**: the Invoices **Excel export** `تاريخ الإصدار` cell (was raw `YYYY-MM-DD` via `slice` → `dateText`) and the **cheque force-delete modal** date cells (→ shared `formatDate`). Everything else was already compliant or correctly excluded: every `input[type=date]` value (incl. `Cheques.fmtDate`, which feeds the edit-form input — must stay ISO), month-year period labels, AI-skill content, print templates, decorative greeting, and `.txt` exports (documented Global Date Standard v1 exclusions). **(2) Currency Display Language setting:** new company setting **`finance.currencyDisplayLanguage`** (`english` | `arabic`, default **english**) in the existing **free-form `Setting` key/value table → no migration**, additive, backward-compatible. **Deterministic formatter (no hidden state):** `lib/format/currencyLanguage.ts` is a pure leaf (type + `normalizeCurrencyLanguage`); **`formatCurrency(value, { language })` is a PURE function** — English `1,250.000 KWD` (safe default) / Arabic `١٬٢٥٠٫٠٠٠ د.ك` (Arabic-Indic digits + `٬`/`٫` separators, exactly 3 decimals, `د.ك`); **no module-level mutable state / no singleton** (resolved after an architecture-review clarification — Option A). **Single source of truth** = a lightweight `settingsStore` (Zustand), loaded post-auth in `Layout` (which subscribes so amounts re-render on change) and applied immediately on Settings save; the **app-level entry points** (`money()` in `config/modules`, and the Reports table cells) resolve the language via `currentCurrencyLanguage()` and pass it **explicitly** into the formatter — so every `money()`-based surface (Dashboard, Expenses, Invoices, Reports, ExplorerKit hubs/drawers, Fast Entry summaries) follows the setting automatically. Company Settings gains a **«لغة عرض العملة»** select with live-example options (+ ar/en i18n). **Print/PDF currency remains English (`ReportPrint` defaults English).** **NO accounting logic, calculations, DB monetary values, invoice/expense totals, GL posting, reports logic, tax, payment, API payload, or `input[type=date]` changes — display/presentation only; existing users default to English.** **Files: 13** (10 modified + 3 new) — new `lib/format/currencyLanguage.ts`, `stores/settingsStore.ts` (+`__tests__`); modified `lib/format/{currency.ts,index.ts,__tests__/currency.test.ts}`, `config/modules.tsx` (`money` resolver), `pages/Reports.tsx` (report-cell resolver), `components/Layout.tsx` (load + subscribe), `pages/Settings.tsx` (select + apply-on-save), `lib/i18n.ts`, `pages/Invoices.tsx` (date), `components/ForceDeleteChequeModal.tsx` (date). Checkpoint tag `checkpoint/presentation-settings-v1-61e482c`; branched from `production` @ `61e482c`; 4 focused commits (`e9b0cc1` fix(dates) · `14c4d8f` refactor(format) deterministic · `f431894` feat(settings) · `fa62c5e` test(format)), merge commit `104f6f4`. **Validation: frontend tsc ✅, backend tsc ✅ (no backend changes), frontend vitest 802 pass ✅ (currency 37 incl. deterministic/english/arabic/report-cell + settings-store 4), builds ✅** (2 pre-existing `printWorkspace.test.tsx` CSS failures — out of scope). Reviews: architecture-review clarification → **Option A** (pure formatter, store single source, explicit resolution at entry points — no hidden mutable singleton); self + security assessment (no new endpoints; setting written via existing `PUT /settings`; value normalized). **Gemini production review: APPROVED ✅.** **Deferred (explicitly out of scope):** print currency path (`formatKWDAr`), `ReportPrint` localization, backend PDF currency formatting, `Cheques.tsx` currency literals, `tafqeet` amount-in-words, and remaining print/template monetary formatting. |
| **ExplorerKit Information Hub — Phase 1** | `stable-explorerkit-information-hub-phase1-v1` | **Frontend presentation/UX-only** upgrade turning the ExplorerKit detail drawer into a reusable **Information Hub**, realized on the three approved reference entities (Customer, Equipment, Invoice). **Six additive, presentation-only primitives** added to the shared kit (`components/explorer/ExplorerKit.tsx` + `explorer-kit.css`, existing `--xpl-*` tokens only, no existing export/selector modified): `DrawerHeaderCard` (icon + title + status chip + 2–4 KPI tiles), `DrawerQuickActions` (icon+label action grid, renders nothing when empty), `DrawerInfoGrid` (2-col grouped key/value, **drops empty values, hides when all empty**), `DrawerRelated` (lazy list with skeleton/error/**hidden-when-empty**), `DrawerActivity` (timeline, **hidden when empty**), `DrawerActionBar` (primary → secondary → danger footer). `Tone` promoted to `export type` (non-breaking). New `components/explorer/hubs/`: `hubTypes.ts` (`EntityHubProps`, `HubComponent`, `buildInfoItems` with `omitKeys`), `CustomerHub.tsx`, `EquipmentHub.tsx` — per-entity hubs that **lazy-load related/activity from EXISTING endpoints only** (`/invoices/stats`, `/statements/customers/:id`, `/invoices?customerId=`, `/contracts?customerId=`, `/maintenance/records|fuel?equipmentId=`), `alive`-guarded + `Promise.allSettled`, hidden on empty/failure. `pages/ResourcePage.tsx` gained a tiny `DRAWER_HUBS` registry (customers, equipment) — **every other module + the employees Tabs/EmployeeFinancialTab branch + permissions + footer are byte-unchanged** (flat-section fallback). `pages/Invoices.tsx` drawer **recomposed** onto the primitives (header Total/Paid/Remaining/Age KPIs, quick actions, payments related, synthesized issued→payments→cancelled activity, action-bar footer) with **all existing handlers/gates/data reused verbatim** (`setPaying`/`canCollectRow`, print nav, `setEditing`/`canEditRow`, `cancel`/`canCancelRow`, `setForceDeleteId`/`isSystemAdmin`). **Honest omissions (verified absent, hidden — no fabricated data):** Customer "Last Update"/Receipt-Payment-Voucher; Equipment Purchase-Date/Transfer/km/Print-card; Invoice Email-PDF/Duplicate/Contract-PO. **NO backend, database, API, new endpoints, routing, permission, validation, calculation, workflow, or deep-link/pre-fill changes.** **Files: 9** (all frontend) — new `hubs/{hubTypes.ts,CustomerHub.tsx,EquipmentHub.tsx}` + 2 test files, modified `ExplorerKit.tsx`, `explorer-kit.css`, `ResourcePage.tsx`, `Invoices.tsx`; +1030/−35. **18 new hub/primitive tests.** Checkpoint tag `checkpoint/pre-explorer-hub-65a3612`; branched from `production` @ `65a3612`; 7 feature commits (`eb7f7e1`…`c050ed1`), merge commit `d5e7331`. **Validation: frontend tsc ✅, electron tsc ✅, full frontend vitest 757/759 (2 pre-existing `printWorkspace.test.tsx` failures — out of scope), vite build ✅.** Reviews: per-task subagent spec+quality reviews (caught & fixed a global-jsdom vite.config misstep → per-file pragma; verified API envelopes against real callers; confirmed backend list payload lacks invoice items/payments so those sections hide — pre-existing, not a regression), final whole-branch review (Opus) **no Critical/Important; Ready-to-merge with fixes** → fix wave `c050ed1` (reduced-motion for quick actions [binding], `buildInfoItems` omit duplicate header field, gate PDF action like Print, disable delete while busy). **Gemini production review: APPROVED ✅.** **Deferred (explicit):** Invoice Drawer Data Enrichment (optional lazy `GET /invoices/:id` to populate payments/activity), global Explorer rollout to other pages, toolbar/table/hero global modernization, Bank Explorer migration, new endpoints, workflow enhancements. |
| **Universal Export File Naming Standard v1** | `stable-universal-export-file-naming-v1` | **Frontend-only, filenames-only** standardization of every user-facing export onto one official format — **`manarERP_<ReportName>_<Identifier>_<YYYY-MM-DD>.<extension>`** — generated by a single **source-of-truth** utility. **New `frontend/src/utils/exportFilename.ts`:** `generateExportFileName({reportName, identifier?, date?, extension})` + `sanitizeFilenameSegment` + a canonical PascalCase **`ReportName`** vocabulary + a **`resourceReportName(moduleKey)`** resolver (maps the 7 generic data modules — contracts/customers/suppliers/equipment/employees/expenses/users — to canonical names, with a PascalCase fallback so a future module is never left un-standardized). Fixed prefix `manarERP`; **identifier segment omitted entirely when absent** (no blank `__`); illegal chars `/ \ : * ? " < > |` + control chars removed, whitespace→`_`, repeated `_` collapsed, leading/trailing `_ - .` trimmed; **Arabic preserved verbatim, never translated**; extension passed without a leading dot (stray dot stripped + lowercased). **Date:** added **`formatFileDate`** to the shared `lib/date.ts` → **local** `YYYY-MM-DD` (fixes a latent UTC-rollover bug in the old `toISOString().slice(0,10)` calls). **`utils/pdfFilename.ts`** reduced to a thin **delegating wrapper** (existing callers keep working, output now standardized). **Electron `pdf.ipc.ts`** `.pdf` append made **idempotent** in both handlers so the renderer can pass a full standardized `.pdf` name without `name.pdf.pdf`. **Backend untouched** (its `Content-Disposition` is invisible to the user — the frontend re-derives every name). **~25 export sites migrated** across PDF/Excel/CSV, blob/anchor downloads, and the Electron save-dialog path: FinancialCenter (statement + AR/AP aging + GL statement + GL report + trial balance + journal book — **PDF and Excel**), FinancialReportsTab, `api/statements.ts` (customer/supplier), `api/bankStatementImport.ts`, BankSalaryAnalytics, BankReconciliation (timeline + batch CSV), BankAccountExplorer, DocumentExpirationCenter, ExecutiveDecisionCenter, ReportPrint, Reports (PDF + Excel), **ResourcePage generic CRUD export (all data modules)**, Expenses, Invoices (list + monthly report), Prices, Salaries, PayrollBankImport (Excel + PDF). **A coverage grep gate confirms zero in-scope ad-hoc export filenames remain.** **Intentional exclusions (unchanged):** DB backups (`manar-backup-*.db`), blank import templates, print-template/layout-designer/cheque-calibration `.json` exports, AI-result `.txt` dumps. **No report logic, file content, report design, print layouts, params, endpoints, blob MIME types, sheet names, or column changes.** **Files: 24** (23 in merge + ResourcePage resolver) — new `utils/exportFilename.ts` (+ test), `lib/date.ts` `formatFileDate` (+ test), `utils/pdfFilename.ts` wrapper, `electron/ipc/pdf.ipc.ts`, and the migrated pages/api; +436/−80 (merge) plus the canonical-name resolver commit. Branched from `production` @ `b8d552a`; checkpoint tag `checkpoint/pre-export-naming-b8d552a`. 13 feature commits (`042091f`…`b7a619a`), merge commit `6526bde`. **Validation: frontend tsc ✅, electron tsc ✅, full frontend vitest 739/741 (2 pre-existing `printWorkspace.test.tsx` failures on untouched `PrintWorkspace.css` — out of scope; +new exportFilename/date/resourceReportName tests), vite build ✅.** Reviews: subagent-driven per-task spec+quality reviews (caught & fixed multiple plan-inventory gaps — 5 FinancialCenter PDF siblings, Reports.tsx Excel, BankReconciliation batch CSV, and the recon-missed ResourcePage generic export + Expenses/Invoices/Prices/Salaries/PayrollBankImport), final whole-branch review (Opus) **Ready to merge = YES**. **Gemini production review: APPROVED ✅.** **Deferred (non-blocking):** dead `today`/`from`/`to` locals in `BankReconciliation.exportTimelineCsv` left for a future `/simplify` pass (no behavior impact). |
| **Global Date Presentation Standardization v1** | `stable-global-date-presentation-standardization-v1` | **Frontend presentation-only** standardization of all user-visible **calendar dates** onto the project's existing shared formatter — **`lib/date.ts` `formatDate` → `DD/MM/YYYY`, English digits** (e.g. `01/07/2026`) — replacing ad-hoc `toLocaleDateString('ar-KW')` calls that rendered Arabic-Indic digits (e.g. `٢٠٢٦/٧/١`). **Audit finding:** the codebase already had a canonical shared formatter (`formatDate`) plus a central re-export `dateText` (= `formatDate`) in `config/modules.tsx` already used by every ResourcePage table, Accounting, Employees, Expenses, Contracts, and the dashboard invoice/expense/financial tables — those were **already standardized**; the gap was ~14 ad-hoc pages/components that bypassed it with page-local helpers. **Changes:** delegated page-local `fmtDate` helpers to shared `formatDate` (Bank Account Explorer, Bank Accounts, Bank Statement Import, Bank Reconciliation, Bank Salary Analytics, Bank Import, Payroll Bank Import, Contract Financial Summary); **removed** two name-colliding local helpers in favor of the shared import (`DocumentVerify.formatDate`, `PrintLogPanel.formatDateTime`); replaced inline `toLocaleDateString('ar-KW')` with `formatDate`/`dateText` (AttachmentsPanel, RecentActivityFeed, Integrations ×2, Invoices); migrated one date-time stamp to shared `formatDateTime` (PayrollBankImport import-success line). **The shared formatter itself was not modified.** **Intentional exclusions (unchanged, rule/decision-based):** month-year period labels (no day), decorative weekday greeting + AI compact chat labels, print/PDF templates (`printI18n`, `formStyles` — already Latin/`en-US`), `.txt` exports (AI conversation/result), AI-skill content, standalone times (`toLocaleTimeString`), internal/diagnostic timestamps, and number `toLocaleString('ar')` (separate concern). **No backend, API, Prisma, database, business-logic, sorting/filtering, or import/export/report/PDF-data changes; `currency.ts` untouched.** **Files: 14** — all frontend `pages/`+`components/`; +28/−43 (net −15). Feature commit `2b35e29`, merge commit `045ae89`. Branched from `production` @ `68542fb`. **Validation: frontend tsc ✅, full frontend vitest 716/718 (2 pre-existing `printWorkspace.test.tsx` failures on untouched `PrintWorkspace.css` — out of scope), vite build ✅.** Decisions (via interactive confirmation): canonical **DD/MM/YYYY** (kept the existing 2-digit shared standard, not `1/7/2026`) and **plain dates only** (times & Arabic month labels left as-is). **Gemini production review: APPROVED ✅.** |
| **Global Monetary Formatting Production Promotion v1** | `stable-global-monetary-formatting-production-v1` | **Promoted the completed Global Monetary Formatting Standardization Pack to production** (previously developed on feature branches and **never merged** — see the 2026-07-05 monetary-formatting audit). Ports the pack's final state (source commits `f6bf86f` + `ee804f1` + `d92fb91`), **excluding** Google Drive backup and all unrelated history. **Added shared currency formatters:** `frontend/src/lib/format/*` (`formatCurrency`, `formatNumber`, `formatInteger`, `formatPercent`, `formatReportCell`, `currencyConfig`) and `backend/src/shared/utils/currency.ts` + `backend/src/shared/config/currencyConfig.ts` (with unit tests both sides). **Standardized money display** to **en-US digits, thousands separators, exactly 3 decimals, `KWD` suffix** where currency is shown; removed Arabic-Indic digits / `د.ك` for pack-covered surfaces (e.g. `reports.service` now has 0 `toLocaleString('ar')` refs). **Migrated call sites** across UI pages, reports, dashboards & charts, financial components (aging, statements, journal, trial balance, balance display), the PDF/report engine (`table.template`, `htmlUtils.fmtCell` two-arg currency, `pdf.service`, `excel.service`), bank analytics/explorer/reconciliation, forms (payment/receipt vouchers, quotation), print templates, and AI skills. **Reconciliation:** the 3 files also touched by the shipped print refresh (`ReportPrint.tsx`, `reportEngine/table.template.ts`, `reportEngine.test.ts`) take the merged state from `9463a87` (the refresh authored on top of the pack) — so **Invoice Report Print Refresh v1 typography is preserved together with pack formatting**. **Bank Account Explorer Default Sort Fix v1 preserved** (bank-timeline `service.ts` not a pack file; `timelineFilters` 9/9 still green). **No business logic, calculation, API, database, Prisma, import, reconciliation, or balance changes.** **Files: 74** — 73 pack files + the now-tracked `backend/src/shared/utils/__tests__/currency.test.ts`; +1430/−317. Ported commit `6b7eb54`, merge commit `b891e49`. Branched from `production` @ `87b0811`. **Validation: backend tsc ✅, frontend tsc ✅, backend vitest 1309/1309 ✅, frontend vitest 694/696 (2 pre-existing `printWorkspace.test.tsx` failures assert untouched `PrintWorkspace.css` — out of scope).** **Gemini review: PASS ✅.** |
| **Smart Import Assistant — Phase 2 (2A + 2B)** | `stable-smart-import-assistant-phase2-v1` | Builds on Phase 1 with **header intelligence, auto-mapping, quality score, more cross-field warnings, in-file smart-duplicate detection, import analytics, and saved mapping profiles** — all preview-only, non-blocking, additive. **Zero Backend endpoint / Prisma / migration / schema / permission changes; no AI/cloud (offline-first, deterministic).** **2A:** (A) **Header Intelligence** — deterministic near-miss detection (Arabic/English normalization + alias dictionary + Levenshtein) in new `frontend/src/utils/headerIntelligence.ts`; (B) **Auto Mapping Assistant** — a review step that appears **only when headers aren't a perfect match** (perfectly-matched files skip it, no friction): Excel column → dropdown target field → confidence → «تجاهل العمود»; mapping applied **client-side** (backend contract unchanged); (C) **Import Quality Score** 0–100 («جودة الملف») informational; (D) cross-field warnings — employees (future birthDate, age<16, residency<passport, salary 0/high), equipment (future manufactureYear, purchaseDate<manufactureYear), invoices (issueDate far-future), contracts (endDate expired, price missing), expenses (**new** high amount, far-off date). **2B:** (E) **Smart Duplicate Detection** — warning-level, **in-file only** (no DB queries → no N+1; DB secondary-dup deferred): employees civilId/phone/email/passport, customers/suppliers phone/email, equipment plate, contracts composite customer+date+price, invoices composite customer+date+total; (F) **Import Analytics** (top warning codes, top error reasons, most-affected fields + quality) in preview; (G) **Saved Mapping Profiles** — **localStorage only, no backend sync** (new `frontend/src/utils/mappingProfiles.ts`), keyed by entity + normalized header signature, stores **only headers + mapping (no row data)**, save/apply/delete + «ربط محفوظ» prompt. Backend adds isolated `warnings/quality.ts` + extends `entityWarnings.ts`/`index.ts`; `import.types.ts` extended additively (`ImportAnalytics`, optional `qualityScore`/`analytics`); preview computes them, **execute byte-identical** (re-validates server-side). **Architectural isolation enforced & verified: Bank Statement Import (`bankStatementImport/*`, reconciliation, Unified Bank Account Timeline) entirely untouched; warnings import only `config/constants` + own `import.types`.** **Files: 12** — 6 modified (`import.service.ts`, `import.types.ts`, `warnings/entityWarnings.ts`, `warnings/index.ts`, `DataImport.tsx`, `DataImport.css`) + 6 new (`warnings/quality.ts`, `warnings/__tests__/phase2.test.ts`, `utils/headerIntelligence.ts`, `utils/mappingProfiles.ts`, 2 util tests); +1003/−29. Feature commit `4b4a656`, merge commit `4116105`. Checkpoint tag `pre-smart-import-assistant-phase2` at `bc55adc`. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, backend vitest 1262/1262 (69 files, +21 Phase-2, bankStatementImport unaffected), frontend vitest 663/665 (+14 util tests), build:back ✅, build:front ✅.** Security review: **no findings** (additive, preview-only; client utils store only headers+mapping; execute re-validates server-side; React auto-escapes). Live UI verification ✅ (mapping step with 83% near-miss suggestion + alias + unknown; quality 85/100; analytics; in-file phone DUP; all rows stayed `valid`). **Known unrelated issue:** 2 pre-existing `printWorkspace.test.tsx` failures (assert untouched `PrintWorkspace.css`) — out of scope. |
| **Smart Import Validation — Phase 1** | `stable-smart-import-validation-phase1-v1` | **Additive, non-blocking advisory-warnings layer** for the generic Data Import module (`/api/import`). Surfaces "valid but suspicious" rows during **preview only**; **warnings never change a row's status (valid/invalid/duplicate) and never block import**. **Zero Backend endpoint / Prisma / migration / financial-calculation / permission changes.** New isolated subsystem `backend/src/modules/import/warnings/` (`helpers.ts`, `entityWarnings.ts`, `index.ts`) — pure functions imported only into `previewImport` via an opt-in `computeWarnings` flag on `buildPreviewRows`; **execute path byte-identical** (flag defaults off, still re-validates + backs up + audits as before). Types extended additively in `import.types.ts` (`ImportWarning`, optional `RowResult.warnings`, `PreviewSummary.warningRows`/`warningsByCode`). **Rules:** employees (IDENTICAL_DATES, DATE_EXPIRED, DATE_EXPIRING_SOON ≤90d, ENUM_DEFAULTED, MISSING_IMPORTANT_OPTIONAL), equipment (registration expiry + DUP_SECONDARY_KEY plate), invoices (dueDate<issueDate, total==0), contracts (price==0, endDate ≤90d), payroll (net<0, baseSalary out-of-range), all-entities ROW_IDENTICAL. Each warning carries `code`/`severity`(info·warning·danger)/`field`/`messageAr`/`messageEn`/`suggestedFix`. **Frontend `DataImport.tsx`:** warning summary card, filter chips (الكل/صالحة/تحذيرات/أخطاء/مكرر), per-row warning badges/column, and a **confirm-before-execute** banner when warnings exist — **`canImport` gating unchanged**. **Architectural isolation enforced & verified:** the warnings subsystem imports only `config/constants` + own `import.types`; **Bank Statement Import (`modules/bankStatementImport/*`, reconciliation, Unified Bank Account Timeline) is entirely untouched and shares no code.** **Files: 8** — `import.service.ts`, `import.types.ts`, 4 new under `warnings/` (incl. `warnings.test.ts`, 16 tests), `DataImport.tsx`, `DataImport.css`; +697/−18. Feature commit `2fc8b69`, merge commit `f50b776`. Checkpoint tag `pre-smart-import-validation-phase1` at `b2a29d4`. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, backend vitest 1241/1241 (68 files, incl. 16 new + all bankStatementImport unaffected), build:back ✅, build:front ✅.** Security review: **no findings** (additive, preview-only, no new attack surface). Live UI verification ✅ (warnings rendered per severity; confirm-before-execute shown; all rows stayed `valid`). **Known unrelated issue:** 2 pre-existing `printWorkspace.test.tsx` failures (assert untouched `PrintWorkspace.css`) — out of scope, not introduced here. |
| **Executive Command Center Dashboard Redesign** | `stable-executive-command-center-dashboard-v1` | **Frontend-only** redesign of the General dashboard tab (`GeneralDashboardContent`) into a content-driven **Executive Command Center**. **Zero Backend / Prisma / APIs / financial-calculations / permissions changes; real data only** (existing endpoints `/dashboard/executive`, `/dashboard/operational`, `/executive/decision-center`, `/dashboard/activity` — no new endpoints created). New `command/` folder of independent, registry-driven sections composed by `CommandCenter`: title bar (with the relocated refresh control) · Health Gauge (real `healthScore` + rule-based reasons/top-risk from real components) · KPI row (5 cards incl. derived net-cash = thisMonth collections − expenses; trend badges only when a real month-on-month change exists — no invented growth) · combo performance chart (revenue/expense bars + derived profit line, Recharts) · revenue-by-customer donut (Top 5 + أخرى) · Action Center (decisionCards) · Quick Actions (existing routes + permission-gated) · Recent Activity (`/dashboard/activity`) · Smart Recommendations (rule-based `recommendations`). **All existing rich panels retained** below the Command Center (daily summary, data-source note, ExpirationWidget, operational alerts, OpsCards, tables, V2 panels); Financial tab untouched. Phase H removed the now-duplicated old Hero + old KPI grid + old quick actions (each fully covered by the Command Center), moving the manual refresh into the title bar — no unique data/function lost (mapping table in the plan). a11y: `<h1>`/`<h3>` hierarchy, `aria-label`s, `:focus-visible`, `prefers-reduced-motion`. RTL + light/dark via existing `--db-*` tokens; responsive verified at 1920/1600/1366 (no horizontal overflow). **Files: 17** — 3 modified (`Dashboard.tsx`, `dashboard.css`, `KPICard.tsx`) + 14 new under `command/` (incl. `commandData.test.ts`, 8 unit tests); +2432/−175 (with docs). Feature commit `bbfcf3f`, merge commit `15d33a7`. Executed A→H phase-by-phase with a screenshot + gate review between phases. **Quality gates: frontend tsc ✅, electron tsc ✅, command vitest 8/8 ✅, build:front ✅ — all green.** **Gemini review: APPROVE ✅.** **Known note:** 2 `printWorkspace.test.tsx` failures are **pre-existing and out of scope** (they assert `PrintWorkspace.css` content, untouched by this feature) — not investigated per instruction. |
| **Stability & UX Safety Pack — Phase 1 + Frontend Performance Pack — Phase 1** | `stable-stability-performance-pack-v1` | **Frontend-only** release of two independently Gemini-APPROVED packages. **(1) Stability & UX Safety — Error Boundaries:** new `RootErrorBoundary.tsx` (+ `.css`) — a professional ExplorerKit full-screen fallback with **Retry** (soft state reset), **Back to Home** (HashRouter-safe via `window.location.hash`, works with/without router context), **Copy technical details** (structured report via `navigator.clipboard`), a **developer `<details>`** section (message / stack / component stack), `console.error` logging, dark/light theming via design tokens, and accessibility (`role="alert"`, `aria-live`, focus-to-region). Wired at **app root** (`main.tsx` wraps `<App/>`, `scope="root"`) and **page level** (`Layout.tsx` wraps `<Outlet/>`, `scope="page"`, `resetKey={pathname}` → **auto-recovery on navigation**, shell preserved). The pre-existing scoped inline `ErrorBoundary` and its `BankAccountExplorer` usage are **left byte-for-byte untouched**. **(2) Frontend Performance — route code-splitting:** all ~55 page routes converted to **`React.lazy` + `Suspense`** with a lightweight ExplorerKit `PageLoader` (themed spinner, `role="status"`, reduced-motion, Arabic label — no generic loading text); `Login` / `Layout` / `ProtectedRoute` kept **eager** for fast first paint. **Vite `manualChunks`** splits large stable libraries into cacheable vendor chunks: `vendor-react`, `vendor-charts` (recharts+d3), `vendor-xlsx`, `vendor-docx` (mammoth), `vendor-zip` (jszip), `vendor-qrcode`. **Measured startup improvement:** initial JS parsed on the login screen dropped **3.25 MB → ~356 KB raw (846 KB → ~110 KB gzip), −89% / −87%**; build now emits **81 route/vendor chunks** instead of a single bundle; ~1.32 MB of heavy libraries deferred out of the initial load; lower steady-state renderer memory (only visited pages compile). **Zero business-logic / DB / Prisma / backend / API / auth / RBAC / printing / Electron / dependency changes**; offline-first + Arabic-first + HashRouter preserved. **Files: 10** — 6 new (`RootErrorBoundary.tsx`+`.css`, `PageLoader.tsx`+`.css`, `RootErrorBoundary.test.tsx`, `docs/STABILITY_ERROR_BOUNDARY.md`), 4 modified (`App.tsx`, `main.tsx`, `Layout.tsx`, `vite.config.ts`); +657/−121. Feature commit `f4aeff6`, merge commit `d6ec309`. Checkpoint (rollback) tag `pre-stability-performance-pack-v1` at `473c724`. **Validation: frontend tsc ✅, frontend vitest 638/638 (39 files) ✅, build:front ✅ (81 chunks). Backend/electron untouched — not re-run.** Both packages Gemini APPROVED ✅. |
| **Executive Dashboard Polish Pack — Phase 1 (Executive Daily Summary)** | `stable-executive-dashboard-polish-phase1-v1` | **Frontend-only** executive dashboard polish. New **"Today's Summary" (ملخص اليوم)** status band on the general dashboard that derives an at-a-glance daily briefing from **existing dashboard state only — no new API fetches**: unpaid invoices, overdue invoices, contracts expiring soon, document-expiry alerts, and pending operations (sum of pending expenses / draft payroll / unprinted cheques / outstanding invoices / expiring agreements), rendered as color-coded chips (`ok` / `warn` / `crit`) with an "all clear" state when nothing is pending. Added `role="status"` + `aria-label` for accessibility, 8 new bilingual i18n keys (AR + EN), 73 lines of `dashboard.css` (`.db-today` band, chips, status dots), and a navigation fix (draft-payroll alert now routes to `/salaries` instead of the non-existent `/payroll`). **Zero backend / API / Prisma / Electron / SQLite / routing / permission / print / financial-calculation changes**; offline-first preserved. **Files: 3** — `Dashboard.tsx` (+54/−1), `i18n.ts` (+16), `dashboard.css` (+73); +143/−1 total. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, electron tsc ✅, backend vitest 1190/1190 (63 files), frontend vitest 610/610 (32 files), build:back ✅, build:front ✅, electron:build ✅.** Gemini APPROVED ✅. |
| **Unified Explorer Design System Rollout — Phase 3E (Premium Invoices Explorer)** | `stable-unified-explorer-ui-phase3e-v1` | **Frontend-only** premium UI/UX modernization of the Invoices module to ExplorerKit — handled extra-conservatively as a critical financial module. **Zero backend / API / Prisma / Electron / SQLite / routing / permissions / posting / payment / tax / numbering / validation / business-rule / calculation / invoice-print / force-delete-logic / dependency changes**; offline-first preserved; reuses existing ExplorerKit components only (no duplication). **UNCHANGED verbatim:** `CreateInvoice`, `EditInvoice`, `AddPayment`, `MonthlyReportModal` and `LocationAutocomplete` remain on the existing `Modal` — every input, **price picker**, contract-based price filtering, unit→price autofill, double-submit guard, payload composition (`toInvoiceItemPayload`), discount/line-item calculations, payment-method conditional fields, validation, `AttachmentsPanel`, and the `ForceDeleteInvoiceModal` flow (permissions + preview + exact-number confirmation + audit + transactional delete) are byte-identical. **Modernized chrome (`Invoices.tsx` + new `.css`, `.invcx-` prefix to avoid Inventory's `.invx-` collision):** executive header (live count / collected / remaining identity chips), **hero "total invoice amount"** card + secondary KPI cards (count / collected / remaining / average) — all sourced from the existing server `/invoices/stats` endpoint (filter-aware, **no new requests, no invented data**), **sticky filter toolbar** (search + status & direction `FilterChip`s + customer/month/year selects + reset/refresh/export/monthly-report — same server params as before), a **modern Explorer table** with keyboard-accessible rows (Enter/Space) → **read-only Invoice Drawer** (hero total/remaining/status; Basic Information / Financial Summary / Line Items — read-only, shown only when already loaded / Notes / Technical) with **permission-gated footer actions** (Print → existing `?print=1` preview route, Edit / Collect / Cancel / Force-Delete) whose eligibility **mirrors the prior table actions exactly** and which defer to the unchanged handlers. The legacy `DataTable` list became the Explorer table + drawer; routing, permissions (`invoices.create/read/update`, SYSTEM_ADMIN force-delete) and query params unchanged. Honest-data note: KPI values come from the server stats endpoint (not loaded-page math). **Files: 2 (1 new CSS, 1 modified) — 350 insertions(+), 175 deletions(−).** **Accessibility:** Drawer focus trap + Escape + focus return + scroll lock, keyboard-accessible table rows, ARIA labels throughout, `prefers-reduced-motion`, dark mode, RTL. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, electron tsc ✅, backend vitest 1190/1190 (63 files), frontend vitest 610/610 (32 files), build:back ✅, build:front ✅.** Commits: `feat(ui): modernize invoices with ExplorerKit`, `docs: update PROJECT_STATE for unified explorer phase 3E`. **Status: merged & tagged (see Stable Tag column); on `production`.** |
| **Unified Explorer Design System Rollout — Phase 3D (Premium Cheques Explorer)** | `stable-unified-explorer-ui-phase3d-v1` | **Frontend-only** premium UI/UX modernization of the Cheques module to ExplorerKit. **Zero backend / API / Prisma / Electron / SQLite / routing / permissions / accounting / posting / cheque-workflow / status-machine / dependency changes**; offline-first preserved; reuses existing ExplorerKit components only (no duplication). **UNCHANGED verbatim:** `ChequePrintOutput` render, the `@media print` CSS + hidden print-only area + A4 page offset, `ChequeCalibrator` overlay, **Arabic tafqeet** (`tafqeetKWD`), cheque-template load/restore, `window.print()` flow, mark-printed / cancel / payment-voucher handlers, validation, and all permissions (`cheques.create/update/print/cancel`, `settings.update`). **Modernized chrome (`Cheques.tsx` + new `.css`):** executive header (bank + counts + status chips), **hero "total cheque value"** card + KPI cards (draft / printed / cancelled / highest / average — computed from already-loaded data, **no new requests**), a **preview workspace** `SectionCard` wrapping the unchanged cheque preview + print / payment-voucher / calibrate / restore / cancel actions, **sticky filter toolbar** (search + status `FilterChip`s + clear — same server params as before), a **modern Explorer table** with print/PV badges → **cheque detail Drawer** (Basic Info / Tafqeet / Bank / Printing / Audit / Notes / Technical, footer Preview-and-Print / Edit / Cancel), and a **sectioned editor `Dialog`** (Basic / Amounts / Bank / Notes) replacing the old inline form. Only existing actions exposed — no new workflow, no Delete (unsupported by the API), no fabricated fields. Honest-data note: value KPIs are derived from the loaded result set, and bank/amount/date server-filters were intentionally **not** added (the `/cheques` API supports only `search`+`status`) to honor the no-backend rule. **Files: 2 (1 new CSS, 1 modified) — 386 insertions(+), 574 deletions(−).** **Accessibility:** Dialog/Drawer focus trap + Escape + focus return + scroll lock, keyboard-accessible rows, ARIA labels throughout, `prefers-reduced-motion`, dark mode, RTL. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, electron tsc ✅, backend vitest 1190/1190 (63 files), frontend vitest 610/610 (32 files), build:back ✅, build:front ✅.** Commits: `feat(ui): modernize cheques with ExplorerKit`, `docs: update PROJECT_STATE for unified explorer phase 3D`. **Status: Gemini-approved, merged to production (`ece7b55`), tagged & pushed.** |
| **Unified Explorer Design System Rollout — Phase 3C (Heavy Operational Modules: Inventory + Maintenance)** | `stable-unified-explorer-ui-phase3c-v1` | **Frontend-only** modernization of the two largest legacy pages to ExplorerKit. **Zero backend / API / Prisma / Electron / routing / permissions / inventory-maintenance business-logic / stock calculations / WAC-costing / purchase-receipt-issue workflows / maintenance workflow / import / export / print / dependency changes**; offline-first preserved; reuses existing ExplorerKit components only (no duplication). **Inventory (`Inventory.tsx` + new `.css`):** executive header + slim `Tabs` (Balance / Materials / Categories / Purchase Orders / Goods Receipts / Material Issues). Balance → **hero (stock value)** + KPI cards + low-stock indicators + material drawer. Materials / Categories → sticky toolbar, modern table → **entity drawer** (Identity / Stock / Costing) + sectioned create/edit `Dialog`. PO / GR / MI → status `FilterChip`s, modern table → **detail drawer** (info + read-only line-items + grand total) with **workflow actions in the footer** (submit / cancel / post / edit / delete by status + permission) and sectioned create `Dialog`s with a restyled line-item builder. The old `DetailModal` became the detail drawer; all stock/WAC/costing/approval/cancel logic, endpoints, permissions preserved. **Maintenance (`Maintenance.tsx` + new `.css`):** executive header, **maintenance hero + KPI cards** (total / completed / open breakdowns / due-soon), slim `Tabs` (Records / Fuel / Breakdowns / Spare Parts). Each tab → sticky toolbar (search + equipment + type/status `FilterChip`s + date range), modern table → **detail drawer**, and sectioned create/edit `Dialog`s (Equipment / Details / Cost / Schedule / Notes) — with equipment plate autofill, record edit/delete, breakdown resolve, fuel & spare-part creation, client-side search, all filters and endpoints preserved. **Files: 4 (2 new CSS, 2 modified) — 1207 insertions(+), 1464 deletions(−).** **Accessibility:** Dialog/Drawer focus trap + Escape + focus return + scroll lock, keyboard-accessible table rows, ARIA labels throughout, `prefers-reduced-motion`, dark mode, RTL. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, electron tsc ✅, backend vitest 1190/1190 (63 files), frontend vitest 610/610 (32 files), build:back ✅, build:front ✅.** Commits: `feat(ui): modernize inventory and maintenance with ExplorerKit`, `docs: update PROJECT_STATE for unified explorer phase 3C`. **Status: merged & tagged (see Stable Tag column); on `production`.** |
| **Unified Explorer Design System Rollout — Phase 3B (Medium-Complexity Operational Modules: Salaries + Project Prices + Attendance)** | `stable-unified-explorer-ui-phase3b-v1` | **Frontend-only** modernization of three operational pages to ExplorerKit. **Zero backend / API / Prisma / Electron / routing / permissions / business-logic / payroll-attendance-pricing calculations / import / export / print / dependency changes**; offline-first preserved; reuses existing ExplorerKit components only (no duplication). **Salaries (`Salaries.tsx` + new `.css`):** executive header, slim `Tabs` (Payroll / History), payroll **hero (net total via `PrivateAmount`)** + KPI cards, sticky toolbar (month/year/employee + status `FilterChip`s + generate/export/import), modern table → **salary drawer** (Employee / Salary / Allowances / Deductions / Pay-method) with payslip·approve·pay·cancel, and a **sectioned adjustments `Dialog`** (recurring inputs + manual draft line). Approval & payment workflows and all calculations preserved. **Project Prices (`Prices.tsx` + new `.css`):** executive header, price KPIs (count/customers/last-update), **agreements dashboard** panel (`FilterChip`s + mini table), sticky filters (search + customer + plant + company + unit chips), modern table → **price drawer** (Project & customer / Pricing), a **5-section price `Dialog`** (Project / Company / Location / Pricing / Metadata), and the **usage report** rebuilt as a `Dialog` (detail vs by-company toggle). Lookup, autofill compatibility, import compatibility, archive and force-delete preserved. **Attendance (`Attendance.tsx` + new `.css`):** executive header, attendance KPIs (**total hero** + present/absent/late/leave), sticky toolbar (search + employee + date-range + status chips), modern table → **attendance drawer**, and a **sectioned create/edit `Dialog`** (Employee / Attendance / Hours with live computed hours / Notes) with the **unsaved-changes guard** preserved. Attendance workflow and validation intact. **Files: 6 (3 new CSS, 3 modified) — 1083 insertions(+), 919 deletions(−).** **Accessibility:** Dialog/Drawer focus trap + Escape + focus return + scroll lock, keyboard-accessible rows, ARIA labels throughout, `prefers-reduced-motion`, dark mode, RTL. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, electron tsc ✅, backend vitest 1190/1190 (63 files), frontend vitest 610/610 (32 files), build:back ✅, build:front ✅.** Commits: `feat(ui): modernize salaries prices and attendance with ExplorerKit`, `docs: update PROJECT_STATE for unified explorer phase 3B`. **Status: merged & tagged (see Stable Tag column); on `production`.** |
| **Unified Explorer Design System Rollout — Phase 3A (Quick Wins: Contracts + Users + Forms Hub)** | `stable-unified-explorer-ui-phase3a-v1` | **Frontend-only** low-risk modernization of three remaining pages to ExplorerKit. **Zero backend / API / Prisma / Electron / database / routing / business-logic / permissions / validation / import / export / print / architecture / dependency changes**; reuses existing ExplorerKit components only (no duplication). **Contracts (config-only):** enabled the Phase-2 `ResourcePage` explorer via `modules.tsx` (`explorer` flag + `formSections` + field `section` tags → Contract / Pricing / Period) and added the **Financial Summary** action to the explorer detail-drawer footer. Force-delete (409 → `ForceDeleteContractModal` via shared `executeDelete`), financial summary, linked-price autofill, search, status filters, permissions all preserved — now with executive header, hero/KPI, sticky toolbar, modern table, row→drawer, sectioned explorer dialog. **Users (`Users.tsx` + new `.css`, full rewrite):** executive header, user KPIs (total/active/roles/system), slim `Tabs` (Users / Roles), sticky **client-side search** toolbar, modern table → **user drawer** (edit / enable-disable), and a **sectioned create/edit `Dialog`** (Identity / Role & permissions, with a live role permission-count hint). Roles tab rebuilt with `SectionCard`s + granted/denied permission `StatusChip`s. All user/role load, save, validation, toggle-active and role-expand logic preserved. **Forms Hub (`Forms.tsx` + new `.css`, landing only — printable forms untouched):** executive header, **hero + KPI cards**, **category FilterChips** (HR / Operations) + search filter, premium form cards, and an `EmptyState`. Employee selector, per-card print-mode selection, navigation and `PrintLogPanel` preserved exactly. **Files: 6 (2 new CSS, 4 modified) — 507 insertions(+), 359 deletions(−).** **Accessibility:** Dialog/Drawer focus trap + Escape + focus return + scroll lock, keyboard-accessible rows, ARIA labels throughout, `prefers-reduced-motion`, dark mode, RTL. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, electron tsc ✅, backend vitest 1190/1190 (63 files), frontend vitest 610/610 (32 files), build:back ✅, build:front ✅.** Commits: `feat(ui): modernize contracts users and forms hub with ExplorerKit`, `docs: update PROJECT_STATE for unified explorer phase 3A`. **Status: merged & tagged (see Stable Tag column); on `production`.** |
| **Unified Explorer Design System Rollout — Phase 2 (Core Operational Modules)** | `stable-unified-explorer-ui-phase2-v1` | **Frontend-only** executive UX modernization extending the Phase-1 ExplorerKit to six core modules. **Zero backend / API / Prisma / Electron / permissions / validation / accounting / import / reconciliation / routing / dependency changes**; offline-first + Arabic-first intact. **ExplorerKit extended (reused everywhere, no duplication):** added `Dialog` + `DialogSection` (modal standard — header icon/title/subtitle → body section cards → footer), slim `Tabs`, `Pagination`, and a shared **`useFocusTrap`** hook now backing **both** `Drawer` and the new `Dialog` (Tab focus-trap both directions, Escape, focus-return, scroll-lock, `aria-modal`/`aria-labelledby`); scoped `.xpl-*` CSS for tabs/dialog/form-grid/pagination/invalid-states. **Architectural decision:** `Accounting` & `Expenses` are standalone pages → direct rewrites; `Suppliers/Customers/Employees/Equipment` are rendered by the **shared** `ResourcePage`+`DataTable`+`FormDialog` (also serving out-of-scope `contracts`/`users`), so the executive layout is an **opt-in `explorer` flag** on `ModuleConfig` and the sectioned dialog is a **backwards-compatible `skin='explorer'`** on `FormDialog` (default `legacy` keeps `contracts`/`users` **byte-for-byte unchanged**); no routing changes. **Accounting Center (`Accounting.tsx` + new `.css`):** executive header + slim `Tabs`; Summary → gradient **hero (net profit)** + KPI cards + two section panels; Chart of Accounts → sticky toolbar with account-type **filter chips**, modern table (type/status chips), **account drawer** (replaces tiny modal) + sectioned account dialog; Journal Entries → modern table, **entry drawer** (line table + balanced totals) and **sectioned posting dialog** with live balanced/unbalanced state chip; Payments → modern table. All accounting/journal/posting/cancel logic + report-highlight row preserved. **Expenses Center (`Expenses.tsx` + new `.css`):** executive header w/ live chips, **hero (total) + KPI cards** (count/pending/month/year), category·supplier·company breakdown chips, **sticky filters** (search + status chips + selects + export), modern table → **expense drawer** (details + approve/reject/edit/delete), and a **5-section add/edit dialog** (Basic / Payment / Supplier / Accounting period / Notes + Attachments). All CRUD, approval, accounting integration, Excel export, highlight preserved. **ResourcePage explorer (Suppliers/Customers/Employees/Equipment):** executive header + KPI cards (equipment → working/not-working/uptime hero; employees → expiring-docs KPI), expiring-document **alert chips**, sticky toolbar + status filter chips, modern table with **row→detail drawer**, kit pagination, premium empty/skeleton states, and **sectioned create/edit dialogs** (field `section` tags in `modules.tsx`; e.g. Employee → Personal / Job & Salary / Documents / Address). All force-delete/archive/409 flows, alerts, equipment & contract stats, export preserved. **Files: 9 (2 new CSS, 7 modified) — 1777 insertions(+), 725 deletions(−).** **Accessibility:** unified `Dialog`/`Drawer` focus trap + Escape + focus return + scroll lock, keyboard-accessible rows, ARIA labels throughout, `prefers-reduced-motion`, dark mode, RTL. **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, electron tsc ✅, backend vitest 1190/1190 (63 files), frontend vitest 610/610 (32 files), build:back ✅, build:front ✅.** Gemini: APPROVED. Commits: `feat(ui): modernize core operational modules with ExplorerKit phase 2`, `docs: update PROJECT_STATE for unified explorer phase 2`. |
| **Unified Explorer Design System Rollout — Phase 1 (Reports + Document Expiry + Data Import Centers)** | `stable-unified-explorer-ui-bundle-phase1-v1` | **Frontend-only** executive UX modernization extending the Bank Account Explorer design language (Phases A→E) to three pages via a new **shared, fully-scoped Explorer Kit**. **Zero backend / API / Prisma / Electron / permissions / validation / import-export-print logic / dependency changes** — all business logic, handlers, and API calls preserved verbatim; offline-first + Arabic-first intact. **New shared kit (2 files):** `frontend/src/components/explorer/ExplorerKit.tsx` (+ `explorer-kit.css`, `.xpl-*` prefix, local design tokens resolving to real theme tokens with fallbacks) — `ExecutiveHeader`, `IdChip`, `MetricCard`, `HeroMetric`, `StatusChip`, `MetricChip`, `FilterChip`, `SearchBox`, `SectionCard`, `EmptyState`, `ErrorBanner`, `SkeletonRows`, accessible `Drawer` (Tab focus-trap + Escape + focus-return + scroll-lock + `aria-modal`/`aria-labelledby`), `DrawerSection`/`DrawerField`, `Button`. Material Symbols + IBM Plex Sans Arabic, soft shadows, rounded cards, 200–250ms transitions with `prefers-reduced-motion` guard, RTL throughout. **Page 1 — Reports Center (`Reports.tsx`/`Reports.css`):** executive header with live status chips → summary hero (`HeroMetric`) + secondary KPI grid → sticky search/recent/filter-chip toolbar → premium report cards (emoji identity, status + category chips, favorite toggle, تشغيل/تهيئة actions) → **professional report drawer** (name, description, category/status, result count + last run, required filters, export Excel/PDF + print, contextual notes) → run/preview section (skeleton loader, results bar, modern preview table with totals row, export dropdown). All export/print/preview/favorites/recent/filter logic unchanged. **Page 2 — Document Expiry Center (`DocumentExpirationCenter.tsx` + new `.css`):** replaced fully inline-styled page with kit composition — executive header (+ export action) → hero KPI + **clickable urgency metric cards** (منتهية/7/30/60/90, filter on click, active state) → sticky filter toolbar (search + category select + urgency chips + active-filter pills) → modern timeline table (category icon cells, entity name/code, colored days-remaining, urgency status chips, keyboard-accessible rows) → **document detail drawer** (entity data, document type, expiry date, days-remaining with a visual gauge, status, quick actions). Honest-data note: backend exposes only `expiryDate`/`daysRemaining`/`urgency` (no issue dates or history), so those drawer fields were omitted rather than fabricated — no API change. **Page 3 — Data Import Center (`DataImport.tsx` + new `.css`):** rebuilt the grid layout into a **step-based workflow** — executive header → 4-node progress **stepper** → import-summary KPI cards → entity selector (icon toggle cards) → **drag-and-drop upload zone** (refactored single `processFile()` reused by picker + drop, identical 10 MB / extension / MIME guards) → validation-summary cards → **ready-status card + import button** → preview table (status pills, error cells) → sticky column-mapping guide (required/optional). Import preview/execute/template/reset logic and all guards unchanged; payroll DRAFT-coercion notice preserved. **Accessibility:** keyboard nav, focus trap, Escape, focus return, ARIA labels on all controls (incl. drawer filter selects/inputs and hidden file input), `aria-pressed` toggles, screen-reader row labels. **Files: 8 (4 new, 4 modified) — 2703 insertions(+), 1218 deletions(−).** **Validation: prisma validate ✅, backend tsc ✅, frontend tsc ✅, electron tsc ✅, backend vitest 1190/1190 (63 files), frontend vitest 610/610 (32 files), build:back ✅, build:front ✅.** Commits: `feat(ui): modernize reports document expiry and import centers`, `docs: update PROJECT_STATE after unified explorer ui bundle`. **Status: merged & tagged (see Stable Tag column); on `production`.** |
| **AI Assistant — Phase AI-2.5 (Professional Business Skills Expansion)** | `stable-ai-assistant-business-skills-v1` | Frontend-only enrichment layer on top of Phase AI-2. No backend changes, no new APIs, no database migrations, no permission changes, no SQL, no LLM, no RAG, no OCR. **New file:** `frontend/src/ai/qualityEngine.ts` — pure `computeQuality()` function (shared across all 6 skills; base 100, completeness/warning/missing-field/custom penalties; returns `qualityScore` + `QualityIssue[]`). **Extended `types.ts`:** 9 new optional fields on `SkillResult` (`capabilityLevel`, `richSources`, `explanationSteps`, `qualityScore`, `qualityIssues`, `relatedSkills`, `relatedPages`, `actions`, `skillMetadata`, `diagnostics`); `RouterDecision` extended with `blocked?` + `blockedMessage?`; new interfaces: `RichSource`, `ExplanationStep`, `QualityIssue`, `RelatedSkill`, `RelatedPage`, `SkillAction`, `SkillMetadata`, `DevDiagnostics`; new types: `CapabilityLevel` (`complete`/`partial`/`preview`/`comingSoon`), `RichSourceType`, `ActionKind`, `SkillGeneration`. **Router (`router.ts` rewrite):** `BLOCKED_PATTERNS` array checked BEFORE scoring loop — intercepts reconciliation/matching terms (`مطابقة`, `تسوية`, `matched`, `unmatched`, `reconciliation`) with informational Arabic message; 12 new routing rules (quality_issues, statement_overview, department, status, equipment, customers, supplier, customer, discover, recommend — +others); total ~35 rules. **ResultCard (`ResultCard.tsx` rewrite, ~410 lines):** 7 new sub-components (`CapabilityBadge`, `QualityGauge`, `ExplainPanel`, `RichSourcesPanel`, `ActionCards`, `RelatedPanel`, `MetaPanel`); 16-section render order; CSS custom properties for progress bar widths (`--rc-fill-width`) avoiding inline style warnings; `downloadResultTxt()` helper; `useNavigate` for action card navigation. **ResultCard.css additions (~150 lines):** `.ai-rc-capability` modifiers, `.ai-rc-quality*` gauge, `.ai-rc-explain*` collapsible panel, `.ai-rc-rich-sources*` grid, `.ai-rc-actions*`, `.ai-rc-related*`, `.ai-rc-meta*`. **Six enriched skills:** `bankStatement.ts` — removed all reconciliation/matching language (changed card title "حالة المطابقة"→"توزيع العمليات"; removed matched/unmatched rows; retained review/duplicates/ignored); 2 new intents (`quality_issues`, `statement_overview`); full AI-2.5 enrichment on all 7 intents. `payroll.ts` — 2 new intents (`department` groups by `r.employee?.department`, `status` groups by status field with STATUS_AR map); full enrichment on all 7 intents. `dashboard.ts` — 2 new intents (`equipment` working/not-working count, `customers` government/private split); full enrichment on all 6 intents. `expenses.ts` — 1 new intent (`supplier` from `stats.bySupplier`); `capabilityLevel: 'comingSoon'` for trends. `contracts.ts` — 1 new intent (`customer` groups by `c.customer?.name`). `reports.ts` — full Discovery Skill replacing stub: two-layer `GET /reports` attempt + `REPORT_REGISTRY` fallback; `discover` + `recommend` intents. **AIAssistant.tsx targeted edits:** `serializeMsgs()` strips 9 large AI-2.5 arrays before localStorage write (9 fields: richSources, explanationSteps, relatedSkills, relatedPages, actions, skillMetadata, diagnostics + highlights, cards); `sendMessage()` handles `decision.blocked === true` with informational blue card (not error); ROADMAP_ITEMS updated: AI-2 `current: false`, AI-2.5 `current: true`. **AIAssistant.css additions:** `.ai-blocked-card` (blue tint, informational), `.ai-kpi-badge--enriched` (teal). **13 files changed, 2188 insertions(+), 169 deletions(−); 1 new file (`qualityEngine.ts`).** Validation: frontend tsc ✅ 0 errors, backend tsc ✅ 0 errors, electron tsc ✅ 0 errors, 1120/1120 backend tests (57 files), build:back ✅, build:front ✅. Gemini APPROVED ✅. Commit `55c7b7d`. |
| **AI Assistant — Phase AI-2 (Safe Intelligence Engine)** | `stable-ai-assistant-phase-ai-2-v1` | Frontend-only deterministic skill engine. No backend AI module, no new backend routes, no database migrations, no permission changes, no LLM, no SQL generation, no RAG, no OCR. **New frontend AI module (`frontend/src/ai/`):** `types.ts` (SkillResult, RouterDecision, ValueKind `'money'|'count'|'percent'|'date'|'text'`, WarningSeverity `'info'|'warning'|'danger'`, SkillStatistic, SkillHighlight, SkillDataCard, SkillSource, SkillWarning); `router.ts` (deterministic keyword router — 27 rules, 6 skills, weight scoring: 3=precise intent, 2=module keyword, 1=fallback; `route(prompt) → RouterDecision` with `skillId`, `intent`, `matchedKeywords`, `confidence`, `fallback`); `registry.ts` (memoized `Map<skillId, SkillExecutor>`, `executeSkill(skillId, prompt, intent) → Promise<SkillResult>`); `ResultCard.tsx` (three-state renderer: normal / `isInsufficientData` / `isError`; sub-components `StatBox`, `HighlightRow`, `DataCardBlock`, `WarnBanner`, `FollowUps`, `DevPanel`; DEV-only dev panel); `ResultCard.css` (scoped `.ai-rc-*`, RTL, CSS custom properties — zero bleed). **Six frontend-only skills (read-only via existing Axios client, no backend changes):** `bankStatement.ts` — `GET /bank-statement-import` + `GET /bank-statement-import/:id/workspace`; intents: summary, withdrawals, deposits, fees, warnings. `payroll.ts` — `GET /payroll?pageSize=150`; intents: summary, highest, lowest, average, comparison; extracts `PayrollRecord[]` from `res.data.data.data`. `expenses.ts` — `GET /expenses/stats`; intents: summary, top categories, trends (`isInsufficientData` — no time-series endpoint). `contracts.ts` — `GET /contracts/summary` + `GET /contracts?pageSize=50`; intents: summary, active, expiring (client-side 90-day filter via `daysUntil()`), top (sort by monthlyTransportValue desc). `dashboard.ts` — `GET /dashboard/overview` + `/trend` + `/operational`; intents: kpi (default), revenue, profit, alerts. `reports.ts` — returns structured `isInsufficientData: true` (no report-summary API exists); sources point to Reports Center + Dashboard; suggested questions redirect to working skills. **AIAssistant.tsx changes:** `AiMessage` extended with `result?: SkillResult` + `routerDecision?: RouterDecision`; `sendMessage()` replaced with async `route()` → `executeSkill()` pipeline; `serializeMsgs()` strips `highlights`/`cards` arrays before localStorage write (compact snapshot); `buildNoSkillResult()` for unmatched prompts; message bubble renders `<ResultCard>` when `msg.result` exists; hero badge updated to AI-2 "Safe Intelligence Engine"; roadmap AI-2 marked `current: true`; What's New updated; DEV-only router decision sidebar panel; `sendTimerRef` removed (replaced by async/await). **13 files changed:** 11 new files in `frontend/src/ai/`, 2 modified (`AIAssistant.tsx`, `AIAssistant.css`). 2105 insertions. Validation: frontend tsc ✅ 0 errors, backend tsc ✅ 0 errors, electron tsc ✅ 0 errors, 1120/1120 backend tests (57 files), build:back ✅, build:front ✅. Gemini APPROVED ✅. Commit `9b727c1`. |
| **AI Assistant — Phase AI-1.5 (Smart Workspace Polish)** | `stable-ai-assistant-phase-ai-1.5-v1` | Local-only polish layer on top of Phase AI-1. Still UI-only, still no backend AI module, no LLM provider, no SQL/RAG/OCR, no database migration. **Package A — Local Conversation History:** conversations stored in `localStorage` under key `ai_assistant_history_v1`; schema `{ id, title, createdAt, updatedAt, messages: SerializedMsg[] }` (timestamp stored as unix number); auto-saved after every assistant reply via `useEffect`; full CRUD: new conversation (clears state + resets `currentConvIdRef`), load, inline rename (blur/Enter confirms, Escape cancels), delete, clear-all; history list in collapsible sidebar panel with active-conversation highlight; safe recovery on JSON parse failure (returns `[]`). **Package B — Pinned Prompts:** localStorage key `ai_assistant_pinned_prompts_v1` (serialized `Set<string>`); ⭐ star button on every prompt chip (toggle with toast feedback); pinned prompts panel visible only when non-empty; unpin from pinned panel; state updates trigger `useEffect → savePinned()`. **Package C — Quick Actions grid:** 10 cards in 5-col grid (8 available, 2 قريباً); 5 `type:'prompt'` cards auto-send via `sendMessage()`, 3 `type:'navigate'` cards use `useNavigate` for internal routing (no backend), 2 `type:'soon'` cards show toast; navigate cards styled with `var(--blue)` border; قريباً badge overlaid; no backend call on any action. **Package D — Copy/Export:** per-message copy buttons on all assistant bubbles — copy text (`navigator.clipboard.writeText`) + copy Markdown (wraps lines with `>`); export conversation button in chat panel header → client-side Blob `text/plain;charset=utf-8` download with UTF-8 BOM (`﻿`) for Arabic Excel compatibility; filename `ai-assistant-<timestamp>.txt`; each action shows toast confirmation. **Package E — Recent Activity Row:** 4-card row between hero and KPI section; cards: رسائل الجلسة (message count), آخر مهارة (last skill used), آخر سؤال (last user question, 32-char truncated), محادثات محفوظة (history count); all derived via `useMemo([messages, conversations.length])`. **Package F — What's New Panel:** collapsible sidebar panel with two sections: المتاح الآن (8 items, green border) and قريباً (6 items AI-2 through AI-7, gray border). **Package G — Better Empty State:** empty conversation view shows 42px icon + title + description + 3 clickable prompt chips that populate the textarea + safety notice badge with `shield` icon. **Package H — Micro-interactions:** 380ms thinking-dots animation (3× `.ai-dot` with staggered `@keyframes ai-dot-bounce`); toast slide-in `@keyframes ai-toast-in` (fixed position, 2.2s auto-dismiss, timer cleared on rapid re-trigger); hover `translateY(-2px)` on KPI cards, quick-action cards, skill cards; pin star transitions; `hourglass_top` icon on send button while sending; input `disabled` during send; `ai-chat-send.sending` amber background. **Package I — Safety improvements:** amber warning banner at top of Safety Center panel ("كل الردود حالياً تجريبية فقط") + lock footer ("لا يتم إرسال أي بيانات خارج الجهاز"). **Package J — UI Consistency:** `.ai-*` CSS prefix maintained throughout; RTL, dark-mode, responsive breakpoints (1100/900/800/600px). **Package K — No Backend:** zero `api.get/post`, zero `fetch`, zero `axios`, zero IPC, zero SQL, zero network call. **Other improvements:** roadmap updated to include AI-1.5 as current phase; hero phase badge updated to "AI-1.5 Smart Workspace"; KPI cards are now clickable and auto-send their canonical prompt; `SKILL_RESPONSES` dict gives skill-specific placeholder responses; timer cleanup on unmount via `useEffect` return. **2 files changed:** `frontend/src/pages/AIAssistant.tsx` (complete rewrite, ~560 lines), `frontend/src/pages/AIAssistant.css` (complete rewrite, ~740 lines). **localStorage keys:** `ai_assistant_history_v1`, `ai_assistant_pinned_prompts_v1`. Validation: frontend tsc ✅ 0 errors, backend tsc ✅ 0 errors, electron tsc ✅ 0 errors, 1120/1120 backend tests (57 files), build:back ✅, build:front ✅. Gemini APPROVED ✅. Commit `3338492`. |
| **AI Assistant — Phase AI-1 (UI Shell & Workspace Foundation)** | *(no separate tag — rolled into `stable-ai-assistant-phase-ai-1.5-v1`)* | UI-only shell for the future AI Assistant. No LLM provider, no SQL/RAG/OCR backend, no database migration. **Architecture doc:** `docs/AI_ASSISTANT_ARCHITECTURE.md` (7 layers, security model, 8-skills catalog, MVP phases AI-1–AI-7, never-allowed rules, 14-step roadmap). **Navigation:** New sidebar group `الذكاء الاصطناعي / Artificial Intelligence` (`nav.group.ai` i18n key) with a single entry المساعد الذكي / AI Assistant (`nav.ai_assistant`), Material Symbols Outlined icon `psychology`, temporarily gated by `reports.read` permission. Route `/ai-assistant` added to `App.tsx` inside the protected Layout block. **Page structure (7 sections):** (1) Hero banner — gradient `#1b3f6a→#0d2741→#1d4f7a`, 4 safety chips (بيانات داخلية فقط / لا يوجد اتصال خارجي / نتائج قابلة للمراجعة / RBAC مطبق). (2) Capability cards — 4 KPI-style ready-capability cards + 5 coming-soon chips. (3) Suggested prompt groups — 4 groups × 4 prompts; click-to-populate textarea. (4) Chat workspace — two-column layout (chat main + 300 px side panels); textarea + send/clear buttons; conversation area with auto-scroll; `useRef` for scroll and input focus; deterministic placeholder responses via `buildResponse()` + `detectSkill()`. (5) Safety Center collapsible panel. (6) Architecture diagram, Roadmap (AI-1 through AI-7), and Source preview collapsible panels. (7) Capability Explorer — 8 skill cards in 4-col grid. **CSS:** New `AIAssistant.css` (~350 lines), all selectors prefixed `.ai-`; responsive breakpoints at 1100/800/600 px; RTL-safe; dark-mode compatible via CSS vars. **Performance:** `useMemo`, `useCallback` on send/clear/prompt/panel-toggle handlers. **i18n:** `nav.group.ai` + `nav.ai_assistant` keys added to both `ar` and `en` sections of `i18n.ts`. **Strictly no backend changes:** no backend AI module, no new Prisma model, no migration, no permission key, no IPC channel, no external HTTP call. **5 files changed:** `docs/AI_ASSISTANT_ARCHITECTURE.md` (new), `frontend/src/pages/AIAssistant.tsx` (277 lines, new), `frontend/src/pages/AIAssistant.css` (~350 lines, new), `frontend/src/App.tsx` (+1 import +1 route), `frontend/src/config/modules.tsx` (+1 nav group), `frontend/src/lib/i18n.ts` (+4 keys). Validation: frontend tsc ✅ 0 errors, backend tsc ✅ 0 errors, electron tsc ✅ 0 errors, 1120/1120 backend tests (57 files), build:back ✅, build:front ✅. Gemini APPROVED ✅. Commit `9be4b26`. |
| **Reports Center Explorer — Phase 7 (UI Modernization)** | `stable-reports-center-phase7-v1` | Comprehensive UI/UX modernization of `Reports.tsx` using the same visual language as Bank Statement Explorer, Payroll Analytics Explorer, and Integrations Center. **CSS:** New `Reports.css` (~420 lines) with `.rc-*` prefix, fully scoped — zero bleed to other pages. All design tokens from CSS vars. **Package A — Hero Header:** Gradient hero with icon, title "مركز التقارير", subtitle, and 4 live chips (total count / ready / needs-filter / live) + favorites chip when any are starred. **Package B — 8 KPI Cards:** 4-col responsive grid; cards: إجمالي التقارير, التقارير المالية, الموارد البشرية, العمليات, المستحقات, جاهز للتشغيل, يحتاج فلاتر, المفضلة; all derived from `REPORT_TYPES` via `useMemo`. **Package C — Instant Search:** Client-side filter on report name (translated), `descAr`, `groupAr`, and `key`; search clear button; zero backend calls. **Package D — Quick Filter Chips:** 8 chips (الكل + 6 category groups + ⭐ المفضلة); active chip highlighted. **Package E — Modern Report Cards:** `.rc-card` with 48×48px icon wrap, Arabic name, description, status badge, category tag, 3 action buttons (▶ تشغيل / ⚙ إعدادات / ☆ مفضلة); selected card border-highlighted. **Package F — Status Badges:** 🟢 جاهز / 🟡 يحتاج فلاتر / 🔵 تقرير مباشر, derived from `statusType` field added to `ReportType`. **Package G — Summary Bar:** Shows visible count, per-group breakdown badges, favorites count. **Package H — Recent Reports:** `localStorage` key `rc_recent_v1`; stores last 5 run report keys; shown as chip buttons in toolbar; updates after each successful preview load. **Package I — Favorites:** `localStorage` key `rc_favorites_v1`; ⭐/☆ toggle per card; persists across sessions; count shown in hero chip and KPI card. **Package J — Side Drawer Preview Panel:** `.rc-panel` (480px) with overlay; shows report description, metadata (category, status badge), all filter fields; "▶ تشغيل التقرير" + "مسح الفلاتر" footer buttons; closes on overlay click. **Package K — Professional Empty States:** No-results state ("لا توجد تقارير مطابقة" with search icon) and pre-run state (report icon + label + hint text). **Package L — Skeleton Loading:** Static grey `.rc-skeleton-shimmer` blocks (no banned `@keyframes`). **Package M — Unified Export Dropdown:** `rc-export-wrap` with outside-click handler (`exportRef`); 3 items (Excel via `ExportExcelButton`, PDF via `exportReportAsPdf`, Print via `navigate`); only shown when `preview` exists and `canExport`. **Package N — Responsive:** Breakpoints at 1300/1100/860/640px; RTL layout preserved. **Package O — CSS `.rc-*`:** Fully scoped 420-line stylesheet, no inline styles, all buttons have `type="button"`. **Package P — Performance:** `useMemo` on `filteredReports`, `kpi`, `recentReports`, `groupCounts`; `useCallback` on `loadPreview`. **Package Q — Preserve Logic:** All original API calls (`/reports/${selected}/preview`, `/reports/${selected}/export`), `buildParams()`, `downloadExcel()`, `downloadPdf()`, `openPrint()`, filter state management, `REPORT_TYPES` definitions, `GROUPS`, `CONTRACT_UNITS`, `COMPANY_GROUPS` — all preserved exactly. `descAr` and `statusType` fields added to `ReportType` interface (additive, no breaking changes). **Backend untouched:** No changes to routes, controllers, services, Prisma schema, migrations, constants, or permissions. **2 files changed: `frontend/src/pages/Reports.tsx` (563 insertions, 213 deletions), `frontend/src/pages/Reports.css` (420 lines, new file). Validation: frontend tsc ✅ 0 errors, backend tsc ✅ 0 errors, electron tsc ✅ 0 errors, 1120/1120 backend tests (57 files), build:back ✅, build:front ✅. Gemini APPROVED ✅. Commit `4ef804b`.** |
| **Integrations Center Phase 2 — Professional UI/UX Modernization** | `stable-integrations-center-phase2-v1` | Full visual redesign of `Integrations.tsx` using the same design system as Bank Statement Explorer and Payroll Analytics Explorer. **CSS:** New `Integrations.css` (670 lines) with `.ic-*` prefix, fully scoped — zero bleed to other pages. All design tokens from CSS vars (`var(--primary)`, `var(--surface)`, `var(--border)`, `var(--text)`, `var(--green)`, `var(--amber)`, `var(--blue)`, `var(--shadow)`, etc.). **Package A — Hero Header:** `.ic-header` with icon, title, subtitle, and 4 live header chips computing counts dynamically from API data (التكاملات النشطة / التكاملات الجاهزة / قريباً / إصدار النظام). **Package B — KPI Dashboard:** `.ic-kpi-grid` 4-col responsive grid; cards: التكاملات المفعلة, التكاملات الجاهزة, التكاملات المستقبلية, حالة النظام; all derived from live `cards` state via `useMemo`. **Package C — Search:** Real-time filter on nameAr, nameEn, descriptionAr, descriptionEn, and capabilities labels; placeholder "ابحث عن تكامل...". **Package D — Quick Filter Chips:** 9 chips (الكل / يعمل / جاهز / قريباً / مخطط / النسخ الاحتياطي / البنوك / Excel / AI); `chipMatches()` pure function; active chip highlighted. **Package E — Better Cards:** Full redesign with 52×52 px icon wrap, nameAr title, nameEn subtitle, description, capability tags, format chips, version, status row, action buttons. **Package F — Professional Status:** `.ic-status-dot` (green=static ring via `box-shadow: 0 0 0 3px var(--green-light)`, amber, gray, blue); text labels 🟢/🔵/🟡/⚪. **Package G — Action Buttons:** ▶ تشغيل / ⚙ إعدادات / ℹ فتح — only rendered when applicable; RBAC-gated (`integrations.configure`, `integrations.run`); disabled state via `.ic-btn:disabled`. **Package H — Feature Tags:** Capability chips (`.ic-tag`), format chips (`.ic-tag.format` blue), entity chips (`.ic-tag.entity` green), bank chips (`.ic-tag.bank` amber). **Package I — Hover Experience:** `translateY(-2px)`, `shadow-lg`, `border-color: var(--primary)`, icon scale `1.08` — all 200ms eased. **Package J — Larger Icons:** 52×52 px icon wrap, 26px emoji, category-coloured background (blue=bank, green=import, amber=backup). **Package K — Import Cards:** `getExtraCardMeta()` provides format arrays and bank arrays for payroll-bank-import (Excel/CSV + 6 banks) and bank-statement-import (Excel/CSV/XML + 7 banks). **Package L — Cloud Backup Card:** Provider (OneDrive/Google Drive), encryption (AES-256), retention (قابل للتخصيص) chips shown when `card.id === 'cloud-backup'`. **Package M — Enhanced Excel Entities:** 7 entity chips (عملاء/موردون/موظفون/معدات/عقود/مصروفات/أسعار) via `getExtraCardMeta`. **Package N — System Health Panel:** `SystemHealthPanel` component reads live health from `cards` state; 5 rows (payroll-import, statement-import, excel-import, cloud-backup, AI); status dot + Arabic label per row. **Package O — Roadmap Panel:** `RoadmapPanel` static component; 5 future items (Open Banking/قريباً, AI Assistant/مخطط, OCR Documents/مخطط, Mobile Companion/مستقبلاً, API Integrations/مستقبلاً); read-only, no actions. **Package P — Responsive Layout:** Breakpoints at 1100px (3-col KPI, single-col panels), 860px (2-col KPI, 1-col cards), 640px (padding 16px, full-width search), 420px (1-col KPI). **Package Q — CSS `.ic-*`:** 670-line scoped stylesheet. **Package R — Performance:** `useMemo` on `filteredCards`, `filteredCategories`, `kpiValues`, `headerCounts`, `byId` (in SystemHealthPanel). **Package S — Preserve Logic:** All routes, permissions, `handleRun()` (targetRoute navigation preserved), `handleSaved()`, `SettingsPanel` (logic 100% unchanged, restyled), `RunResultDialog` (preserved), `integrationsApi` calls unchanged. **No backend changes. No Prisma schema changes. No migrations. No permission changes. No route changes. No Electron IPC changes.** 2 files changed: `frontend/src/pages/Integrations.tsx` (519 insertions, 280 deletions), `frontend/src/pages/Integrations.css` (670 lines, new file). **Validation: frontend tsc ✅ 0 errors, backend tsc ✅ 0 errors, electron tsc ✅ 0 errors, 1120/1120 backend tests (57 files), build:back ✅, build:front ✅ 6.47s. Gemini APPROVED ✅. Commit `7c5d8fb`.** |
| **Operational Polish Suite Phase 4** | *(feature branch — pending production merge/tag)* | Frontend-only polish release. No DB migrations, no new modules, no new permissions, no backend API changes. **Package A — Company Name Standardization:** Full official name `شركة المنار الدولية لإنشاء وإصلاح الطرق والشوارع والأرصفة ومستلزمات الطرق ذ.م.م` propagated to all print surfaces: `FormHeader.tsx`, `FormFooter.tsx`, `formStyles.ts`, `InvoicePreview.tsx` (engine + legacy headers), `ReportPrint.tsx`, `DocumentVerify.tsx`, `Cheques.tsx` beneficiary fallback, `brandingLoader.ts` fallback, `seed.ts` `company.name` value. **Package B — Multiple Signature Support in Settings:** `SigSlot` interface (`id, name, title, imageUrl, show, isDefault`); `migrateLegacySig()` backward-compat migration; multi-signature card list with add/remove/default-select/show-toggle/field-edit/upload; `saveSignatures()` writes `print.signatures` (JSON) + syncs `print.signatureImage` + `print.showSignature` for backward compatibility with print engine; `BrandingLayoutDesigner` uses default-signature image. Removed: `sigInputRef`, single-signature handlers. **Package C — Translation Dictionary Add-Row Fix:** Root cause was IIFE pattern causing new rows to have no stable React identity; fixed by extracting `DictTable` sub-component with `tbodyRef`, auto-scroll to last row via `requestAnimationFrame`, auto-focus first input. **Package D — Bank Statement Import UX Redesign:** All 5 wizard steps redesigned using project CSS vars (`.card.panel`, `.btn`, `.btn-secondary`, `var(--primary)`, `var(--border)`, `var(--text-muted)`); all Tailwind classes removed; post-import navigation bug fixed — replaced `<a href="#/bank-reconciliation/...">` (causes full page reload in Electron HashRouter) with `useNavigate` + `onClick` button. **Package E — Privacy Button Icon-Only Simplification:** Removed `<span className="privacy-label">` text labels from both privacy toggle button states; icon-only (🔒/🔓). **Package F — Payroll Analytics UI Polish:** `BankSalaryAnalytics.tsx` — 3 KPI summary cards (إجمالي المبالغ المحوّلة/عدد عمليات التحويل/الموظفون المدرجون) with `<PrivateAmount>` wrapping; filter panel reorganized into 4 labeled groups with quick period chips (آخر 3 أشهر/آخر 6 أشهر/هذه السنة); chart sections wrapped in `.card.panel`; tables use project CSS vars + sticky thead; full Tailwind-to-project-css migration (hover classes, skeleton, `s.cls` color strings all converted). **Package G — Sidebar Import Center Cleanup:** 4 import/bank items extracted from `nav.group.financial` into new `nav.group.import_center` group (مركز الاستيراد) in `modules.tsx` + `i18n.ts`. **1 file changed (frontend only: BankSalaryAnalytics.tsx).** All prior packages committed in earlier waves on the same branch (commits `dd67e0e`→`f55e4f4`; Package F commit `8fc606e`). **Validation: frontend tsc ✅ 0 errors, frontend build ✅ 5.88s. No backend changes, no schema migrations, no new permission keys. Gemini APPROVED ✅.** |
| **Operational Polish Suite Phase 3** | `stable-operational-polish-suite-phase3-v1` | Frontend-only polish release. No DB migrations, no new modules, no new permissions, no backend API changes. **Package A — Global Header UI Polish:** `layout-polish.css` — added `flex-shrink: 1; min-width: 0` on `.top-actions` and `.search` to prevent topbar overlap at 1024px and 125%/150% browser zoom; added `text-overflow: ellipsis` via `.nav-label` span wrapper in `Layout.tsx` for sidebar nav items; added `.privacy-label` span wrapper to hide privacy button text at 1100px breakpoint; responsive media queries at 1200px (search 240px) and 1100px (search 160px, privacy label hidden). **Package B — Salary Certificate Optional Editable Overrides:** `SalaryCertificateTemplate.tsx` — exported `PrintOverrides` interface with 6 fields (`purpose`, `jobTitle`, `department`, `salaryText`, `issueDate`, `notes`); added `val()` helper for `override?.trim() \|\| fallback \|\| '—'` pattern (print-only, never persisted); both AR and EN template branches consume overrides with DB fallbacks; optional notes paragraph and issueDate field in both branches. `SalaryCertificate.tsx` — 2-column 6-field no-print override panel. **Package C — Backup/Restore/Bank Statement Audit:** Audited `backup.ipc.ts`, `backup.service.ts`, `Backup.tsx`, `BankStatementImport.tsx` — no defects found. Safety backup before restore is present, WAL checkpoint before copy is present, app relaunch after restore is wired correctly, bank statement 5-step wizard persists to SQLite correctly. No changes made. **Package D — Administrative Forms Full English Localization (D1 audit + D2 implementation):** `FormLayout.tsx` — added `lang?: 'ar' \| 'en'` prop, threads to FormHeader, ApprovalSection, direction, copy count (1 copy / N copies), and button labels (Print / Save PDF, Back). `FormHeader.tsx` — added `lang` prop, renders English company name `ALAMANAR ALDAWLIYA FOR STREET CONSTRUCTION & MAINTENANCE CO., W.L.L.` when `lang='en'`. Added `lang={lang}` to FormLayout in all 10 employee form pages: `SalaryCertificate`, `EmployeeWarning`, `LeaveRequest`, `PerformanceEvaluation`, `PurchaseRequest`, `Resignation`, `ReturnToWork`, `SalaryAdvance`, `ToWhomItMayConcern`, `Quotation`. `PaymentVoucherTemplate.tsx` + `ReceiptVoucherTemplate.tsx` — added `lang` prop with direction switching (LTR when `lang='en'`). `PaymentVoucher.tsx` — added `lang` state + `LanguageToggle` in `toolbarExtra`. `ReceiptVoucher.tsx` (standalone, no FormLayout) — added `lang` state, `LanguageToggle` in toolbar, threaded `lang` to `FormHeader`, `ApprovalSection`, `ReceiptVoucherTemplate`, and Back/Print button labels. **Package E — Validation:** frontend tsc ✅ 0 errors, backend tsc ✅ 0 errors, electron tsc ✅ 0 errors, `prisma validate` ✅, 1068/1068 backend tests (57 files, unchanged), `build:back` ✅, `build:front` ✅. **Gemini APPROVED ✅. 19 files changed, 243 insertions, 58 deletions. Commit `6de75c6`.** |
| **Operations & Financial Workflow Suite Phase 2** | `stable-financial-workflow-suite-phase2-v1` (pending) | Seven-phase financial print workflow, header safety, and test-hardening release. **Phase A — Cheque Auto-Save + PV Button:** `Cheques.tsx` — "طباعة الشيك" auto-saves cheque to DB before opening the print window (idempotent via `PUT /api/cheques/:id/status`); reprint is supported (button always available for `PRINTED` cheques). "طباعة سند الصرف" calls `POST /api/cheques/:id/payment-voucher-number` to get/create the PV number, then navigates to `/forms/payment-voucher/:chequeId`. PV number column added to Cheques table. **Phase B — Payment Voucher Print Form:** `PaymentVoucherTemplate.tsx` (visual design from `sanad_sarf_design1.html`): blue title box `#2b2e83`, meta row with voucher number + date, 5-row bilingual table (beneficiary / amount figures in red / amount-in-words via `tafqeetKWD` / reason / payment method with pre-checked Cheque), 3-column signatures. `PaymentVoucher.tsx` fetches cheque data; guards on missing PV number with Arabic error; `FormLayout ready={false}` (preview mode, no auto-print). Route `/forms/payment-voucher/:chequeId`. `payment-voucher` print profile added to `printProfiles.ts`. **Phase C — Receipt Voucher Stateless Form:** Completely stateless — no DB record, no Cheque link. `ReceiptVoucherTemplate.tsx` (green amount color `#1b5e20`, dynamic payment-method checkboxes, bilingual 5-row table, finance-manager signature column). `ReceiptVoucher.tsx`: fill-in form with live preview showing `---` placeholder; clicking "طباعة" validates input, calls `POST /api/forms/receipt-voucher-number` (atomic `prisma.$transaction` read-increment-write), then `useEffect([printing, rcvNumber])` fires `window.print()` after React commits the real number to DOM. Number permanently consumed even if user cancels print dialog. Route `/forms/receipt-voucher`; Forms page "سند قبض" card. **Backend sequences:** PV counter key `finance.paymentVoucher.lastSequence` in `Setting` table; RCV counter key `finance.receiptVoucher.lastSequence`. Both use `upsert` + `$transaction`; both format as 6-digit zero-padded global sequence (PV-000001, RCV-000001). PV is idempotent (cheque stores the number); RCV is not (stateless, always new). **Phase D — Bank Statement Parser Fix:** Fixed case-sensitive column matching bug in `bankStatementParser.ts` — `cell()`, `parseExcelRowsClient`, `parseCsvRowsClient` now normalize all column keys to lowercase; detection already used `normalized.includes()` (already lowercase). Analyze button now correctly enables for all supported banks regardless of export casing. **Phase E — Header Polish:** `Layout.tsx` — privacy toggle now always renders exactly ONE button (was previously rendering two simultaneously: a disabled indicator + an active re-hide button inside a fragment). `privacy.css` — removed `cursor: default` from `.privacy-btn--off` (now an active button). `layout-polish.css` — username truncation: `max-width: 140px`, `overflow: hidden`, `text-overflow: ellipsis`, `white-space: nowrap`, `display: block` on `.user-info strong/small`; `flex-shrink: 1` on `.user` to prevent pushing topbar icons. **Gemini Fixes (Phase F):** (1) `ReceiptVoucher.tsx` — `setRcvNumber('')` added at top of `handlePrint()` before `setPrinting(true)` to prevent stale-number print on second session print. (2) `cheques.service.ts` — inner `tx.cheque.findUnique()` re-read added inside `$transaction` — concurrent requests that both pass the outer null-check now have an inner guard so only the write-lock winner increments the sequence. (3) `schema.prisma` — `paymentVoucherNumber String? @unique`; migration `20260627200000_add_unique_payment_voucher_number` applies `CREATE UNIQUE INDEX`. SQLite allows multiple NULLs in UNIQUE index — existing rows unaffected. **Phase G — Tests:** `cheques/__tests__/cheques.voucher.test.ts` (12 tests: outer guard × 3, generation × 4, inner concurrency guard × 3, NaN fallback × 1, double-click × 1); `forms/__tests__/forms.service.test.ts` extended (+6 RCV tests including second-print scenario, atomicity assertion, unique-per-call). **Prisma migrations (2):** `20260627100000_add_payment_voucher_number_to_cheque` (`ALTER TABLE "cheques" ADD COLUMN "paymentVoucherNumber" TEXT`); `20260627200000_add_unique_payment_voucher_number` (`CREATE UNIQUE INDEX "cheques_paymentVoucherNumber_key"`). **24 files changed, 1435 insertions, 50 deletions. Tests: 1068/1068 backend (57 files, +20 new tests across 2 test files). prisma validate ✅, backend tsc ✅, frontend tsc ✅, build:back ✅, build:front ✅. Gemini APPROVED (focused re-review after fix pass) ✅.** |
| **Dashboard Financial Fix + Manus Audit** | `stable-dashboard-financial-fix-v1` | Bug-fix-only release. **Root Cause (Part A):** `dashboard-summary.service.ts` two `$queryRaw` tagged template literals used Prisma model names (`Invoice`, `Customer`, `Supplier`) instead of SQLite `@@map` table names (`invoices`, `customers`, `suppliers`). SQLite returned "no such table" errors; Prisma threw `PrismaClientKnownRequestError P2010`; `errorHandler.ts` catch-all for unrecognized Prisma error codes returned HTTP 400. This caused the Dashboard "مالي" tab to fail every time it loaded. **Fix:** Four table name corrections in `backend/src/shared/services/financial/dashboard-summary.service.ts` — `FROM Invoice i` → `FROM invoices i` (both queries), `JOIN Customer c` → `JOIN customers c`, `JOIN Supplier s` → `JOIN suppliers s`. **Regression Tests Added:** `backend/src/modules/financial/__tests__/financial.dashboard.test.ts` — 7 tests: valid `DashboardSummary` shape, ISO `generatedAt`, `topCustomers` row mapping, `topSuppliers` row mapping, `$queryRaw` called exactly twice, customer SQL uses `FROM invoices` + `JOIN customers` (with regex guards blocking singular model names), supplier SQL uses `FROM invoices` + `JOIN suppliers`. **Part B — Manus Invoice Linkage Audit:** Verified that `contractId` is accepted by `createInvoiceSchema` (line 26) and persisted at `invoices.service.ts` line 245. Verified that `priceId` is in `itemSchema` (line 14) and spread into `lines` via `computeTotals` → `items: { create: lines }`. Both fields exist in the Prisma schema with proper indexes and FK relations. **Conclusion: Manus audit finding is obsolete — no code changes required.** **Validation:** `prisma validate` ✅, backend tsc ✅, frontend tsc ✅, electron tsc ✅, 1050/1050 backend tests (56 files, +7 new tests in 1 new test file), build:back ✅, build:front ✅, electron:build ✅. **Gemini APPROVED ✅. 2 files changed, 107 insertions (+4 deletions in service fix).** |
| **Final Professional Polish Suite Phase 2** | `stable-final-polish-suite-phase2-v1` | 9-package UX, print, navigation, and quality audit release. **Package A — Privacy Mode:** `PrivateAmount.tsx` component with blurred masking, `usePrivacy()` Zustand store, privacy toggle button in Layout topbar, CSS `privacy.css` blur classes; applied across all financial KPI cards (Dashboard, StatCard, FinancialIntelPanel, ExecutiveIntelligenceV2Panel, AgingSummaryCards, SummaryCards, FinancialDashboardTab, Salaries); print-safe (masks screen, not print/export). **Package B — Inline Text Editing in Template Studio:** Added inline text editing capability to `TemplateStudioEditor.tsx`; implemented in a prior session. **Package C — English Administrative Forms Completion:** All hardcoded Arabic strings replaced with `useT()` + `t()` across `Forms.tsx` and `EmployeeWarning.tsx`; added `page.forms.*`, `page.warning.*`, `msg.error` i18n keys with full AR/EN coverage. **Package D — Print Engine Final Polish:** `usePrintProfileMemory` hook — localStorage persistence of print profile + copies per `formType`; copies control `[− N نسخة +]` in FormLayout toolbar; `@page` CSS fixed using actual profile margins; maxWidth 793px (A4 at 96dpi); `.form-page-footer { page-break-inside: avoid }`; all 10 form pages updated. **Package E — QR Verification:** Public `/verify/:uuid` route (no auth); `DocumentVerify.tsx` calls backend without JWT; verification backend route made public (removed `authenticate` middleware). **Package F — Statement Navigation Cleanup:** Duplicate "Statements" sidebar removed; `/statements` redirects to `/financial?tab=statement`; `nav.financial` i18n key added. **Package G — DataTable Final Polish:** New Column props (`truncate`, `multiline`, `width`); new DataTable props (`expandRow`, `onRowClick`); `SkeletonRows` with shimmer animation; expand row toggle `▸/▾`; truncation with native tooltip; column visibility reset badge; empty states 🔍 (filtered) and 📋 (empty); 5 i18n keys added. **Package H — Validation & Error UX:** `errorMessage()` enhanced with Arabic network/HTTP error messages (ECONNREFUSED, ERR_NETWORK, ECONNABORTED, HTTP 400/403/404/408/409/413/422/429/500/503); `AttachmentsPanel.tsx` — all 3 `alert()` calls replaced with inline dismissible `InlineError` component; load errors also shown inline. **Package I — Contract Translation Dictionary:** `BASE_NATIONALITY_EN` + `BASE_JOB_TITLE_EN` exported from `contractTranslations.ts`; `applyTranslationOverrides(nat, job)` runtime override support; Settings page "قاموس الترجمة" section with tabbed AR→EN dictionary editor (add/edit/delete rows, saves to `dict.nationalities` + `dict.jobTitles` Settings keys); `EmploymentContract.tsx` loads overrides on mount. **Package J — Technical Audit:** Fixed confirmed bug in `contracts.repository.ts` `financials()` — CANCELLED expenses were included in contract profitability (`status: { not: 'REJECTED' }` → `status: { notIn: ['REJECTED', 'CANCELLED'] }`); 6 regression tests added. All other flows verified correct (invoice totals, payment transitions, collection rates, dashboard KPIs). **Security:** QR verification endpoint reviewed — no PII or financial data exposed (only documentType/number/status/dates/UUID); UUID-based, no sequential enumeration; screen privacy masking does not affect print/export. **Validation:** prisma validate ✅, backend tsc ✅, frontend tsc ✅, electron tsc ✅, 1043/1043 backend tests (55 files, +6 new regression tests), build:back ✅, build:front ✅, electron:build ✅. **Gemini APPROVED ✅. 42 files changed, 1415 insertions, 836 deletions.** **Roadmap note:** Theme System explicitly excluded and not planned. Remaining future items: Daily Trip Log, Cheque workflow + Payment Voucher, Receipt Voucher / Payment Voucher templates, Bank statement data visibility issue. |
| **Operations & Management Suite Phase 1** | `stable-ops-management-suite-phase1-v1` | Six-phase enterprise management suite (Print Polish + UI Polish + Document Expiration Center + Backup Verification + Attachments Center + Financial Operations Dashboard). **Phase A — Print & Forms Polish:** English-language branches for `EmploymentContractTemplate.tsx`, `PurchaseRequestTemplate.tsx`, `QuotationTemplate.tsx`; shared utilities expanded (`NATIONALITY_EN`, `JOB_TITLE_EN`, `getPriorityEn`, `getPriorityLabelEn`, `longTextCell`); bilingual `ApprovalSection` (`lang?: 'ar' \| 'en'`); three-party approval grid restored in PurchaseRequest. **Phase F — UI Polish (frontend-only):** `useShortcut(key, handler, options?)` keyboard hook (`frontend/src/utils/shortcuts.ts`); `TruncatedText.tsx` component (40-char default, `title` tooltip); `DataTable.tsx` column visibility toggle ("الأعمدة ▾" dropdown with outside-click handler, `hiddenColumns?` + `onColumnVisibilityChange?` props); `FormDialog.tsx` inline required-field validation (red border + per-field Arabic error text, clears on input change). **Phase B — Document Expiration Center:** New `expirations` backend module — `list()`, `summary()`, `exportExcel()`; 7 document categories; urgency classification (expired <0d, critical 0–7d, soon-30 8–30d, soon-60 31–60d, soon-90 61–90d, ok >90d); Prisma field `Equipment.insuranceExpiry DateTime? @@index` + migration `add_equipment_insurance_expiry`; permissions `expirations.read/export`; 4 backend tests. New frontend page `DocumentExpirationCenter.tsx` (summary cards, filter bar, table, export button); `ExpirationWidget.tsx` dashboard widget integrated into `Dashboard.tsx`. **Phase C — Backup Verification:** SHA-256 checksum + SQLite header integrity check; 4 new nullable fields on `Backup` model (`checksumSha256`, `verifiedAt`, `verificationStatus`, `verificationNote`) + migration `add_backup_verification_fields`; `computeChecksum()` + `verify()` in `backup.service.ts`; `POST /api/backups/:id/verify` endpoint (`backups.read` permission + audit log); `BackupRecord` typed interface (12 fields) in `Backup.tsx`; verification status badges + Verify button; 3 backend tests. **Phase E — Attachments Center:** New `attachments` backend module; multer diskStorage (`<uuid>-<safeOriginalName>` filenames), `ATTACHMENTS_DIR` env var, `ALLOWED` entity type set (`CUSTOMER/CONTRACT/INVOICE/EMPLOYEE/SUPPLIER/EXPENSE/EQUIPMENT`), `path.resolve` boundary validation; new npm dep `multer@^2.2.0` + `@types/multer`; `Attachment` Prisma model + migration `add_attachment`; entity-module permission checks on GET/POST/DELETE (POST upload cleans temp file on 403); 2 new Electron IPC channels (`attachments:openFileDialog`, `attachments:openPath`) — `openPath` validates path within `ATTACHMENTS_DIR`, allowed extension, regular-file check; new preload: `window.manar.openFileDialog()`, `window.manar.openAttachment(path)`; reusable `AttachmentsPanel.tsx`; embedded in `Invoices.tsx` + `Expenses.tsx`. **Phase D — Financial Operations Dashboard:** New `FinancialOperationsDashboard.tsx` page (4 tabs: ربحية العقود / تحليل المصروفات / تحليل العملاء / الاتجاهات); 3 new GET routes in `executive.routes.ts` (`/contract-profitability`, `/expense-breakdown`, `/customer-analytics`, all `financialdashboard.read`); `financial-exec.service.ts` with parallel `Promise.all` queries; Recharts `BarChart` in expense breakdown tab; margin % color-coded; 12 backend tests. **Security fixes (4):** (1) `attachments:openPath` IPC — path within `ATTACHMENTS_DIR`, allowed extension, regular-file validation; (2) `attachments.delete` removed from `PROJECT_MANAGER` seed; (3) POST upload route adds entity-module write permission check with temp-file cleanup on 403; (4) Multer errors return HTTP 400 with Arabic messages (`MulterError` branch + `AppError.badRequest` in fileFilter/destination). **Prisma migrations (3):** `add_equipment_insurance_expiry`, `add_backup_verification_fields`, `add_attachment`. **49 files changed, 2782 insertions, 120 deletions. Tests: 1037/1037 backend (54 files, +19 new tests across 3 test files), TypeScript 0 errors (backend/frontend/electron), builds clean. Gemini APPROVED ✅.** |
| **Integrations Center Phase 1 — Foundation** | `stable-integrations-center-phase1-v1` | Foundation-only integration registry and management center. **No external connections in Phase 1 — offline-first.** **Architecture:** Static `INTEGRATION_REGISTRY` (6 entries) + runtime settings overlay via existing `Setting` table (key pattern `integrations.<id>.<field>`). **Backend — new module `integrations/` (7 files):** `integrations.types.ts` (5 TypeScript interfaces: `IntegrationDefinition`, `IntegrationCard`, `IntegrationHealth`, `IntegrationSettingsUpdate`, `IntegrationRunResult`); `integrations.registry.ts` (static registry: `payroll-bank-import` `available/stable`, `bank-statement-import` `planned`, `bank-reconciliation` `planned`, `cloud-backup` `planned`, `enhanced-excel-import` `available/stable`, `connector-sdk` `comingSoon`; `findIntegration(id)` helper); `integrations.schema.ts` (Zod: `enabled?: boolean, notes?: string(max 500)`); `integrations.service.ts` (`list()`, `getById()`, `updateSettings()`, `run()`; single `findMany` query, `buildCard()` overlay, `deriveHealth()`); `integrations.controller.ts`; `integrations.routes.ts` (`authenticate` + `requirePermission` per route); `__tests__/integrations.service.test.ts` (21 tests across 5 groups). **Endpoints:** `GET /api/integrations` (`integrations.read`), `GET /api/integrations/:id` (`integrations.read`), `PUT /api/integrations/:id/settings` (`integrations.configure`), `POST /api/integrations/:id/run` (`integrations.run`). **Run behavior:** Phase 1 always returns `{ success: false, status: 'not_implemented', messageAr: 'هذا التكامل لم يتم تفعيله بعد' }` + writes `AuditLog`. **Settings:** existing `Setting` table; `available` integrations default `enabled=true`; `planned`/`comingSoon` default `enabled=false`; no secrets/tokens/credentials. **Permissions added:** `integrations.read` (SYSTEM_ADMIN, GENERAL_MANAGER, ACCOUNTANT, PROJECT_MANAGER), `integrations.configure` (SYSTEM_ADMIN), `integrations.run` (SYSTEM_ADMIN, ACCOUNTANT). **constants.ts:** `'integrations'` added to MODULES; `'configure'` and `'run'` added to ACTIONS. **Frontend:** `frontend/src/api/integrations.ts` (typed API client — 4 methods); `frontend/src/pages/Integrations.tsx` (page: cards by category, status/maturity badges, health indicator, capabilities, inline SettingsPanel modal, RunResultDialog modal, RBAC-gated buttons — `إعدادات` requires `configure`, `تشغيل` requires `run`, planned integrations show `قريباً`); route `/integrations` in `App.tsx`; nav entry (`hub` icon, `integrations.read` permission) in `modules.tsx` + `i18n.ts`. **Intentionally NOT implemented:** bank reconciliation, cloud backup, external APIs, OAuth, secret storage, new Prisma models, migrations. **Tests: 852/852 backend (49 files, +21 new tests) + 588/588 frontend (28 files, unchanged). Gemini APPROVED ✅. 15 files changed (9 new, 6 modified), 1281 insertions.** |
| **Approval Workflow Pack v1 — Phase A (Universal Approval Engine Foundation)** | `stable-approval-workflow-pack-v1-phase-a` | Foundation-only: reusable approval infrastructure. **No module integration in Phase A** — zero changes to existing Expenses, Payroll, or Invoice workflows. **Backend — Schema:** `ApprovalHistory` table (polymorphic: `entityType` + `entityId`, 3 indexes — `[entityType,entityId]`, `[userId]`, `[createdAt]`). **Backend — Types (`approval.types.ts`):** 9 TypeScript interfaces/types — `ApprovalModuleConfig<TEntity>` (with optional `historyPermission?: string`), `ApprovalTransition`, `ApprovalMeta`, `ApprovalSideEffect<TEntity>`, `TransitionInput`, `ApprovalHistoryEntry`, `TransitionResult<TEntity>`, `ApprovalTransitionEvent`, `ApprovalTransitionListener`. **Backend — Engine (`approval.service.ts`):** `ApprovalEngine` class exported as `approvalEngine` singleton. Registry/composition pattern: `register(config)` stores `ApprovalModuleConfig`; `transition(input, tx?)` validates entity/action/fromStatus, checks permissions (`assertPermission` — SYSTEM_ADMIN bypasses), runs status update + `ApprovalHistory` write + `AuditLog` fire-and-forget in ONE `prisma.$transaction`; side-effect hooks run inside same transaction and propagate errors for rollback; `getHistory(entityType, entityId)` → `ApprovalHistoryEntry[]`; `onTransition(listener)` → notification array (zero listeners in Phase A); `hasModule(entityType): boolean` + `getHistoryPermission(entityType): string | undefined` (minimal exposure, no full config leaked). **Backend — Endpoint:** `GET /api/approval-history/:entityType/:entityId` — `authenticate` only; controller validates params, rejects unregistered `entityType` with HTTP 400 + `نوع الكيان غير معروف`, enforces `config.historyPermission` (if set) with HTTP 403, SYSTEM_ADMIN bypasses. Since zero modules register in Phase A, all calls return 400 — no history data exposed. **Backend — Tests:** 26 unit tests for `ApprovalEngine` (17 core: unknown module, unknown action, invalid fromStatus, missing permission, SYSTEM_ADMIN bypass, happy path, side-effect propagation, getHistory; + 4 new: hasModule/getHistoryPermission coverage) + 5 controller tests (unregistered→400, registered no permission→allow, registered with permission user lacks→403, registered with permission user has→allow, SYSTEM_ADMIN bypass→allow). **Backend — constants.ts:** added `'submit'` and `'reopen'` to ACTIONS array; `ActionName` union updated. **Frontend — API:** `approvalHistoryApi.getHistory(entityType, entityId)` → `GET /api/approval-history/:entityType/:entityId`. **Frontend — Components (4, all standalone, not wired to any page):** `ApprovalBadge` — status chip, 11 Arabic status labels (DRAFT/PENDING/SUBMITTED/APPROVED/REJECTED/CANCELLED/REVERSED/PAID/UNPAID/PARTIAL/OVERDUE), custom `statusMap` prop; `ApprovalTimeline` — timeline list of `ApprovalHistoryEntry[]`, colored action icons, relative timestamps in Arabic; `ApprovalActions` — two-stage button group (click → confirm dialog with optional comment textarea → execute), supports `requireComment`; `ApprovalHistoryPanel` — composite: fetches history, renders Timeline + optional Actions panel, cancellation guard on initial fetch, refresh-after-action. **Frontend — Barrel:** `frontend/src/components/approval/index.ts` exports all 4 components + `ApprovalTransitionDef` + `ApprovalHistoryEntry`. **Security hardening:** IDOR vulnerability closed — `hasModule()` guard + optional `historyPermission` field on `ApprovalModuleConfig`. Phase B integration point: set `historyPermission: 'module.view'` in `register(config)` to enforce per-module permission on history access. 16 files changed (7 new backend files, 3 modified backend files, 6 new frontend files). 1322 insertions. No frontend pages modified. No Electron IPC changes. No new npm packages. **Tests: 617/617 backend (35 files — +26 tests across 2 new test files) + 507/507 frontend (24 files, unchanged).** |
| **UX Polish Pack v4** | `stable-ux-polish-pack-v4` | Global UX hardening — frontend-only release. **ConfirmModal system:** `ConfirmModal.tsx` async confirmation modal replaces all `window.confirm()` calls across Inventory.tsx (9 calls / 5 sub-tabs), Cheques.tsx (2), TemplateStudioEditor.tsx (1), Attendance.tsx (2), and 10 print-form pages (ToWhomItMayConcern, LeaveRequest, SalaryCertificate, EmployeeWarning, Resignation, PerformanceEvaluation, SalaryAdvance, ReturnToWork, Quotation, PurchaseRequest). **Toast notification system:** `toastStore.ts` (Zustand) + `Toast.tsx` (fixed-position container, mounted in Layout.tsx). `useToast()` provides `toast.ok/error/warn()` with 4-second auto-dismiss. Replaced all `const [msg, setMsg]` inline alert patterns across: Users.tsx, Prices.tsx, Attendance.tsx, Expenses.tsx, Invoices.tsx, Accounting.tsx (2 tabs), Inventory.tsx (5 tabs), Backup.tsx (3-variant showMsg pattern), Settings.tsx. **Modal hardening:** `Modal.tsx` — added `size?: 'sm'|'md'|'lg'|'xl'` prop, focus trap, scroll lock, Escape key handler, focus restoration on close. **Modal sizes standardized** across Invoices (xl/lg), Inventory (xl/lg), Accounting (xl/lg), Attendance (lg), Maintenance (lg), Expenses (lg), Cheques (lg), Prices (xl/lg), Users (lg). **DataTable a11y:** `aria-busy={!!loading}` on table, `aria-live="polite"` on tbody, `className="th-actions"`/`"td-actions"` with `position:sticky; inset-inline-end:0` for sticky action column. **Button hardening:** `type="button"` on all non-submit buttons in form contexts; `:focus-visible` focus rings; `.btn.loading` class; `.btn:hover:not(:disabled)` guard. **CSS/RTL improvements:** `.user-info { text-align: start }`, `.field label.required::after`, `alert-close-btn`, `.skip-to-content`, `.spinner-sm`, dark-mode modal overlay. **Scope:** 32 files changed (29 modified + 3 new), 787 insertions, 364 deletions. No Prisma schema changes. No migrations. No backend API changes. No Electron IPC changes. No new npm packages. |
| **Executive Decision Center — Phase 1** | `stable-executive-decision-center-phase1-v1` | مركز القرار التنفيذي — 7 integrated features: Financial Summary (8 KPI cards, top debtors, this/last month comparison), Decision Cards (8 intelligent insight cards by priority), Executive Alerts V3 (9 alert types, HIGH/MEDIUM/LOW, deduplicated, sorted), KPI Timeline (1m/3m/6m/12m Recharts area charts), Company Health Score (0–100, 6 weighted components, EXCELLENT/GOOD/WATCH/RISK), Executive Recommendations V2 (rule-based, max 10), PDF export via existing `printToPDF` Electron IPC. New `/api/executive` module reusing `dashboard.read` permission. 30 new Vitest tests. No schema changes, no migrations, no new packages. |
| **Executive Intelligence Bundle — Phase 2** | `stable-executive-intelligence-bundle-phase2-v1` | `ExecutiveIntelligenceV2Panel`: KPI comparison, monthly revenue trend, contract health tracker, smart recommendations engine, forecast chart, alerts panel. Dashboard flicker fix. |
| **Executive Intelligence Bundle — Phase 1** | `stable-executive-intelligence-bundle-phase1-v1` | Executive KPI cards, alerts, financial forecast, dashboard foundation for Phase 2. |
| **Stabilization Audit Sprint 1** | `stable-stabilization-audit-sprint1-v1` | 20 critical/high/medium production fixes across multiple modules. |
| **Critical Hardening Sprint 1** | `stable-critical-hardening-sprint1-v1` | Security hardening, stability fixes, print and reports improvements. |
| **UI Typography Refresh v1** | `stable-ui-typography-refresh-v1` | IBM Plex Sans Arabic as primary UI font. Tajawal as second fallback. Local WOFF2 assets, no CDN. Print/form pages retain Cairo. |
| **Ultimate Professional UX Pack** | `stable-ultimate-professional-ux-pack-v1` | Employment Contract column layout fix. 5 HR form template field additions. ResourcePage sort/filter/pagination. Cheques `tafqeet` integration. `BankSalaryAnalytics` page. Dashboard stabilization. |
| **Auto Backup — Phase 1** | `stable-auto-backup-phase1-v1` | Daily node-cron scheduler in Electron main. WAL-safe copy. Retention policy (default 30 AUTO). Startup catch-up. `LastAutoBackupCard` dashboard widget. Settings controls. |
| **Page-Level Improvements — Phase 1** | *(merged)* | Persisted search/filter/page state across ResourcePage instances. Refresh actions. |
| **Inventory Module** | *(stable)* | Material tracking, issue management. |
| **Data Import** | *(stable)* | 7 entities: employees, customers, equipment, suppliers, project prices, contracts, expenses. FK code resolution. |
| **Audit Log** | *(stable)* | Server-side paginated `GET /api/audit`. Filter bar. Details drawer. |
| **Reports & Export Center** | *(stable)* | Excel/PDF export. Attendance, payroll, financial reports. |
| **Roles & Permissions (RBAC)** | *(stable)* | Role-based access. Permission keys format `<module>.<action>`. `SYSTEM_ADMIN` bypasses. |
| **Backup & Restore** | *(stable)* | Manual backup/restore via Electron IPC. Auto-backup via node-cron. |
| **Authentication** | *(stable)* | JWT (12h). bcrypt passwords. `authenticate` + `requirePermission` middleware. |
