# سياق المشروع للمتابعة (Handoff) — نظام إدارة أعمال شركة المنار

> هذه الوثيقة تُعطى لأي مساعد ذكاء اصطناعي (مثل Google Gemini) ليكمل العمل من حيث توقّفنا. اقرأها أولًا، ثم اقرأ `README.md` ووثائق `docs/`.

## 1. ما هو المشروع
تطبيق سطح مكتب لويندوز يعمل **محليًا بالكامل (Offline)** لإدارة أعمال شركة مقاولات طرق (نقل أسفلت) في **الكويت**. العملة **دينار كويتي (KWD)** و**لا يوجد نظام ضريبي**.

## 2. المجموعة التقنية (Stack)
- **الخلفية:** Node.js + Express + TypeScript + Prisma ORM + SQLite. (مجلد `backend/`)
- **الواجهة:** React 18 + TypeScript + Vite + Zustand + Chart.js. (مجلد `frontend/`)
- **سطح المكتب:** Electron (يشغّل الخلفية داخليًا). (مجلد `electron/`)
- **المثبّت:** electron-builder (`electron-builder.yml`).
- المعمارية: Clean Architecture + Repository Pattern + RBAC. التفاصيل في `docs/01-معمارية-النظام.md`.

## 3. كيف يُشغَّل (مرجع سريع)
```bash
# أول مرة
npm install
cd backend && copy .env.example .env
npm run prisma:generate && npm run prisma:migrate && npm run prisma:seed && cd ..
# التشغيل
npm run dev          # يفتح نافذة التطبيق (admin / Admin@123)
# المثبّت
npm run dist         # ينتج release\ManarSetup-1.0.0.exe
```

## 4. ما أُنجز بالكامل

- **UI Consistency Pack — Phase 1: Buttons & Action Bars (2026-06-17) — الوسم `stable-ui-consistency-phase1-buttons-v1`:**
  - **ExportExcelButton:** نص التحميل `'...'` → `'جاري...'` (عربي موحّد)
  - **ResourcePage + Prices:** ترتيب page-head صُحح — زر الإضافة قبل التصدير (إضافة → تصدير)
  - **Reports:** حُذفت 3 `style={{ padding }}` مخصصة من أزرار عرض/إعادة تعيين/طباعة
  - **Invoices:** `type="button"` على زري collect/cancel + حذف inline style من زر الحذف النهائي
  - **Salaries:** `type="button"` على 10 أزرار (generate/save/add-line + 6 row-action)
  - **Users:** `type="button"` على 5 أزرار (create/tabs/edit/toggle/role-expand)
  - **Cheques:** تصحيح ترتيب className (`btn sm secondary` → `btn secondary sm`) + close-alert buttons من inline styles إلى `.alert-close-btn`
  - **theme.css:** إضافة `.alert-close-btn` utility class
  - **لا Migration — لا Permissions جديدة — لا Breaking Changes**
  - **التحقق:** tsc ×3 نظيف، prisma validate ✅، **349/349 اختبار**. Quality Gate: 5/5 ✅

- **Expenses Enhancement Pack — Phase A (2026-06-17) — الوسم `stable-expenses-enhancement-phase-a-v1`:**
  - **نموذج المصروفات:** أُضيف حقل `paymentMethod` (نقداً / تحويل بنكي / ذمم الموردين) كان موجوداً في DB لكن غائباً عن الواجهة
  - **STATUS_PILL:** اكتمل لـ 5 حالات (PENDING/APPROVED/REJECTED/REVERSED/CANCELLED) — كانت REVERSED/CANCELLED تظهر كـ `—`
  - **فلتر الحالة:** أُضيفت REVERSED و CANCELLED للـ dropdown
  - **إصلاح amount:** initial state = `''` بدلاً من `0` لتجنب عرض صفر مبدئي
  - **بحث موسّع:** يبحث في code + description + supplierName (كان يبحث في description فقط)
  - **إصلاح supplierName cast** في `update()` — كان غير آمن
  - **إصلاح stats():** حذف استدعاء `.filter()` مكرر لـ pendingRows
  - **تقرير Excel:** فئات بالعربية (وقود/رواتب/...)، حالات بالعربية، عمود المورد، `numFmt: '#,##0.000'` (KWD)
  - **schema.prisma:** `@@index([status, date])` composite index
  - **Migration:** `20260617200000_add_expense_status_date_index` — `CREATE INDEX` فقط، آمنة تمامًا
  - **+40 اختبارًا:** `expenses.schema.test.ts` (25) + `expenses.service.test.ts` (15) — يغطيان: الفئات الـ14، paymentMethod، بيلينغ الشهر/السنة، حراس الحالة في update/approve/remove/cancel/reject
  - **التحقق:** tsc ×3 نظيف، prisma validate ✅، **349/349 اختبار** (21 ملف). **Quality Gate: 5/5 ✅**

- **Customer Quality Pack v1 (2026-06-17) — الوسم `stable-customer-quality-pack-v1`:**
  - **FormDialog:** أضاف `placeholder?: string` لـ `FormField` interface — يُمرَّر لـ `<input>` و`<textarea>` في جميع النماذج
  - **نموذج العملاء:** placeholders عربية/إنجليزية لـ 7 حقول (code/name/phone/email/contactName/address)
  - **customers.service:** حذف استعلام `findMany` مكرر في `forceRemove` — `getChildCounts` تُعيد `contractIds` مباشرةً
  - **schema.prisma:** `@@index([isArchived, type])` على جدول customers (composite index للاستعلام الشائع)
  - **Migration:** `20260617190000_add_customer_composite_index` — `CREATE INDEX` فقط، آمنة تمامًا
  - **+28 اختبارًا:** `customers.schema.test.ts` (18) + `customers.service.test.ts` (10) — يغطيان: الحقول المطلوبة، النوع الافتراضي PRIVATE، صحة البريد، duplicate code على الإنشاء والتعديل، statusCode 409، notFound
  - **التحقق:** tsc ×3 نظيف، prisma validate ✅، 309/309 اختبار. **Gemini APPROVED.**

- **Export Standardization Pack (2026-06-17) — الوسم `stable-export-standardization-pack-v1`:**
  - **ExportExcelButton component:** زر تصدير موحّد (`⬇ تصدير Excel` / loading: `...` / class: `btn secondary`) يُستخدم في جميع الصفحات
  - **exportUtils.ts:** `downloadBlob()` + `downloadXlsx()` — حذف 6-10 أسطر boilerplate من كل صفحة
  - **Invoices.tsx:** أضاف عمودَي `تاريخ الإصدار` + `الحالة` للتصدير (كانا مفقودَين مقارنةً بجدول الواجهة)
  - **الصفحات المُوحَّدة:** Invoices + Expenses + ResourcePage + Prices + Salaries + Reports — جميعها تستخدم المكوّن والأدوات المشتركة
  - **Backend prices report:** أضاف عمود `العميل` + فلتر `isArchived: false` (كان العمود مفقوداً رغم ظهوره في UI)
  - **التحقق:** tsc ×3 نظيف، prisma validate ✅، 281/281 اختبار. Gemini APPROVED.

- **Invoice Customer Price Filter (2026-06-17) — الوسم `stable-invoice-customer-price-filter-v1`:**
  - **Endpoint جديد:** `GET /prices/for-invoice?customerId=X` — فلتر صارم (`customerId = X AND isArchived = false`)، لا OR مع null، لا أسعار عملاء آخرين
  - **Backend validation آمن:** `customerId` يُرفض عند NaN / 0 / سالب / غير صحيح → يرجع `[]` بأمان دون الوصول إلى Prisma
  - **Frontend (إنشاء + تعديل الفاتورة):**
    - يستخدم الـ endpoint الجديد بدلاً من `/prices?customerId=X`
    - رسالة توجيهية «اختر العميل أولاً لعرض اتفاقيات أسعاره» عند عدم اختيار عميل
    - رسالة Empty State واضحة «لا توجد اتفاقيات أسعار مسجّلة لهذا العميل»
    - **إصلاح تسرب الأسعار:** عند تغيير العميل تُمسح جميع الأسعار (بما فيها المعبأة تلقائياً بتطابق وحدة واحدة) لا فقط المحددة بالـ picker
  - **7 اختبارات جديدة:** exact query shape، no OR-null leak، controller validation chain، 281/281 إجمالاً
  - **التحقق:** tsc ×3 نظيف، prisma validate ✅، 281/281 اختبار. Gemini APPROVED.

- **Operations & Workflow Pack — Phase E (2026-06-17) — الوسم `stable-operations-workflow-pack-v1`:**
  - **Expenses UX Complete:**
    - `Expense.supplierName String?` — حقل نصي حر للموردين غير المسجّلين في النظام (migration + schema + service + UI)
    - خيار «مورد آخر (غير مسجّل)» في نموذج المصروف → حقل نص ظاهر عند الاختيار
    - شريط KPIs: شريحة «المعلّق» بمبلغه + تفصيل `byCompanyGroup` (حسن / غانم / نظير / هارون / عمليات)
    - عمود المورد يعرض `supplierName` عند غياب مورد مسجّل
  - **Agreements Dashboard:**
    - `ProjectPrice.validUntil DateTime?` — تاريخ انتهاء الاتفاقية (migration + schema + service + UI)
    - `GET /prices/agreements-dashboard` — غير مستخدمة / تنتهي خلال 30 يومًا / الأعلى استخدامًا (top5) / الأقل استخدامًا (least5)
    - لوحة dashboard مدمجة في صفحة الأسعار مع 4 تبويبات
    - حقل `صالح حتى` في نموذج إنشاء/تعديل الاتفاقية
  - **Operational Dashboard:**
    - `GET /dashboard/operational` — عدد/مبلغ المصاريف المعلّقة، مسيرات مسودة، شيكات غير مطبوعة، فواتير مستحقة، اتفاقيات تنتهي قريبًا
    - قسم «عمليات معلّقة» في Dashboard بـ widgets قابلة للنقر (يظهر فقط إذا كانت الأعداد > 0)
  - **Invoice Preview:**
    - منطقة توقيع 3 أعمدة: ختم العميل / ممثل الشركة / المحاسب
    - ظاهرة على الشاشة والطباعة، في أسفل الفاتورة بعد الملاحظات
  - **Operational Reports:**
    - مفاتيح فلتر جديدة: `billingMonth`, `billingYear`, `company`, `workType`
    - مجموعة تقارير «التشغيلية» مع 3 أنواع: المصروفات حسب الشركة، الفواتير حسب العميل، استخدام الاتفاقيات
    - شريط فلاتر كامل + زر إعادة تعيين + تصدير Excel/طباعة
  - **التحقق:** 274/274 اختبار، tsc ×3 نظيف، prisma validate ✅، build:back + build:front ✅. Gemini APPROVED.

- **Unified Financial Completion Pack — Phase D (2026-06-17) — الوسم `stable-unified-financial-completion-pack-v1`:**
  - **Shared GL Helpers:** `gl.service.ts` — `createBalancedJournal`, `reverseGL`, `checkDuplicatePosting`, `generateEntryNumber`, `GL_REFERENCE_TYPES`, `round3`. جميع وحدات GL تستخدم هذه المساعدات.
  - **Payroll GL:** `postPayrollToGL()` → Dr مصروف الرواتب (5100) Cr صندوق/بنك/رواتب مستحقة (1000/1010/2100). `reversePayrollGL()`. مستدعاة في `markPaid()`.
  - **Purchase Invoice GL:** `postPurchaseInvoiceToGL()` → Dr مشتريات (5000) Cr صندوق/بنك/ذمم موردين (1000/1010/2000). `reversePurchaseInvoiceGL()`. حقل `Invoice.paymentMethod` + migration.
  - **Payment Method Standardization:** `ENUMS.glPaymentMethod = ['CASH','BANK','ACCOUNTS_PAYABLE']` موحّد لـ Expenses + Payroll + Purchase Invoice. الـ `paymentMethod` القديم للمبيعات `['CASH','BANK','CHEQUE','TRANSFER']` محفوظ.
  - **Financial Integrity:** حارس ترحيل مزدوج، قيود متوازنة، عكس آمن، لا حذف للقيود القديمة.
  - **الاختبارات:** 274/274 (زيادة +34). Gemini APPROVED.

- **Financial Integrity Pack — Phase C1 + C2 (2026-06-17) — الوسم `stable-financial-integrity-pack-v1`:**
  - تصحيح Dashboard: `status: 'APPROVED'` filter + `referenceType: { notIn: AUTO_REFERENCE_TYPES }` في P&L. Gemini APPROVED.

- **الخلفية (17 وحدة):** المصادقة، المستخدمون والأدوار (RBAC 7 أدوار)، العملاء، **العقود** (نقل أسفلت: رقم العقد/مصنع الأسفلت/المكان/قيمة النقل الشهري)، **المعدّات** (رقم/نوع/مالك/سائق/لوحة/انتهاء دفتر المركبة + تنبيه 30 يوم/الحالة تعمل-لا تعمل)، الصيانة، **الموظفون** (اسم عربي/إنجليزي، الرقم المدني، المهنة، الجنسية، الجواز+انتهاؤه، الإقامة+انتهاؤها، رخصة القيادة، رخصة المركبة، لوحة المركبة، الميلاد، الشركة، العنوان، الراتب، الحالة + تنبيهات انتهاء المستندات)، الفواتير (بنود/دفعات/قيد محاسبي)، المصروفات (اعتماد)، الموردون، القيود المحاسبية، **الرواتب** (`SalaryPayment` + ملخص شهري محسوب)، لوحة التحكم، التقارير، النسخ الاحتياطي، الإعدادات، سجل التدقيق.
- **الواجهة:** صفحات لكل ما سبق + لوحة تحكم بالرسوم + صفحة رواتب بفلتر باسم الموظف + صفحة طباعة تقارير بعربية سليمة (`/print/:type`).
- **حزمة Business Alignment Pack (2026-06-16) — الوسم `stable-business-alignment-pack-v1`:**
  - **Pack A:** تصحيح تسميات اتجاه الفاتورة («نقليات عميل» / «مشتريات مورد»)، تخصيص نوع العميل الافتراضي «خاص» عند الإنشاء، تحسين رسائل تعارض الأرقام في العملاء/العقود/الموظفين/المعدّات/الشيكات، تحديث تقرير الفواتير Excel (شهر الحساب + المتبقي)، تحسين طباعة الفاتورة (مسافة التوقيع).
  - **Pack B:** 6 فئات مصروفات جديدة (`EQUIPMENT_RENT, TRUCK_RENT, HASSAN, GHANEM, NATHEER, HAROON`)، migration لجدول المصروفات (`billingMonth, billingYear, notes`)، صفحة مصروفات مخصصة (`Expenses.tsx`) تضم: شريط KPIs، 6 فلاتر، جدول بيانات، نموذج إنشاء/تعديل، تصدير Excel.
- **أدوات استيراد Excel** (مجلد `backend/scripts/`): عامة مدفوعة بإعدادات `import-config.ts`، تدعم: `employees`, `equipment`, `contracts`, `customers`, `salaries`. تكتشف ورقة العمل وصف العناوين تلقائيًا. الأمر: `npm run import -- <الوحدة> "ملف.xlsx"` والقوالب: `npm run template`.
- **بيانات حقيقية مُستوردة:** 26 موظفًا + 212 عملية رواتب (من ملفات المستخدم).

## 5. اصطلاحات مهمة يجب اتباعها
- **بنية كل وحدة خلفية:** مجلد تحت `backend/src/modules/<name>/` فيه: `*.routes.ts` (مسارات) → `*.controller.ts` → `*.service.ts` (منطق الأعمال) → `*.repository.ts` (Prisma) → `*.schema.ts` (تحقق Zod). تُسجَّل الراوترات في `backend/src/app.ts`.
- **الصلاحيات:** كل مسار محمي بـ `authenticate` + `requirePermission('module.action')`. مدير النظام يتجاوز كل الفحوص.
- **الواجهة مدفوعة بإعدادات:** معظم الصفحات تُعرَّف في `frontend/src/config/modules.tsx` (أعمدة الجدول + حقول النموذج) وتُعرض عبر `frontend/src/pages/ResourcePage.tsx`. الصفحات الخاصة (Dashboard, Invoices, Salaries, Accounting, Reports, Settings, Backup) منفصلة.
- **القائمة الجانبية والراوتر:** تُضاف الوحدة في `NAV` داخل `modules.tsx` + مسار في `frontend/src/App.tsx`.
- **عميل الـ API:** `frontend/src/api/client.ts` (Axios، يضيف رمز الجلسة). الاستجابات بصيغة `{ success, data }`؛ القوائم `{ data: { data:[], meta:{} } }`.
- **العملة:** دالة `money()` في `modules.tsx` (KWD، حتى 3 خانات). لا ضريبة (taxRate=0 دائمًا).
- **PDF العربي:** لا تستخدم مولّد PDFKit للعربية (يكسر الحروف). استخدم صفحة الطباعة `/print/:type` (Chromium) أو تصدير Excel.
- **بعد أي تعديل في `schema.prisma`:** نفّذ `npx prisma migrate dev --name <اسم>` ثم `npx prisma generate` من `backend/`.

## 6. مهمة قيد التنفيذ (لم تكتمل) — تخصيص الفاتورة لنقل الأسفلت
المطلوب من المستخدم: في نافذة «فاتورة جديدة»:
1. إضافة **نوع الخدمة** للفاتورة بقيم: «نقل اسفلت»، «يومية عمل مالينج»، «يومية نقل اسفلت».
2. إضافة **وحدة الكمية** لكل بند بقيم: «طن»، «درب»، «يومية».
3. إبقاء **السعر** مع حساب الإجمالي تلقائيًا (الكمية × السعر).

**الخطوات المتبقية لإكمالها:**
- `backend/prisma/schema.prisma`: أضف `serviceType String?` إلى موديل `Invoice`، و`unit String?` إلى موديل `InvoiceItem`. ثم migration.
- `backend/src/modules/invoices/invoices.schema.ts`: أضف `serviceType` (اختياري) لمخطط الإنشاء/التعديل، و`unit` (اختياري) لمخطط البند `itemSchema`.
- `backend/src/modules/invoices/invoices.service.ts`: خزّن `serviceType` على الفاتورة و`unit` على البنود (في `computeTotals`/`create`/`update`).
- `frontend/src/pages/Invoices.tsx` (مكوّن `CreateInvoice`): أضف قائمة «نوع الخدمة» (القيم الثلاث)، وقائمة «الوحدة» بجانب كل بند (طن/درب/يومية)، وأرسلها في `POST /invoices`. اعرض القيم في قائمة/تفاصيل الفاتورة.

> القيم تُخزَّن كنص عربي مباشرة (لا حاجة لـ enums).

## 7. مهام مقترحة لاحقة (المرحلة التالية)
- **Phase F — Invoice Workflow Completion:** تفعيل `customerNumber` فريد، تحديد نوع العميل الافتراضي، `deliveryDate`، تحسينات Excel للفاتورة.
- استيراد المعدّات/العقود/العملاء (الأدوات جاهزة، فقط تشغيل الأوامر بملفات المستخدم).
- ربط صفحة الرواتب باستيراد دوري بدل الثابت (جاهزة فعليًا).
- طباعة الفاتورة كـ PDF عبر صفحة طباعة مخصّصة (مثل `/print/:type`).
- تحسينات تقارير لكل وحدة.

## 8. Production HEAD & Stable Tags
- **Production HEAD (2026-06-17):** `stable-ui-consistency-phase1-buttons-v1` (Merge: `b50036c`)
- الوسوم المستقرة السابقة: `stable-expenses-enhancement-phase-a-v1`, `stable-customer-quality-pack-v1`, `stable-export-standardization-pack-v1`, `stable-invoice-customer-price-filter-v1`, `stable-operations-workflow-pack-v1`, `stable-unified-financial-completion-pack-v1`, `stable-financial-integrity-pack-v1`, `stable-business-alignment-pack-v1`

## 9. ملاحظات تشغيلية من تجربة المستخدم
- مكافح الفيروسات لديه **McAfee** كان يحذف `electron.exe`؛ الحل: استثناء مجلد المشروع في McAfee أو إيقاف الفحص الحقيقي عند تثبيت Electron، واستخدام مرآة التنزيل `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`.
- نسخة Node لدى المستخدم حديثة (v24)؛ يُفضّل Node 20 LTS.
- ملفات Excel قد تحتوي صف عنوان كبير وأوراق متعددة — أدوات الاستيراد تتعامل مع ذلك تلقائيًا.

---

## 9. حالة المشروع الحالية

| البند | القيمة |
|---|---|
| **Current HEAD** | `b50036c` |
| **Stable Tag** | `stable-ui-consistency-phase1-buttons-v1` |
| **Latest Production State** | UI Consistency Pack Phase 1: توحيد أزرار 8 صفحات — نص تحميل عربي، ترتيب page-head موحّد (Add→Export)، حذف inline styles من الأزرار، type="button" على 18 زراً. 349 اختبار إجمالاً. |
| **Next Recommended Phase** | Phase F — إكمال سير عمل الفاتورة: `customerNumber` فريد، `deliveryDate`، ربط `paymentMethod` بواجهة الفواتير والرواتب، صفحة دفتر اليومية GL. |
