/**
 * إقرار دين موظف — الثوابت الخاصة بهذا المستند.
 *
 * ═══ لماذا هنا لا في إعدادات الشركة العامة ═══
 * هذه القيم تخصّ **الدائن في هذا الإقرار** (الشركة بصفتها صاحب العمل المُقرِض). لا
 * يوجد اليوم في إعدادات النظام حقلٌ للسجل التجاري ولا للرقم الموحّد، وإنشاء إعداد
 * عام جديد من أجل نموذج واحد يوسّع سطحًا مشتركًا لأجل مستهلك واحد. فحين يُضاف مصدر
 * رسمي موحّد لبيانات الشركة، يصبح هذا الملف نقطة تحويل واحدة بدل ملاحقة نصوص متناثرة.
 *
 * لا تُكرَّر أي من هذه القيم نصًّا حرفيًا في أي ملف آخر — يحرس ذلك اختبار.
 */
import { DEBT_ACK_CONTENT_AR } from './content.ar';

/** رقم السجل التجاري للشركة — يُطبع في خانة «السجل التجاري» في القوالب الثلاثة. */
export const CREDITOR_COMMERCIAL_REGISTRATION_NO = '509001';

/**
 * الرقم الموحّد للشركة — يُطبع في خانة «الرقم المدني/الرقم الموحد».
 * الخانة في ملفات Word واحدة للاثنين (`الرقم المدني/الرقم الموحد` ·
 * `Civil ID / Unified No.` · `सिविल आईडी / यूनिफाइड नं.`)، والمنشأة لها رقم موحّد
 * لا رقم مدني — فالقيمة المطبوعة هي الرقم الموحّد.
 */
export const CREDITOR_UNIFIED_NUMBER = '554731';

/** الاسم العربي للشركة — نفس ثابت `forms/shared/formStyles`، يُقرأ منه لا يُنسخ. */
export { COMPANY_NAME as CREDITOR_NAME_AR } from '../shared/formStyles';

/**
 * الاسم القانوني الإنجليزي للشركة — يُملأ في القالبين الإنجليزي والهندي، فيمنع أشهر
 * تسرّب للعربية فيهما (اسم الدائن). نصّه مطابق حرفيًا لما تعرضه ترويسة النماذج
 * (`forms/shared/FormHeader`) وقالبا عرض السعر وطلب الشراء؛ يُحرَس بالتطابق في اختبار.
 */
export const CREDITOR_NAME_LATIN = 'ALAMANAR ALDAWLIYA FOR STREET CONSTRUCTION & MAINTENANCE CO., W.L.L.';

/**
 * آيبان الشركة الذي يُطبع في الإقرار — **ثابت ومصدر واحد**.
 *
 * لا يتبع الموظف ولا اللغة ولا مسودّة قديمة: يُفرَض في `withFixedCreditorData` مع
 * السجل التجاري والرقم الموحّد، فيُصحَّح بعد الملء التلقائي وبعد تحميل أي مسودّة
 * وبعد مسح الحقول. ويُعرض في شاشة الإدخال للقراءة فقط، فلا يوحي بأنه قابل للتعديل.
 *
 * لاتينيّ كما هو في اللغات الثلاث — رقم حساب لا يُترجم ولا تُبدَّل أرقامه.
 */
export const CREDITOR_IBAN = 'KW78NBOK0000000000002039042550';

/**
 * القيم الافتراضية **الخاصة بهذا المستند** — لا بيانات شركة عامة ولا master data.
 *
 * تُملأ عند إنشاء نموذج جديد وحين يكون الحقل فارغًا فقط (`applyAutofill` لا تدهس
 * إدخالًا قائمًا)، وتبقى قابلة للتعديل، ويُحفظ التعديل في مسودّة المستند. تغيير
 * الموظف لا يمسّها لأن الملء لا يكتب فوق حقل مكتوب.
 *
 * والنظير اللاتيني للممثل القانوني مكتوبٌ صراحةً لا منقولٌ حرفيًا من العربية: القالبان
 * الإنجليزي والهندي لا يقبلان حرفًا عربيًا (يمنعه الحارس اللغوي قبل الطباعة)، والاسم
 * العربي الأصلي يبقى كما هو في القالب العربي.
 */
export const DEFAULT_CREDITOR_REPRESENTATIVE = 'حسن فلاح نايف-مدير';
export const DEFAULT_CREDITOR_REPRESENTATIVE_LATIN = 'HASSAN FALAH NAYEF - MANAGER';
/** وسيلة الاتصال الافتراضية — لاتينية أصلًا، فتصلح للقوالب الثلاثة كما هي. */
export const DEFAULT_CREDITOR_CONTACT = 'tel:94404401';

/**
 * عدد صفوف **صفحة ملحق واحدة** = عدد صفوف «ملحق (أ) — جدول السداد» في ملفات Word.
 *
 * **مشتقّ لا مكتوب**: يُقرأ من حزمة المحتوى نفسها، فلو تغيّر الملحق يومًا تغيّر معه بلا
 * رقم منسوخ يتخلّف عنه. اختبار الأمانة يثبت أن الحزم الثلاث تعلن العدد نفسه وأنه
 * يطابق ملفات DOCX.
 *
 * هذا رقمُ **تصميم صفحة**، لا حدُّ عمل. ما زاد عن اثني عشر قسطًا يُقسَّم على صفحات
 * ملحق إضافية بنفس التصميم، لا يُرفض.
 */
export const ROWS_PER_ANNEX_PAGE = DEBT_ACK_CONTENT_AR.annexRowCount;

/**
 * أقصى عدد أقساط يقبله النموذج.
 *
 * ═══ لماذا لم يعد اثني عشر ═══
 * كان `MAX_INSTALLMENTS` يساوي عدد صفوف صفحة الملحق، فصار قيدَ **عمل** مصدره قيدُ
 * **تخطيط صفحة**: من أراد أربعة عشر قسطًا مُنع لأن الورقة تتّسع لاثني عشر صفًّا. وهذا
 * خلطٌ بين شيئين لا علاقة لأحدهما بالآخر — قرار مالك المنتج: يُفصلان.
 *
 * ═══ ولماذا حدٌّ أصلًا ═══
 * ليس حدًّا قانونيًا ولا يزعم أنه كذلك: هو سياجٌ تقنيّ حول رقمٍ يُدخله إنسان. كل قسط
 * فوق الاثني عشر يضيف صفًّا، وكل اثني عشر صفًّا تضيف **صفحتين** إلى المطبوع (نسختا
 * الملحق). فرقمٌ عبثيّ كـ`100000` لا يُنتج خطأً بل مستندًا لا يُطبع ولا يُراجَع.
 *
 * **120** = عشر سنوات من الأقساط الشهرية. يتجاوز أي سلفة موظف معقولة بفارق واسع،
 * ويحدّ المطبوع عند عشرين صفحة ملحق (عشر لكل نسخة) زائد صفحتَي المستند. وتغييره سطر
 * واحد هنا إن قرّر مالك المنتج غير ذلك.
 */
export const MAX_INSTALLMENTS = 120;

/** أقل عدد أقساط مقبول. */
export const MIN_INSTALLMENTS = 1;
