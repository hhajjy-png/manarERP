import { CSSProperties } from 'react';
import { labelCell, sectionHeader, tableCell } from '../../shared/formStyles';

/**
 * أنماط القالب الثنائي English + हिन्दी — **توسعة** لا تعديل.
 *
 * كل ثابت هنا مبنيّ فوق نظيره في `forms/shared/formStyles.ts` بالانتشار (spread)،
 * فلا يُمَسّ الأصل ولا أي نموذج يستهلكه.
 *
 * ── لماذا سطر واحد أفقي (لا سطران) ──────────────────────────────────────────
 * النسخة الأولى وضعت الهندية في سطر **مستقل** تحت الإنجليزية. أضاف ذلك سطرًا
 * ثانيًا لكل تسمية وكل عبارة، فتجاوز النموذج صفحة A4 وانتقل إلى صفحتين. الحلّ
 * هو دمج الطرفين في سطر واحد يفصلهما `—`: المعلومة كاملة كما هي (لا حذف ولا
 * اختصار)، والارتفاع يعود إلى ارتفاع القالب الإنجليزي تقريبًا.
 */

/**
 * الفاصل الموحَّد بين الإنجليزية والهندية في كل موضع من القالب.
 * ثابت واحد كي لا ينحرف الشكل بين تسمية وأخرى.
 */
export const EN_HI_SEPARATOR = ' — ';

/**
 * عرض خلية التسمية: 180 → 280.
 *
 * التسمية صارت `English — हिन्दी` على سطر واحد، فطولها هو مجموع الطرفين
 * والفاصل. أطول تسمية في هذا النموذج هي
 * «Expected Return Date — अपेक्षित वापसी तिथि»، و280px هي أصغر قيمة تستوعبها
 * بلا التفاف — والالتفاف هنا يُعيد السطر الثاني الذي جئنا لإزالته. يبقى لعمود
 * القيمة ~450px، وهو أكثر من كافٍ لأطول قيمة يحملها النموذج.
 */
export const labelCellEnHi: CSSProperties = {
  ...labelCell,
  width: 280,
  textAlign: 'left',
  // 6px → 5px: تقليم رأسي طفيف يتراكم على 11 صفًا. الخط لم يصغر.
  padding: '5px 14px',
};

/** خلية القيمة تتبع نفس تقليم الحشو الرأسي حتى يبقى الصفّان متحاذيين. */
export const valueCellEnHi: CSSProperties = {
  ...tableCell,
  flex: 1,
  color: '#0f172a',
  padding: '5px 14px',
};

/** ترويسة القسم: `EN — HI` على سطر واحد. */
export const sectionHeaderEnHi: CSSProperties = {
  ...sectionHeader,
  textAlign: 'left',
  padding: '6px 14px',
};

/** الجزء الهندي داخل ترويسة القسم — نفس السطر، أخفّ وأصغر قليلًا. */
export const sectionHeaderHiPart: CSSProperties = {
  fontSize: 11.5,
  fontWeight: 600,
  opacity: 0.85,
};

/**
 * الجزء الهندي بعد أي تسمية إنجليزية — **سطري** (`inline`) لا `block`.
 *
 * 11px (لا 10.5) لأن الديفاناغارية بجوار لاتينية 13px تحتاج هامش وضوح؛ ولأن
 * العنصر سطري فإن حجمه لا يضيف أي ارتفاع ما دام أقل من ارتفاع سطر الصف.
 */
export const hiLabelText: CSSProperties = {
  fontSize: 11,
  fontWeight: 600,
  color: '#475569',
};

/** الجزء الهندي بعد قيمة تعدادية (نوع الإجازة) — سطري كذلك. */
export const hiValueText: CSSProperties = {
  fontSize: 11.5,
  fontWeight: 400,
  color: '#475569',
};

/** الجزء الهندي داخل فقرة الإفادة/التعهد — يكمل نفس الفقرة بلا سطر جديد. */
export const hiParagraphText: CSSProperties = {
  fontSize: 11.5,
  color: '#475569',
};

/**
 * ارتفاع السطر داخل صفوف الجدول.
 *
 * الديفاناغارية تحمل ماترات فوق الحرف وتحته، فارتفاع 1.5 الافتراضي في
 * `tableCell` قد يقصّها عند دمجها في سطر لاتيني. 1.65 يمنح المتّسع اللازم
 * ويبقى أقلّ بكثير من كلفة سطر ثانٍ كامل.
 */
export const enHiLineHeight = 1.65;
